 নিচে একটি **কো-অপারেটিভ রাইডশেয়ারিং অ্যাপ** বানানোর বিস্তারিত ব্লুপ্রিন্ট দিলাম। এটি এমনভাবে সাজানো, যেন আপনি শুধু একটি “রাইড অ্যাপ” নয়, বরং **ড্রাইভার, যাত্রী ও কর্মী/উদ্যোক্তাদের মালিকানাধীন একটি ডিজিটাল সমবায় প্ল্যাটফর্ম** তৈরি করতে পারেন। ধরে নিচ্ছি প্রাথমিক লক্ষ্য: **বাংলাদেশ/দক্ষিণ এশিয়া প্রেক্ষাপট, শহরভিত্তিক পাইলট, মোবাইল অ্যাপ + ওয়েব অ্যাডমিন + সমবায় গভর্নেন্স**।

---

# ১. মূল ধারণা: কো-অপারেটিভ রাইডশেয়ারিং কী?

সাধারণ রাইডশেয়ারিং অ্যাপে সাধারণত একটি কোম্পানি মালিক হয়, ড্রাইভার ও যাত্রী শুধু ব্যবহারকারী হয়। কিন্তু **কো-অপারেটিভ রাইড অ্যাপে**:

- ড্রাইভাররা শুধু “কন্ট্রাক্টর” নয়, তারা **মালিক-সদস্য** হতে পারেন।
- যাত্রীরা চাইলে **সদস্য** হয়ে অংশীদারিত্ব, ভোট, লভ্যাংশ বা গভর্নেন্সে অংশ নিতে পারেন।
- মুনাফা শুধু মালিকের কাছে যায় না; একটি অংশ **রিজার্ভ**, একটি অংশ **সেবা/প্রযুক্তি উন্নয়ন**, একটি অংশ **সদস্যদের ব্যবহার অনুপাতে প্যাট্রোনেজ**, এবং আইনসম্মতভাবে শেয়ার লভ্যাংশে বণ্টন হয়।
- কমিশন, ফেয়ার ফর্মুলা, সেফটি নীতি, ড্রাইভার কল্যাণ—এগুলো আরও স্বচ্ছ ও গণতান্ত্রিকভাবে নির্ধারণ করা যায়।

**মূল স্লোগান হতে পারে:**  
“যারা চালায়, যারা চড়ে, তারাই মালিক।”

---

# ২. প্রথম করণীয়: সমবায় কাঠামো ঠিক করা

প্রযুক্তির আগে সমবায়ের আইনি ও গভর্নেন্স কাঠামো ঠিক করা জরুরি।

## ২.১ সদস্য ক্যাটাগরি

আপনি এক বা একাধিক ক্যাটাগরি রাখতে পারেন:

| সদস্য ক্যাটাগরি | ভূমিকা | সম্ভাব্য অধিকার |
|---|---|---|
| ড্রাইভার সদস্য | রাইড প্রদানকারী | ভোট, লভ্যাংশ, কমিশন নীতিতে মতামত |
| যাত্রী সদস্য | সেবা গ্রহণকারী | ভোট, গভর্নেন্স, লভ্যাংশ/রিবেট |
| কর্মী/উদ্যোক্তা সদস্য | অ্যাপ পরিচালনা, সাপোর্ট, টেক টিম | ভোট, কর্মী-মালিকানা |
| বিনিয়োগকারী সদস্য | মূলধন দেয় | সীমিত লভ্যাংশ, সীমিত ভোট |

সবচেয়ে গুরুত্বপূর্ণ নীতি: **এক সদস্য, এক ভোট**। বড় বিনিয়োগকারী থাকলেও ভোট যেন গণতান্ত্রিক থাকে।

---

## ২.২ মূলধন ও শেয়ার

উদাহরণ কাঠামো:

- প্রতি সদস্যের জন্য ন্যূনতম শেয়ার: ৫০০–৫,০০০ টাকা
- ড্রাইভার সদস্য হলে গাড়ি/মোটরসাইকেল ভেরিফিকেশন বাধ্যতামূলক
- শেয়ার কিনলে সদস্যপদ সক্রিয় হবে
- সদস্যপদ বাতিল/স্থগিতের নিয়ম বাইলজে থাকতে হবে
- সদস্য ত্যাগ করলে শেয়ার ফেরতের নিয়ম ও লিকুইডিটি ফান্ড থাকতে হবে

---

## ২.৩ মুনাফা বণ্টন মডেল

সমবায় আইন ও বাইলজ অনুযায়ী পরিবর্তন করতে হবে। একটি সম্ভাব্য নীতিমালা:

| খাত | শতাংশ | উদ্দেশ্য |
|---|---:|---|
| আইনসম্মত রিজার্ভ ফান্ড | ২০–৩০% | ঝুঁকি ও স্থিতিশীলতা |
| প্রযুক্তি ও উন্নয়ন ফান্ড | ১০–২০% | অ্যাপ আপগ্রেড, সিকিউরিটি |
| ড্রাইভার প্যাট্রোনেজ | ২৫–৩৫% | ড্রাইভারদের রাইড সংখ্যা/আয় অনুপাতে |
| যাত্রী প্যাট্রোনেজ | ১০–২০% | যাত্রীদের ব্যবহার অনুপাতে |
| কমিউনিটি/কল্যাণ ফান্ড | ৫–১০% | দুর্ঘটনা, শিক্ষা, সামাজিক কাজ |
| শেয়ার লভ্যাংশ | ০–১০% | সীমিত হারে, আইন অনুযায়ী |

**গুরুত্বপূর্ণ:** মুনাফা বণ্টনের আগে স্থানীয় সমবায় আইন, ট্যাক্স ও রেজিস্ট্রার অফিসের নিয়ম জেনে নিন।

---

# ৩. আইনি ও রেগুলেটরি চেকলিস্ট

এটি আইনি পরামর্শ নয়। বাংলাদেশ বা আপনার দেশের আইন অনুযায়ী আইনজীবী ও সমবায় বিভাগের সঙ্গে কথা বলুন।

## ৩.১ সমবায় নিবন্ধন

সম্ভাব্য ধাপ:

1. প্রতিষ্ঠাতা সদস্য দল গঠন
2. সমবায়ের নাম নির্বাচন
3. বাইলজ/গঠনতন্ত্র তৈরি
4. সদস্য শেয়ার মূলধন সংগ্রহ
5. সমবায় বিভাগে নিবন্ধন আবেদন
6. নিবন্ধন সনদ প্রাপ্তি
7. ব্যাংক অ্যাকাউন্ট খোলা
8. টিন/ভ্যাট/ট্যাক্স নিবন্ধন, প্রযোজ্য হলে
9. বার্ষিক রিটার্ন ও হিসাব নিরীক্ষা প্রক্রিয়া

---

## ৩.২ রাইডশেয়ারিং/ট্রান্সপোর্ট অনুমতি

প্রযোজ্য হলে:

- সড়ক পরিবহন কর্তৃপক্ষ/সিটি কর্পোরেশন/স্থানীয় কর্তৃপক্ষের অনুমতি
- বাণিজ্যিক লাইসেন্স
- ড্রাইভার লাইসেন্স ও ভেরিফিকেশন
- গাড়ির ফিটনেস/রেজিস্ট্রেশন
- বীমা
- যাত্রী নিরাপত্তা নীতি
- ডেটা সুরক্ষা নীতি
- পেমেন্ট/ওয়ালেট নিয়ম, বিশেষ করে টাকা জমা রাখলে আর্থিক প্রতিষ্ঠান/পেমেন্ট লাইসেন্স সংক্রান্ত বিষয়

---

## ৩.৩ ডেটা ও প্রাইভেসি

যেহেতু লোকেশন, ফোন নম্বর, যাত্রা তথ্য সংবেদনশীল:

- প্রাইভেসি পলিসি
- ডেটা রিটেনশন নীতি
- ডেটা মুছে ফেলার নিয়ম
- সদস্য/ব্যবহারকারীর সম্মতি
- ফোন নম্বর মাস্কিং
- লোকেশন ট্র্যাকিং শুধু ট্রিপ চলাকালীন সীমিত রাখা
- আইনি চাহিদা ছাড়া ডেটা প্রকাশ না করা

---

# ৪. পণ্যের রূপরেখা: অ্যাপে কী কী থাকবে?

আপনার কমপক্ষে চারটি অংশ থাকবে:

1. **যাত্রী অ্যাপ**
2. **ড্রাইভার অ্যাপ**
3. **অ্যাডমিন/অপারেশন ওয়েব প্যানেল**
4. **সমবায় সদস্য পোর্টাল**

---

## ৪.১ যাত্রী অ্যাপ

### মৌলিক ফিচার

- ফোন নম্বর/ইমেইল দিয়ে সাইনআপ
- OTP লগইন
- প্রোফাইল, প্রোফাইল ছবি
- গন্তব্য সার্চ, ম্যাপে পিন করা
- রাইড ফেয়ার আনুমানিক দেখা
- রাইড টাইপ নির্বাচন: বাইক, অটো, কার, প্রিমিয়াম, নারী-চালিত, শেয়ারড ইত্যাদি
- রাইড রিকোয়েস্ট
- ড্রাইভার অ্যাসাইনমেন্ট
- লাইভ ড্রাইভার ট্র্যাকিং
- রাইড শেয়ারিং: পরিবার/বন্ধুকে লাইভ লিংক পাঠানো
- কল/মেসেজ, ফোন নম্বর মাস্কিং
- রাইড শুরু/শেষ নিশ্চিতকরণ
- রেটিং ও রিভিউ
- পেমেন্ট: ক্যাশ, ওয়ালেট, মোবাইল মানি, কার্ড
- ট্রিপ হিস্ট্রি
- রসিদ/ইনভয়েস
- প্রিয় ঠিকানা
- প্রোমো/সদস্য রিবেট
- নিরাপত্তা বাটন: SOS, পুলিশ/সাপোর্ট/জরুরি যোগাযোগ
- অভিযোগ/রিপোর্ট

### সদস্য হলে অতিরিক্ত

- সদস্যপদ আবেদন
- শেয়ার কেনা
- প্যাট্রোনেজ/লভ্যাংশ দেখা
- গভর্নেন্স পোর্টাল
- ভোট/মিটিং নোটিশ
- সমবায়ের আর্থিক সারাংশ

---

## ৪.২ ড্রাইভার অ্যাপ

### মৌলিক ফিচার

- ড্রাইভার নিবন্ধন
- লাইসেন্স, গাড়ির কাগজ, বীমা আপলোড
- ভেরিফিকেশন স্ট্যাটাস
- অনলাইন/অফলাইন টগল
- রাইড অফার গ্রহণ/বাতিল
- যাত্রীর পিকআপ লোকেশন
- নেভিগেশন
- ট্রিপ স্ট্যাটাস: পিকআপে যাচ্ছি, পৌঁছেছি, ট্রিপ শুরু, ট্রিপ শেষ
- আয়ের ড্যাশবোর্ড
- দৈনিক/সাপ্তাহিক পেমেন্ট হিস্ট্রি
- কমিশন/প্ল্যাটফর্ম ফি স্বচ্ছভাবে দেখা
- ক্যাশ কালেকশন রিপোর্ট
- ওয়ালেট
- রেটিং
- ইনসেনটিভ/বোনাস
- দুর্ঘটনা/জরুরি বাটন
- যাত্রী নিরাপত্তা চেকলিস্ট
- ট্রেনিং মডিউল
- সমবায় সদস্যপদ
- শেয়ার/লভ্যাংশ ড্যাশবোর্ড

### গুরুত্বপূর্ণ ড্রাইভার-বান্ধব নীতি

- কমিশন ফর্মুলা স্বচ্ছ হতে হবে
- অ্যালগরিদম কেন কোনো রাইড দিল/দিল না, তা ড্রাইভার বুঝতে পারবে
- মিথ্যা রিপোর্টের বিরুদ্ধে আপিল
- দুর্ঘটনা বীমা
- অতিরিক্ত কাজ প্রতিরোধে ফ্যাটিগ সতর্কতা
- ক্যাশ সংগ্রহের নিরাপত্তা ও হিসাব সহজ করা

---

## ৪.৩ অ্যাডমিন/অপারেশন প্যানেল

### মূল মডিউল

- ড্যাশবোর্ড: লাইভ রাইড, রেভিনিউ, সক্রিয় ড্রাইভার, যাত্রী
- ইউজার ম্যানেজমেন্ট
- ড্রাইভার ভেরিফিকেশন
- গাড়ি/ডকুমেন্ট অডিট
- রাইড মনিটরিং
- ম্যানুয়াল রাইড অ্যাসাইনমেন্ট
- প্রাইসিং কনফিগারেশন
- জোন/সার্জ নিয়ন্ত্রণ
- প্রোমো/কুপন
- পেমেন্ট/রিফান্ড
- কমিশন সেটিং
- ড্রাইভার পেআউট
- অভিযোগ/ডিসপিউট
- সেফটি ইনসিডেন্ট ম্যানেজমেন্ট
- মেম্বারশিপ ম্যানেজমেন্ট
- শেয়ার লেজার
- প্যাট্রোনেজ ক্যালকুলেশন
- ভোট/মিটিং ম্যানেজমেন্ট
- নোটিশ বোর্ড
- রিপোর্ট ও অডিট
- রোল ও পারমিশন

---

## ৪.৪ সমবায় সদস্য পোর্টাল

এটি কো-অপারেটিভ অ্যাপের অন্যতম পার্থক্য।

### ফিচার

- সদস্য প্রোফাইল
- শেয়ার সার্টিফিকেট
- শেয়ার কেনার হিস্ট্রি
- সদস্যপদ ফি
- ক্যাপিটাল অ্যাকাউন্ট
- প্যাট্রোনেজ পয়েন্ট
- বার্ষিক মুনাফা বণ্টন
- ডিভিডেন্ড হিস্ট্রি
- মিটিং নোটিশ
- ই-ভোট বা মতামত
- সমবায়ের আর্থিক রিপোর্ট
- বাইলজ/নীতিমালা ডকুমেন্ট
- অভিযোগ/আপিল
- সদস্যপদ স্থগিত/সক্রিয় স্ট্যাটাস

---

# ৫. রাইড লাইফসাইকেল

একটি রাইডের স্টেট মেসিন এরকম হতে পারে:

| স্টেট | বর্ণনা |
|---|---|
| REQUESTED | যাত্রী রাইড চেয়েছে |
| QUOTED | ফেয়ার আনুমানিক দেওয়া হয়েছে |
| SEARCHING | ড্রাইভার খোঁজা হচ্ছে |
| OFFERED | নির্দিষ্ট ড্রাইভারকে অফার পাঠানো হয়েছে |
| ACCEPTED | ড্রাইভার গ্রহণ করেছে |
| ARRIVED | ড্রাইভার পিকআপে পৌঁছেছে |
| PICKUP_CONFIRMED | যাত্রী উঠেছে |
| ONGOING | ট্রিপ চলছে |
| NEAR_DROP | গন্তব্যের কাছে |
| COMPLETED | ট্রিপ শেষ |
| PAYMENT_PENDING | পেমেন্ট বাকি |
| PAID | পেমেন্ট সম্পন্ন |
| RATED | রেটিং সম্পন্ন |
| CANCELLED | বাতিল |
| DISPUTED | বিরোধ/অভিযোগ |
| INCIDENT | দুর্ঘটনা/নিরাপত্তা ঘটনা |

---

# ৬. টেকনিক্যাল আর্কিটেকচার

প্রাথমিক পর্যায়ে **মডুলার মনোলিথ** দিয়ে শুরু করা ভালো। পরে প্রয়োজনে মাইক্রোসার্ভিসে যাওয়া যায়।

## ৬.১ উচ্চ-স্তরের আর্কিটেকচার

```
যাত্রী অ্যাপ  +  ড্রাইভার অ্যাপ  +  ওয়েব অ্যাডমিন
                ↓
            API Gateway / REST + WebSocket
                ↓
        ব্যাকএন্ড অ্যাপ্লিকেশন
        ├─ Auth Service
        ├─ Member/Co-op Service
        ├─ Ride Service
        ├─ Matching/Dispatch Service
        ├─ Pricing Service
        ├─ Payment/Ledger Service
        ├─ Notification Service
        ├─ Safety Service
        ├─ Admin/Reporting Service
        └─ Document Verification Service
                ↓
┌──────────────┬──────────────┬──────────────┐
│ PostgreSQL + │ Redis        │ Message Queue │
│ PostGIS      │ Cache/Geo    │ RabbitMQ/Kafka│
└──────────────┴──────────────┴──────────────┘
                ↓
    Maps/Routing, SMS/Push, Payment Gateway,
    Object Storage, Monitoring, Logs
```

---

## ৬.২ সম্ভাব্য টেক স্ট্যাক

### মোবাইল অ্যাপ

- **Flutter**: একটি কোডবেজে Android + iOS, দ্রুত ডেভেলপমেন্ট
- অথবা **React Native**
- ড্রাইভার অ্যাপে ব্যাকগ্রাউন্ড লোকেশন, লো-ডাটা মোড, অফলাইন কুই জরুরি

### ব্যাকএন্ড

- **Node.js + NestJS/Express** অথবা
- **Python + Django REST Framework / FastAPI** অথবা
- **Go + PostgreSQL**

### ডেটাবেস

- **PostgreSQL + PostGIS**: লোকেশন, জোন, জিওফেন্সিং
- **Redis**: ক্যাশ, রেট লিমিটিং, রিয়েলটাইম লোকেশন, সেশন
- প্রয়োজনে **TimescaleDB** বা অ্যানালিটিক্স ডেটাবেস

### রিয়েলটাইম

- WebSocket / Socket.IO
- MQTTও ব্যবহার করা যায়, বিশেষ করে ড্রাইভার লোকেশন স্ট্রিমের জন্য

### ম্যাপ ও রাউটিং

শুরুতে দুটি পথ:

1. **সহজ ও নির্ভরযোগ্য কিন্তু খরচ বেশি:**
   - Google Maps Platform
   - Distance Matrix, Directions, Places, Geocoding

2. **খরচ কমানোর জন্য ওপেন-সোর্স/সেমি-ওপেন:**
   - MapLibre GL
   - OpenStreetMap
   - OSRM / Valhalla / GraphHopper
   - Nominatim বা বাণিজ্যিক জিওকোডিং
   - বাংলাদেশে ঠিকানা ডেটা উন্নত করতে নিজস্ব ঠিকানা ডেটা/ল্যান্ডমার্ক যোগ করা লাগবে

### নোটিফিকেশন

- Firebase Cloud Messaging
- Apple Push Notification Service
- SMS Gateway: স্থানীয় প্রোভাইডার বা ইনফোবিপ/টুইলিও
- ইমেইল: SES/Resend/Postmark

### পেমেন্ট

- ক্যাশ
- মোবাইল মানি: বিকাশ/নগদ/রকেট, যদি প্রযোজ্য হয়
- কার্ড: পেমেন্ট গেটওয়ে ব্যবহার করুন; নিজে কার্ড ডেটা রাখবেন না
- ওয়ালেট: লেজার-ভিত্তিক, কিন্তু আর্থিক নিয়ম মেনে
- পিসিআই-ডিএসএস কমপ্লায়েন্স

### অবকাঠামো

- শুরুতে: একটি ক্লাউড ভিপিএস বা ম্যানেজড ভিএম
- পরে: Docker + Kubernetes
- CI/CD: GitHub Actions/GitLab CI
- Monitoring: Prometheus + Grafana
- Logs: Loki/ELK
- Error tracking: Sentry

---

# ৭. মূল সার্ভিস/মডিউল ভেঙে দেখা

## ৭.১ অথেনটিকেশন ও প্রোফাইল

- ফোন + OTP
- ইমেইল + পাসওয়ার্ড ঐচ্ছিক
- রোল: Rider, Driver, Admin, Support, Auditor, Board, Member
- KYC স্ট্যাটাস
- ডিভাইস বাইন্ডিং, বিশেষ করে ড্রাইভার অ্যাপে
- ফোন পরিবর্তন করলে সেকেন্ডারি ভেরিফিকেশন

---

## ৭.২ মেম্বারশিপ/কো-অপারেটিভ লেজার

সবচেয়ে গুরুত্বপূর্ণ পার্থক্য। প্রতিটি আর্থিক ঘটনার ডাবল-এন্ট্রি ধাঁচের লেজার রাখা ভালো।

### প্রয়োজনীয় সত্তা

- Member
- Share
- ShareTransaction
- CapitalAccount
- PatronageAccount
- Dividend
- GeneralMeeting
- Motion
- Vote
- BylawDocument
- MemberWallet
- MemberContribution
- RefundRequest

---

## ৭.৩ ট্রিপ সার্ভিস

- রাইড রিকোয়েস্ট
- ফেয়ার এস্টিমেট
- ড্রাইভার সার্চ রেডিয়াস
- অফার টাইমআউট
- ট্রিপ স্ট্যাটাস ইভেন্ট
- রুট পয়েন্ট
- ট্রিপ টাইমলাইন
- ক্যানসেলেশন কারণ
- ডিসপিউট
- অডিট লগ

---

## ৭.৪ ম্যাচিং/ডিসপ্যাচ

প্রাথমিক মডেল:

1. রাইড রিকোয়েস্ট আসবে
2. কাছের সক্রিয় ড্রাইভারদের তালিকা তৈরি হবে
3. স্কোরিং করে সেরা ড্রাইভার বেছে নেওয়া হবে
4. ড্রাইভারকে অফার পাঠানো হবে
5. নির্দিষ্ট সেকেন্ডে গ্রহণ না করলে পরের ড্রাইভার
6. ৩–৫ বার চেষ্টা করে না পেলে যাত্রীকে জানানো হবে

### স্কোরিং উদাহরণ

```
score =
  w1 × distance_to_pickup
+ w2 × eta
+ w3 × driver_rating
+ w4 × acceptance_rate
+ w5 × idle_time_bonus
+ w6 × co_op_priority
− penalty × recent_cancellations
```

তবে কো-অপারেটিভ হিসেবে শুধু “কাছের ড্রাইভার” নয়, **ন্যায্য বণ্টন**ও বিবেচনা করুন।

---

## ৭.৫ প্রাইসিং

স্বচ্ছ ফেয়ার ফর্মুলা থাকা ভালো।

উদাহরণ:

```
fare =
  base_fare
+ per_km × distance_km
+ per_minute × trip_duration_min
+ demand_adjustment
+ tolls/parking_extra
```

সীমা:

- minimum_fare
- maximum_cap
- cancellation_fee, যদি প্রযোজ্য হয়
- night_fee
- peak_time_cap

প্ল্যাটফর্ম ফি:

```
platform_fee = fare × platform_percentage
driver_net = fare − platform_fee − payment_gateway_fee
```

কো-অপারেটিভ হিসেবে ফর্মুলা সদস্যদের কাছে প্রকাশ করাই ভালো।

---

## ৭.৬ পেমেন্ট ও লেজার

প্রতিটি লেনদেনের জন্য আলাদা লেজার এন্ট্রি রাখুন।

উদাহরণ:

| ঘটনা | ডেবিট | ক্রেডিট |
|---|---|---|
| যাত্রী ক্যাশে পেমেন্ট করল | ড্রাইভার ক্যাশ কালেকশন | রাইড রেভিনিউ |
| ড্রাইভার প্ল্যাটফর্ম ফি পরিশোধ করল | প্ল্যাটফর্ম ফি অ্যাকাউন্ট | ড্রাইভার ওয়ালেট/ক্যাশ অ্যাডজাস্টমেন্ট |
| যাত্রী কার্ডে পেমেন্ট করল | পেমেন্ট গেটওয়ে রিসিভেবল | রাইড রেভিনিউ |
| ড্রাইভার পেআউট | ড্রাইভার পেয়েবল | ব্যাংক/মোবাইল মানি |
| সদস্য শেয়ার কিনল | ব্যাংক | শেয়ার ক্যাপিটাল |

### পেমেন্ট পদ্ধতি

- ক্যাশ: বাংলাদেশে শুরুতে সবচেয়ে বাস্তবসম্মত
- মোবাইল মানি: বিকাশ/নগদ/রকেট
- কার্ড: পরে
- কর্পোরেট অ্যাকাউন্ট: পরে
- ওয়ালেট: সতর্কভাবে, আইনি পরামর্শ নিয়ে

---

## ৭.৭ নিরাপত্তা ও সেফটি সার্ভিস

### ফিচার

- লাইভ ট্রিপ শেয়ার
- জরুরি বাটন
- জরুরি যোগাযোগ তালিকা
- ট্রিপ অ্যানোমালি ডিটেকশন: অস্বাভাবিক রুট, দীর্ঘ সময় স্থির থাকা
- ড্রাইভার ফ্যাটিগ সতর্কতা
- গতি সীমা লঙ্ঘন সতর্কতা
- দুর্ঘটনা রিপোর্ট
- ইনসিডেন্ট টিকেট
- অডিও রেকর্ডিং ঐচ্ছিক ও সম্মতিভিত্তিক
- সার্ভিসে অযাচিত আচরণের রিপোর্ট
- যাত্রী/ড্রাইভার ব্লক
- আপিল প্রক্রিয়া

### সেফটি অপারেশন SOP

1. SOS চাপলে অ্যাডমিন অ্যালার্ট
2. যাত্রী/ড্রাইভারকে কল
3. জরুরি যোগাযোগকে জানানো
4. প্রয়োজনে স্থানীয় জরুরি সেবা
5. ট্রিপ ডেটা সংরক্ষণ
6. ইনসিডেন্ট রিপোর্ট
7. পরবর্তী পদক্ষেপ: সতর্কতা, স্থগিত, বহিষ্কার, আইনি পদক্ষেপ

---

# ৮. ডেটা মডেল: মূল টেবিলগুলো

নিচে সংক্ষিপ্ত কিন্তু বাস্তব ডেটা মডেল দেওয়া হলো।

## ৮.১ ব্যবহারকারী ও সদস্য

```text
users
- id
- name
- phone
- email
- password_hash
- role
- status
- created_at

rider_profiles
- user_id
- emergency_contact_name
- emergency_contact_phone
- is_member
- preferred_language

driver_profiles
- user_id
- license_number
- license_expiry
- vehicle_id
- verification_status
- online_status
- rating
- acceptance_rate
- total_rides
- member_id
```

---

## ৮.২ সমবায় সদস্য

```text
members
- id
- user_id
- member_type
- join_date
- status
- share_capital
- member_number

shares
- id
- member_id
- share_amount
- issued_at
- status

share_transactions
- id
- member_id
- type: purchase, refund, transfer
- amount
- created_at

patronage_ledger
- id
- member_id
- period
- rides_completed
- fare_generated
- patronage_points
- payout_amount
- status

general_meetings
- id
- title
- agenda
- starts_at
- ends_at

votes
- id
- meeting_id
- member_id
- motion_id
- choice
- cast_at
```

---

## ৮.৩ ভেহিকেল

```text
vehicles
- id
- driver_id
- type: bike, auto, car, micro, scooter
- make
- model
- year
- color
- plate_number
- registration_expiry
- insurance_expiry
- fitness_status
- photos
- status
```

---

## ৮.৪ রাইড

```text
ride_requests
- id
- rider_id
- pickup_lat
- pickup_lng
- pickup_address
- drop_lat
- drop_lng
- drop_address
- ride_type
- fare_estimate_min
- fare_estimate_max
- status
- created_at

rides
- id
- request_id
- rider_id
- driver_id
- vehicle_id
- fare_amount
- platform_fee
- driver_net_amount
- payment_method
- payment_status
- distance_km
- duration_min
- started_at
- completed_at
- cancellation_reason
- rating_by_rider
- rating_by_driver
- safety_flags
- created_at

ride_events
- id
- ride_id
- event_type
- lat
- lng
- timestamp
- metadata

route_points
- id
- ride_id
- point_sequence
- lat
- lng
- timestamp
```

---

## ৮.৫ পেমেন্ট

```text
payments
- id
- ride_id
- rider_id
- amount
- method
- status
- gateway_reference
- created_at

ledger_entries
- id
- reference_type
- reference_id
- account_type
- debit
- credit
- created_at

driver_payouts
- id
- driver_id
- period_start
- period_end
- gross_amount
- deductions
- net_amount
- status
- paid_at
- payment_method

refunds
- id
- payment_id
- reason
- amount
- status
- created_at
```

---

## ৮.৬ নিরাপত্তা ও ডিসপিউট

```text
incidents
- id
- ride_id
- reported_by
- type: accident, harassment, theft, medical, other
- description
- evidence_urls
- status
- created_at

disputes
- id
- ride_id
- raised_by
- reason
- status
- resolution_notes
- resolved_by
- resolved_at

reports
- id
- user_id
- target_type
- target_id
- reason
- status
```

---

# ৯. অ্যাপ ফ্লো

## ৯.১ যাত্রীর রাইড বুকিং ফ্লো

1. যাত্রী অ্যাপ খুলছে
2. পিকআপ লোকেশন স্বয়ংক্রিয় বা ম্যাপে নির্বাচন
3. গন্তব্য লিখছে
4. রাইড টাইপ ও আনুমানিক ফেয়ার দেখছে
5. পেমেন্ট মেথড নির্বাচন
6. রাইড রিকোয়েস্ট
7. সার্ভার ড্রাইভার খুঁজছে
8. ড্রাইভার অ্যাসাইন
9. ড্রাইভারের নাম, ছবি, গাড়ি, রেটিং, প্লেট নম্বর দেখানো
10. লাইভ ট্র্যাকিং
11. রাইড শুরু
12. রাইড শেষ
13. পেমেন্ট
14. রেটিং
15. রসিদ

---

## ৯.২ ড্রাইভার রাইড গ্রহণ ফ্লো

1. ড্রাইভার অনলাইন হয়
2. লোকেশন সার্ভারকে পাঠায়
3. রাইড অফার পায়
4. দূরত্ব, আনুমানিক আয়, গন্তব্য দেখে
5. নির্দিষ্ট সময়ের মধ্যে গ্রহণ/বাতিল
6. পিকআপে নেভিগেশন
7. যাত্রী ওঠে
8. ট্রিপ শুরু
9. গন্তব্যে পৌঁছায়
10. ট্রিপ শেষ
11. পেমেন্ট কনফার্ম
12. রেটিং

---

## ৯.৩ সদস্য হওয়ার ফ্লো

1. অ্যাপে “সদস্য হোন”
2. শর্তাবলি ও বাইলজ সম্মতি
3. ব্যক্তিগত তথ্য ও জাতীয় পরিচয়/ডকুমেন্ট
4. শেয়ার কেনার জন্য পেমেন্ট
5. অ্যাডমিন/বোর্ড অনুমোদন
6. সদস্য নম্বর ও ডিজিটাল সার্টিফিকেট
7. সমবায় পোর্টাল অ্যাক্সেস

---

# ১০. ডিজাইন ও ইউএক্স নীতি

## যাত্রী অ্যাপে

- বড় বাটন, সহজ ভাষা
- ঠিকানা খোঁজা সহজ
- নিরাপত্তা ফিচার দৃশ্যমান
- কম ডেটা ব্যবহার
- অফলাইন রসিদ/হিস্ট্রি

## ড্রাইভার অ্যাপে

- বড় টেক্সট, হাই কনট্রাস্ট
- রাইড অফার কার্ডে পরিষ্কার আয়
- এক হাতে ব্যবহারযোগ্য
- নেভিগেশন দ্রুত
- ব্যাকগ্রাউন্ড লোকেশন নির্ভরযোগ্য
- ডাটা/ব্যাটারি সাশ্রয়ী

## অ্যাডমিনে

- লাইভ ম্যাপ
- ইনসিডেন্ট অ্যালার্ট
- ভেরিফিকেশন কিউ
- পেমেন্ট রিকনসিলিয়েশন
- রিপোর্ট এক্সপোর্ট

---

# ১১. প্রাইসিং, কমিশন ও ইনসেনটিভ নীতি

কো-অপারেটিভ হিসেবে সবচেয়ে বড় সুবিধা: **ন্যায্যতা ও স্বচ্ছতা**।

## ১১.১ কমিশন মডেল

উদাহরণ:

| ফেয়ার স্ল্যাব | প্ল্যাটফর্ম ফি |
|---|---:|
| ৳০–১০০ | ১০% |
| ১০১–৩০০ | ১২% |
| ৩০১+ | ১৫% |

অথবা ফ্ল্যাট:

- বাইকে ১০%
- অটোতে ১২%
- কারে ১৫%

তবে শুরুতে আকর্ষণীয় করতে কমিশন ৫–১০% রাখা যেতে পারে।

---

## ১১.২ ড্রাইভার ইনসেনটিভ

- নির্দিষ্ট সংখ্যক রাইড সম্পন্ন করলে বোনাস
- পিক আওয়ার বোনাস
- নারী ড্রাইভারদের বিশেষ সহায়তা
- নিরাপদ ড্রাইভিং স্কোর
- সদস্য ড্রাইভারদের প্যাট্রোনেজ
- বাতিল কম হলে বোনাস

---

## ১১.৩ যাত্রী রিবেট

- সদস্য যাত্রীরা মাসিক/বার্ষিক ব্যবহার অনুযায়ী রিবেট পেতে পারেন
- রেফারেল রিওয়ার্ড
- পরিবেশবান্ধব রাইডে ছাড়
- শেয়ারড রাইডে ছাড়

---

# ১২. অপারেশনাল ব্লুপ্রিন্ট

## ১২.১ ড্রাইভার অনবোর্ডিং

ধাপ:

1. আবেদন ফর্ম
2. মোবাইল নম্বর ও জাতীয় পরিচয়
3. ড্রাইভিং লাইসেন্স
4. গাড়ির কাগজ
5. বীমা
6. গাড়ির ছবি
7. ব্যাকগ্রাউন্ড চেক, সম্ভব হলে
8. প্রশিক্ষণ
9. অ্যাপ ট্রেনিং
10. পাইলট রাইড
11. সদস্যপদ ঐচ্ছিক/বাধ্যতামূলক
12. অ্যাক্টিভেশন

---

## ১২.২ ড্রাইভার প্রশিক্ষণ

বিষয়:

- অ্যাপ ব্যবহার
- রাস্তা ও নেভিগেশন
- যাত্রী সেবা
- নিরাপত্তা
- নারী যাত্রী ও সংবেদনশীলতা
- দুর্ঘটনা প্রোটোকল
- ক্যাশ হ্যান্ডলিং
- সমবায়ের মূলনীতি
- অধিকার ও দায়িত্ব

---

## ১২.৩ সাপোর্ট

- ইন-অ্যাপ চ্যাট
- হটলাইন
- ইমেইল
- অফিস/হেল্পডেস্ক
- ড্রাইভার হাব
- জরুরি সেফটি টিম
- এসএলএ: সাধারণ অভিযোগ ২৪ ঘণ্টা, সেফটি ১ ঘণ্টা

---

# ১৩. নিরাপত্তা, ঝুঁকি ও কমপ্লায়েন্স

## ১৩.১ প্রযুক্তিগত নিরাপত্তা

- HTTPS বাধ্যতামূলক
- টোকেন-ভিত্তিক অথেনটিকেশন
- রোল-ভিত্তিক অ্যাক্সেস কন্ট্রোল
- রেট লিমিটিং
- ডিভাইস ট্রাস্ট স্কোর
- লগইন অ্যালার্ট
- সেনসিটিভ ডেটা এনক্রিপশন
- লোকেশন ডেটা অ্যাক্সেস লগ
- নিয়মিত পেনটেস্ট
- ব্যাকআপ
- ডিজাস্টার রিকভারি
- অডিট লগ

---

## ১৩.২ প্রতারণা প্রতিরোধ

প্রধান ঝুঁকি:

- ফেক রাইড
- জিপিএস স্পুফিং
- প্রোমো অপব্যবহার
- ড্রাইভার অ্যাকাউন্ট শেয়ার
- যাত্রীর মিথ্যা অভিযোগ
- ড্রাইভারের মিথ্যা আচরণ রিপোর্ট
- ক্যাশ আত্মসাৎ
- পেমেন্ট চার্জব্যাক

প্রতিরোধ:

- ডিভাইস বাইন্ডিং
- বায়োমেট্রিক/সেলফি ভেরিফিকেশন
- রাইড শুরুতে যাত্রী OTP
- ট্রিপ প্যাটার্ন ডিটেকশন
- অস্বাভাবিক আয় ফ্ল্যাগ
- রেটিং অ্যানোমালি
- ডকুমেন্ট ভেরিফিকেশন
- ক্যাশ রিকনসিলিয়েশন
- অভিযোগে উভয় পক্ষের শুনানি

---

# ১৪. অ্যানালিটিক্স ও KPI

## ১৪.১ ব্যবসায়িক মেট্রিক্স

- দৈনিক রাইড সংখ্যা
- মাসিক সক্রিয় যাত্রী
- মাসিক সক্রিয় ড্রাইভার
- গড় রাইড ফেয়ার
- প্ল্যাটফর্ম রেভিনিউ
- ড্রাইভার গড় আয়
- কমিশন হার
- কাস্টমার অ্যাকুইজিশন খরচ
- ড্রাইভার অ্যাকুইজিশন খরচ
- চার্জব্যাক/রিফান্ড হার

## ১৪.২ অপারেশনাল মেট্রিক্স

- রাইড অ্যাকসেপ্ট্যান্স রেট
- ক্যানসেলেশন রেট
- গড় এটিএ
- পিকআপ টাইম
- রাইড সম্পন্ন হার
- ম্যাচিং সফলতা
- ড্রাইভার অনলাইন ঘণ্টা
- ড্রাইভার প্রতি রাইড

## ১৪.৩ সমবায় মেট্রিক্স

- মোট সদস্য
- ড্রাইভার সদস্য সংখ্যা
- যাত্রী সদস্য সংখ্যা
- শেয়ার মূলধন
- প্যাট্রোনেজ বণ্টন
- ভোটদান হার
- সদস্য সন্তুষ্টি
- ড্রাইভার টার্ণওভার

## ১৪.৪ সেফটি মেট্রিক্স

- দুর্ঘটনা সংখ্যা
- জরুরি বাটন ব্যবহার
- হয়রানি অভিযোগ
- সমাধান সময়
- বীমা ক্লেম
- সেফটি রেটিং

---

# ১৫. রোডম্যাপ: ধাপে ধাপে বাস্তবায়ন

## ফেজ ০: প্রস্তুতি ও গবেষণা

সময়: ৪–৬ সপ্তাহ

কাজ:

- বাজার গবেষণা
- প্রতিযোগিতা বিশ্লেষণ
- ড্রাইভার/যাত্রী সাক্ষাৎকার
- লক্ষ্য শহর ও এলাকা নির্বাচন
- সমবায় কাঠামো নির্ধারণ
- বাইলজ খসড়া
- আইনি পরামর্শ
- আর্থিক মডেল
- ব্র্যান্ডিং
- প্রাথমিক বাজেট

আউটপুট:

- ফিজিবিলিটি রিপোর্ট
- সমবায় গঠনতন্ত্র
- প্রোডাক্ট রিকোয়ারমেন্ট ডকুমেন্ট
- প্রাথমিক আর্থিক প্রক্ষেপণ

---

## ফেজ ১: ন্যূনতম ভিয়েবল প্রোডাক্ট

সময়: ৩–৪ মাস

### MVP-তে যা থাকবে

- যাত্রী: সাইনআপ, রাইড বুকিং, লাইভ ট্র্যাকিং, রেটিং, ক্যাশ পেমেন্ট
- ড্রাইভার: অনলাইন/অফলাইন, রাইড অ্যাসাইন, ট্রিপ স্ট্যাটাস, আয় ড্যাশবোর্ড
- অ্যাডমিন: ইউজার, রাইড, ড্রাইভার ভেরিফিকেশন, ফেয়ার সেটিং, রিপোর্ট
- সমবায়: সদস্য আবেদন, শেয়ার রেকর্ড, সদস্য তালিকা
- নিরাপত্তা: লাইভ শেয়ার, SOS, ইনসিডেন্ট টিকেট

### যা বাদ রাখা যাবে

- জটিল প্যাট্রোনেজ অটোমেশন
- ই-ভোট
- মাল্টি-সিটি
- অ্যাডভান্সড ডাইনামিক প্রাইসিং
- শেয়ারড রাইড
- কর্পোরেট পেমেন্ট
- কার্ড পেমেন্ট

---

## ফেজ ২: বন্ধ বীটা পাইলট

সময়: ৬–৮ সপ্তাহ

- ২০–৫০ ড্রাইভার
- ৩০০–১,০০০ যাত্রী
- নির্দিষ্ট ২–৩ এলাকা
- দিনে নির্দিষ্ট সময়
- সেফটি ও অপারেশন টিম সক্রিয়
- প্রতিদিন বাগ ফিক্স
- ফিডব্যাক লুপ

---

## ফেজ ৩: পাবলিক লঞ্চ

সময়: ফেজ ২ সফল হলে

- এলাকা সম্প্রসারণ
- মার্কেটিং
- সদস্যপদ ক্যাম্পেইন
- ড্রাইভার রিক্রুটমেন্ট
- পেমেন্ট অপশন বাড়ানো
- সেফটি অডিট
- প্রেস/কমিউনিটি ইভেন্ট

---

## ফেজ ৪: স্কেল

- নতুন শহর
- নতুন ভেহিকেল টাইপ
- শেয়ারড রাইড
- সাবস্ক্রিপশন
- কর্পোরেট যাতায়াত
- ডেলিভারি/লজিস্টিক্স সংযুক্তি
- ই-ভোট ও পূর্ণ গভর্নেন্স
- ডেটা অ্যানালিটিক্স
- নিজস্ব ম্যাপ ডেটা উন্নয়ন

---

# ১৬. টিম

প্রাথমিক লিড টিম:

| ভূমিকা | দায়িত্ব |
|---|---|
| প্রোজেক্ট লিড/সিইও | কৌশল, সমন্বয়, ফান্ডিং |
| অপারেশন ম্যানেজার | ড্রাইভার, ফ্লিট, সেফটি |
| কমিউনিটি/কো-অপ ম্যানেজার | সদস্যপদ, গভর্নেন্স |
| আইনি উপদেষ্টা | সমবায়, ট্রান্সপোর্ট, ডেটা |
| প্রোডাক্ট ম্যানেজার | রোডম্যাপ, ইউএক্স |
| ইউআই/ইউএক্স ডিজাইনার | অ্যাপ ডিজাইন |
| ব্যাকএন্ড ডেভেলপার | সার্ভার, এপিআই |
| মোবাইল ডেভেলপার | রাইডার/ড্রাইভার অ্যাপ |
| ফ্রন্টএন্ড ডেভেলপার | অ্যাডমিন প্যানেল |
| কোয়ালিটি অ্যাসুরেন্স | টেস্টিং |
- পরে: ডেভঅপস, ডেটা অ্যানালিস্ট, সাপোর্ট এজেন্ট, সেফটি অফিসার

---

# ১৭. আনুমানিক খরচ

খরচ দেশ, টিম, স্কোপ অনুযায়ী অনেক বদলাবে। নিচেরটি একটি ধারণা।

## ১৭.১ প্রাথমিক খরচ

| খাত | আনুমানিক পরিসর |
|---|---:|
| সমবায় নিবন্ধন ও আইনি | ৫০,০০০–৩,০০,০০০ টাকা |
| ব্র্যান্ডিং ও ডিজাইন | ৫০,০০০–২,০০,০০০ টাকা |
| প্রোডাক্ট রিসার্চ | ৫০,০০০–১,৫০,০০০ টাকা |
| এমভিপি ডেভেলপমেন্ট | ২৫,০০,০০০–৮০,০০,০০০ টাকা |
| পাইলট অপারেশন | ৩,০০,০০০–১০,০০,০০০ টাকা |
| মার্কেটিং | ১,০০,০০০–৫,০০,০০০ টাকা |
| বীমা/কমপ্লায়েন্স | প্রকল্পভেদে |

## ১৭.২ মাসিক চলমান খরচ

- ক্লাউড/সার্ভার
- ম্যাপ এপিআই
- এসএমএস
- পেমেন্ট গেটওয়ে ফি
- সাপোর্ট টিম
- অফিস/হেল্পডেস্ক
- মার্কেটিং
- বীমা
- অডিট/অ্যাকাউন্টিং

খরচ কমাতে:

- ওপেন-সোর্স ম্যাপ/রাউটিং
- সীমিত এলাকায় পাইলট
- ম্যানুয়াল সদস্যপদ শুরুতে
- ক্যাশ-ফার্স্ট পেমেন্ট
- ফ্রিল্যান্স/হাইব্রিড টিম

---

# ১৮. ঝুঁকি ও প্রতিকার

| ঝুঁকি | প্রতিকার |
|---|---|
| যাত্রী পায় না, ড্রাইভার পায় না | এক এলাকায় ঘন পাইলট, ইনসেনটিভ, কমিউনিটি মার্কেটিং |
| ড্রাইভার দ্রুত চলে যায় | ন্যায্য কমিশন, স্বচ্ছ আয়, সদস্যপদ, বীমা |
| নিরাপত্তা ঘটনা | SOS, ট্রিপ শেয়ার, ভেরিফিকেশন, দ্রুত রেসপন্স টিম |
| আইনি বাধা | আগে আইনি পরামর্শ, স্থানীয় কর্তৃপক্ষের সঙ্গে যোগাযোগ |
| পেমেন্ট জটিলতা | শুরুতে ক্যাশ, পরে লাইসেন্সধারী পেমেন্ট অংশীদার |
| সমবায় রাজনীতি/দ্বন্দ্ব | স্বচ্ছ বাইলজ, নিয়মিত মিটিং, অডিট, আপিল প্রক্রিয়া |
| ম্যাপ/ঠিকানা সমস্যা | ল্যান্ডমার্ক, ম্যানুয়াল পিন, লোকাল ম্যাপ ডেটা সংশোধন |
| প্রযুক্তি ব্যর্থতা | মনিটরিং, ব্যাকআপ, ইনসিডেন্ট রেসপন্স, ফলব্যাক সার্ভিস |
| ফ্রড | ডিভাইস বাইন্ডিং, রাইড ভেরিফিকেশন, লেজার অডিট |

---

# ১৯. সমবায় গভর্নেন্স ডিজাইন

## ১৯.১ বোর্ড

প্রস্তাবিত:

- সভাপতি
- সহ-সভাপতি
- সাধারণ সম্পাদক
- কোষাধ্যক্ষ
- সদস্য
- ড্রাইভার প্রতিনিধি
- যাত্রী প্রতিনিধি
- নারী প্রতিনিধি
- নির্বাহী/প্রযুক্তি উপদেষ্টা, অ-ভোটাধিকার থাকতে পারে

## ১৯.২ নীতিগত সিদ্ধান্ত

সাধারণ পরিষদে ভোট হতে পারে:

- কমিশন হার পরিবর্তন
- ফেয়ার ফর্মুলা
- মুনাফা বণ্টন
- নতুন সদস্য ক্যাটাগরি
- বার্ষিক বাজেট
- নির্বাহী নিয়োগ
- সেফটি নীতি
- বীমা পরিকল্পনা

## ১৯.৩ স্বচ্ছতা

প্রকাশ করা উচিত:

- মাসিক রাইড সারাংশ
- কমিশন আয়
- ড্রাইভার গড় আয়
- প্ল্যাটফর্ম খরচ
- সেফটি ইনসিডেন্ট
- মুনাফা বণ্টন
- অডিট রিপোর্ট

---

# ২০. কারিগরি বাস্তবায়ন: প্রথম ১২ সপ্তাহের পরিকল্পনা

## সপ্তাহ ১–২: ফিজিবিলিটি ও দল

- সমবায় আইনজ্ঞের সঙ্গে বৈঠক
- বাইলজ খসড়া
- প্রতিষ্ঠাতা সদস্য সংগ্রহ
- লক্ষ্য এলাকা নির্বাচন
- প্রতিযোগিতা ও ফেয়ার গবেষণা

## সপ্তাহ ৩–৪: প্রোডাক্ট ডিজাইন

- ইউজার ফ্লো
- ওয়্যারফ্রেম
- ফেয়ার মডেল
- কমিশন মডেল
- সেফটি নীতি
- ডেটা মডেল খসড়া

## সপ্তাহ ৫–৮: কোর ডেভেলপমেন্ট

- অথেনটিকেশন
- লোকেশন সার্ভিস
- রাইড রিকোয়েস্ট
- ম্যাচিং
- ড্রাইভার অ্যাপ বেসিক
- অ্যাডমিন রাইড মনিটরিং

## সপ্তাহ ৯–১০: পেমেন্ট, সেফটি, অ্যাডমিন

- ক্যাশ লেজার
- ফেয়ার ক্যালকুলেশন
- SOS
- ট্রিপ শেয়ার
- রিপোর্টিং
- ডকুমেন্ট আপলোড

## সপ্তাহ ১১: টেস্টিং

- অভ্যন্তরীণ টেস্ট
- ফিল্ড টেস্ট
- দুর্বল নেটওয়ার্ক টেস্ট
- সিকিউরিটি বেসিক টেস্ট
- পেমেন্ট রিকনসিলিয়েশন টেস্ট

## সপ্তাহ ১২: পাইলট প্রস্তুতি

- ড্রাইভার ট্রেনিং
- হেল্পলাইন চালু
- সেফটি চেকলিস্ট
- মার্কেটিং ম্যাটেরিয়াল
- সীমিত লঞ্চ

---

# ২১. পাইলটের জন্য সুপারিশ

## এলাকা

একটি সীমিত কিন্তু ঘন এলাকা ভালো:

- বিশ্ববিদ্যালয় ক্যাম্পাস/আশেপাশে
- নির্দিষ্ট আবাসিক এলাকা
- অফিস পাড়া
- মেডিকেল/নার্সিং রুট
- নারী-নিরাপদ যাতায়াত প্রয়োজন এমন এলাকা

## যানবাহন

শুরুতে:

- মোটরসাইকেল/বাইক: দ্রুত ও কম খরচ
- অটোরিকশা: স্থানীয় গ্রহণযোগ্যতা
- কার: পরে
- নারী-চালিত বা নারী-বিশেষ রাইড: নিরাপত্তা ও বৈষম্য কমাতে

## সময়

- সকাল অফিস/স্কুল
- সন্ধ্যা ফেরত
- বৃষ্টি/রাতে আলাদা নিরাপত্তা প্রোটোকল

---

# ২২. আর্থিক মডেল উদাহরণ

ধরা যাক একটি রাইড:

- যাত্রী ফেয়ার: ৳১০০
- প্ল্যাটফর্ম ফি: ১০% = ৳১০
- পেমেন্ট ফি: ৳২
- ড্রাইভার পায়: ৳৮৮

মাসে ১০,০০০ রাইড হলে:

- মোট ফেয়ার ভলিউম: ৳১০,০০,০০০
- প্ল্যাটফর্ম ফি ১০%: ৳১,০০,০০০
- পেমেন্ট/এসএমএস/ম্যাপ খরচ ধরা যাক ৳২০,০০০
- অপারেশন/সাপোর্ট/টেক খরচ: ৳৬০,০০০
- নিট উদ্বৃত্ত: ৳২০,০০০

এই উদ্বৃত্ত রিজার্ভ, প্রযুক্তি, প্যাট্রোনেজ ও কল্যাণে বণ্টন হবে।

---

# ২৩. পণ্যের পার্থক্যকারী বৈশিষ্ট্য

আপনার কো-অপ অ্যাপকে আলাদা করতে পারে:

1. **স্বচ্ছ ফেয়ার ফর্মুলা**
2. **কম ও ন্যায্য কমিশন**
3. **ড্রাইভার মালিকানা**
4. **যাত্রী সদস্য রিবেট**
5. **নিরাপত্তা প্রথম**
6. **স্থানীয় ভাষা ও ঠিকানা সহায়তা**
7. **নারী-নিরাপদ রাইড**
8. **পরিবেশবান্ধব রাইড**
9. **অডিটযোগ্য আর্থিক রিপোর্ট**
10. **গণতান্ত্রিক সিদ্ধান্ত**

---

# ২৪. প্রযুক্তিগত ঝুঁকি কমানোর টিপস

- শুরু থেকে সবকিছু পারফেক্ট বানানোর চেষ্টা করবেন না
- প্রথমে ম্যানুয়াল অপারেশন চলতে পারে
- ড্রাইভার অ্যাপে লো-ডাটা মোড রাখুন
- ম্যাপ এপিআই ফলব্যাক রাখুন
- লোকেশন আপডেট ব্যাচ করুন, প্রতি সেকেন্ডে নয়, প্রয়োজন অনুযায়ী
- ব্যাটারি অপ্টিমাইজেশন করুন
- অফলাইন ইভেন্ট কুই রাখুন
- রাইড ডেটা এনক্রিপ্ট করুন
- অডিট লগ রাখুন
- ডেটাবেসে জিও-ইনডেক্স ব্যবহার করুন

---

# ২৫. বাস্তবায়নের জন্য ডকুমেন্ট তালিকা

আপনার এগুলো তৈরি করতে হবে:

1. ফিজিবিলিটি রিপোর্ট
2. সমবায় বাইলজ
3. সদস্যপদ চুক্তি
4. ড্রাইভার চুক্তি/অংশগ্রহণ শর্ত
5. যাত্রী শর্তাবলি
6. প্রাইভেসি পলিসি
7. সেফটি পলিসি
8. ডেটা রিটেনশন পলিসি
9. অভিযোগ প্রতিকার নীতি
10. মুনাফা বণ্টন নীতি
11. ফেয়ার ফর্মুলা
12. কমিশন নীতি
13. ইনসিডেন্ট রেসপন্স প্লে
14. অ্যাডমিন রোল পারমিশন ম্যাট্রিক্স
15. পিআরডি
16. টেকনিক্যাল ডিজাইন ডকুমেন্ট
17. টেস্ট প্ল্যান
18. পাইলট পরিকল্পনা
19. মার্কেটিং প্ল্যান
20. আর্থিক প্রক্ষেপণ

---

# ২৬. প্রাথমিক এপিআই নকশা উদাহরণ

নিচে কিছু উদাহরণ এপিআই দেওয়া হলো:

```text
POST /api/v1/auth/otp/request
POST /api/v1/auth/otp/verify

GET  /api/v1/profile
PUT  /api/v1/profile

POST /api/v1/rides/estimate
POST /api/v1/rides
GET  /api/v1/rides/{id}
POST /api/v1/rides/{id}/cancel

POST /api/v1/driver/online
POST /api/v1/driver/offline
POST /api/v1/driver/location
POST /api/v1/driver/rides/{id}/accept
POST /api/v1/driver/rides/{id}/decline
POST /api/v1/driver/rides/{id}/arrived
POST /api/v1/driver/rides/{id}/start
POST /api/v1/driver/rides/{id}/complete

POST /api/v1/payments/initiate
POST /api/v1/payments/webhook

GET  /api/v1/member/profile
POST /api/v1/member/join
POST /api/v1/member/shares/purchase
GET  /api/v1/member/patronage

POST /api/v1/safety/sos
POST /api/v1/incidents
GET  /api/v1/admin/rides
GET  /api/v1/admin/drivers/pending
```

---

# ২৭. সেফটি ফিচারের বিশদ

## যাত্রীর জন্য

- রাইড শেয়ার লিংক
- ট্রিপ কোড
- SOS
- জরুরি যোগাযোগ
- বিশ্বস্ত ব্যক্তি
- ড্রাইভার প্রোফাইল ভেরিফিকেশন ব্যাজ
- রিপোর্ট
- ব্লক

## ড্রাইভারের জন্য

- জরুরি বাটন
- মিথ্যা অভিযোগে আপিল
- নিরাপদ পিকআপ পয়েন্ট
- ক্যাশ সংগ্রহ সতর্কতা
- সহায়তা হটলাইন
- দুর্ঘটনা বীমা

## প্ল্যাটফর্মের জন্য

- ইনসিডেন্ট ড্যাশবোর্ড
- অ্যালার্ট রুল
- অডিট লগ
- ফরেনসিক ডেটা সংরক্ষণ
- নিরপেক্ষ তদন্ত

---

# ২৮. কো-অপ প্রযুক্তিতে বিশেষ মডিউল: প্যাট্রোনেজ ইঞ্জিন

এটি আপনার অ্যাপকে প্রকৃত কো-অপারেটিভ করবে।

### ইনপুট

- ড্রাইভার সম্পন্ন রাইড
- ড্রাইভার আয়
- যাত্রীর মোট ফেয়ার
- সদস্যপদ সময়কাল
- সদস্য শেয়ার
- নীতিমালা

### আউটপুট

- প্যাট্রোনেজ পয়েন্ট
- বণ্টনযোগ্য অর্থ
- সদস্যভিত্তিক বণ্টন
- অনুমোদন ওয়ার্কফ্লো
- ট্যাক্স রিপোর্ট
- অডিট ট্রেইল

উদাহরণ সূত্র:

```
driver_patronage_share =
  total_driver_pool ×
  (driver_completed_rides / all_driver_completed_rides)

rider_patronage_share =
  total_rider_pool ×
  (rider_paid_fare / all_rider_paid_fare)
```

তবে আইন ও বাইলজ অনুযায়ী ক্যাপ বা ন্যূনতম সীমা রাখা যেতে পারে।

---

# ২৯. লঞ্চের আগে চূড়ান্ত চেকলিস্ট

## আইনি

- সমবায় নিবন্ধন সম্পন্ন/প্রক্রিয়াধীন
- বাইলজ অনুমোদিত
- ব্যাংক অ্যাকাউন্ট
- ট্যাক্স/TIN প্রস্তুত
- পরিবহন/স্থানীয় অনুমতি পর্যালোচিত
- ডেটা প্রাইভেসি নীতি প্রকাশিত

## প্রোডাক্ট

- যাত্রী অ্যাপ টেস্ট সম্পন্ন
- ড্রাইভার অ্যাপ টেস্ট সম্পন্ন
- অ্যাডমিন প্যানেল প্রস্তুত
- রাইড লাইফসাইকেল টেস্ট সম্পন্ন
- ক্যানসেলেশন/রিফান্ড ফ্লো টেস্ট সম্পন্ন

## অপারেশন

- ড্রাইভার ভেরিফিকেশন প্রক্রিয়া
- ট্রেনিং সম্পন্ন
- হেল্পলাইন চালু
- সেফটি টিম
- ইনসিডেন্ট প্লে
- বীমা প্রস্তুত, সম্ভব হলে

## আর্থিক

- প্ল্যাটফর্ম ফি সেট
- ড্রাইভার পেআউট প্রক্রিয়া
- ক্যাশ রিকনসিলিয়েশন
- লেজার অডিট
- পাইলট বাজেট অনুমোদিত

---

# ৩০. সবচেয়ে গুরুত্বপূর্ণ পরামর্শ

1. **প্রযুক্তির আগে সমবায়ের আইনি ও গভর্নেন্স কাঠামো ঠিক করুন।**
2. **একটি ছোট এলাকায় পাইলট করুন।**
3. **ড্রাইভারদের মালিকানা ও স্বচ্ছ আয় দিন।**
4. **যাত্রীদের নিরাপত্তা সর্বোচ্চ অগ্রাধিকার দিন।**
5. **ক্যাশ-ফার্স্ট শুরু করুন; জটিল পেমেন্ট পরে যোগ করুন।**
6. **সব আর্থিক লেনদেন লেজারভিত্তিক রাখুন।**
7. **সদস্যদের কাছে নিয়মিত রিপোর্ট প্রকাশ করুন।**
8. **ম্যাপ/ঠিকানা সমস্যা স্থানীয়ভাবে সমাধান করুন।**
9. **অ্যাডমিন প্যানেলে সেফটি ও ডিসপিউট ম্যানেজমেন্ট শক্ত রাখুন।**
10. **ধাপে ধাপে স্কেল করুন; একবারে বড় শহরজুড়ে যাবেন না।**

---

# ৩১. সম্ভাব্য সর্বনিম্ন কার্যকর সংস্করণ

যদি খুব দ্রুত শুরু করতে চান, তাহলে এমভিপি হতে পারে:

- ফোন + OTP
- রাইডার রাইড বুকিং
- ড্রাইভার অ্যাপে রাইড গ্রহণ
- লাইভ লোকেশন
- ক্যাশ পেমেন্ট
- রেটিং
- অ্যাডমিন ড্যাশবোর্ড
- ড্রাইভার ডকুমেন্ট আপলোড
- সদস্য আবেদন ফর্ম
- SOS বাটন
- ট্রিপ শেয়ার

পরে যোগ করবেন:

- পূর্ণ সদস্য লেজার
- প্যাট্রোনেজ অটোমেশন
- ই-ভোট
- ওয়ালেট
- মোবাইল মানি/কার্ড
- শেয়ারড রাইড
- মাল্টি-সিটি

---

# ৩২. আপনার জন্য পরবর্তী ধাপ

আপনি চাইলে এখান থেকে এগোতে পারেন এভাবে:

1. একটি ১০–২০ জন ড্রাইভার ও ১০০ জন যাত্রীর সমীক্ষা করুন
2. লক্ষ্য এলাকা নির্বাচন করুন
3. সমবায় রেজিস্ট্রেশন প্রক্রিয়া শুরু করুন
4. ফেয়ার ও কমিশন মডেল খসড়া করুন
5. অ্যাপের ওয়্যারফ্রেম বানান
6. ডেভেলপমেন্ট টিম বা ভেন্ডর নির্বাচন করুন
7. ৯০ দিনের পাইলট পরিকল্পনা চূড়ান্ত করুন

---

চাইলে আমি পরের ধাপে আপনার জন্য আলাদাভাবে তৈরি করে দিতে পারি:

1. **বাংলাদেশ-নির্দিষ্ট সমবায় নিবন্ধন চেকলিস্ট**  
2. **সম্পূর্ণ প্রোডাক্ট রিকোয়ারমেন্ট ডকুমেন্ট**  
3. **ডেটাবেস স্কিমা ও এপিআই স্পেসিফিকেশন**  
4. **পাইলট লঞ্চ প্ল্যান ও বাজেট শিট**  
5. **ড্রাইভার/যাত্রী অ্যাপের ওয়্যারফ্রেম তালিকা**

অবশ্যই। নিচে একটি **কো-অপারেটিভ রাইডশেয়ারিং অ্যাপ** বানানোর বিস্তারিত ব্লুপ্রিন্ট দিলাম। এটি এমনভাবে সাজানো, যেন আপনি শুধু একটি “রাইড অ্যাপ” নয়, বরং **ড্রাইভার, যাত্রী ও কর্মী/উদ্যোক্তাদের মালিকানাধীন একটি ডিজিটাল সমবায় প্ল্যাটফর্ম** তৈরি করতে পারেন। ধরে নিচ্ছি প্রাথমিক লক্ষ্য: **বাংলাদেশ/দক্ষিণ এশিয়া প্রেক্ষাপট, শহরভিত্তিক পাইলট, মোবাইল অ্যাপ + ওয়েব অ্যাডমিন + সমবায় গভর্নেন্স**।

---

# ১. মূল ধারণা: কো-অপারেটিভ রাইডশেয়ারিং কী?

সাধারণ রাইডশেয়ারিং অ্যাপে সাধারণত একটি কোম্পানি মালিক হয়, ড্রাইভার ও যাত্রী শুধু ব্যবহারকারী হয়। কিন্তু **কো-অপারেটিভ রাইড অ্যাপে**:

- ড্রাইভাররা শুধু “কন্ট্রাক্টর” নয়, তারা **মালিক-সদস্য** হতে পারেন।
- যাত্রীরা চাইলে **সদস্য** হয়ে অংশীদারিত্ব, ভোট, লভ্যাংশ বা গভর্নেন্সে অংশ নিতে পারেন।
- মুনাফা শুধু মালিকের কাছে যায় না; একটি অংশ **রিজার্ভ**, একটি অংশ **সেবা/প্রযুক্তি উন্নয়ন**, একটি অংশ **সদস্যদের ব্যবহার অনুপাতে প্যাট্রোনেজ**, এবং আইনসম্মতভাবে শেয়ার লভ্যাংশে বণ্টন হয়।
- কমিশন, ফেয়ার ফর্মুলা, সেফটি নীতি, ড্রাইভার কল্যাণ—এগুলো আরও স্বচ্ছ ও গণতান্ত্রিকভাবে নির্ধারণ করা যায়।

**মূল স্লোগান হতে পারে:**  
“যারা চালায়, যারা চড়ে, তারাই মালিক।”

---

# ২. প্রথম করণীয়: সমবায় কাঠামো ঠিক করা

প্রযুক্তির আগে সমবায়ের আইনি ও গভর্নেন্স কাঠামো ঠিক করা জরুরি।

## ২.১ সদস্য ক্যাটাগরি

আপনি এক বা একাধিক ক্যাটাগরি রাখতে পারেন:

| সদস্য ক্যাটাগরি | ভূমিকা | সম্ভাব্য অধিকার |
|---|---|---|
| ড্রাইভার সদস্য | রাইড প্রদানকারী | ভোট, লভ্যাংশ, কমিশন নীতিতে মতামত |
| যাত্রী সদস্য | সেবা গ্রহণকারী | ভোট, গভর্নেন্স, লভ্যাংশ/রিবেট |
| কর্মী/উদ্যোক্তা সদস্য | অ্যাপ পরিচালনা, সাপোর্ট, টেক টিম | ভোট, কর্মী-মালিকানা |
| বিনিয়োগকারী সদস্য | মূলধন দেয় | সীমিত লভ্যাংশ, সীমিত ভোট |

সবচেয়ে গুরুত্বপূর্ণ নীতি: **এক সদস্য, এক ভোট**। বড় বিনিয়োগকারী থাকলেও ভোট যেন গণতান্ত্রিক থাকে।

---

## ২.২ মূলধন ও শেয়ার

উদাহরণ কাঠামো:

- প্রতি সদস্যের জন্য ন্যূনতম শেয়ার: ৫০০–৫,০০০ টাকা
- ড্রাইভার সদস্য হলে গাড়ি/মোটরসাইকেল ভেরিফিকেশন বাধ্যতামূলক
- শেয়ার কিনলে সদস্যপদ সক্রিয় হবে
- সদস্যপদ বাতিল/স্থগিতের নিয়ম বাইলজে থাকতে হবে
- সদস্য ত্যাগ করলে শেয়ার ফেরতের নিয়ম ও লিকুইডিটি ফান্ড থাকতে হবে

---

## ২.৩ মুনাফা বণ্টন মডেল

সমবায় আইন ও বাইলজ অনুযায়ী পরিবর্তন করতে হবে। একটি সম্ভাব্য নীতিমালা:

| খাত | শতাংশ | উদ্দেশ্য |
|---|---:|---|
| আইনসম্মত রিজার্ভ ফান্ড | ২০–৩০% | ঝুঁকি ও স্থিতিশীলতা |
| প্রযুক্তি ও উন্নয়ন ফান্ড | ১০–২০% | অ্যাপ আপগ্রেড, সিকিউরিটি |
| ড্রাইভার প্যাট্রোনেজ | ২৫–৩৫% | ড্রাইভারদের রাইড সংখ্যা/আয় অনুপাতে |
| যাত্রী প্যাট্রোনেজ | ১০–২০% | যাত্রীদের ব্যবহার অনুপাতে |
| কমিউনিটি/কল্যাণ ফান্ড | ৫–১০% | দুর্ঘটনা, শিক্ষা, সামাজিক কাজ |
| শেয়ার লভ্যাংশ | ০–১০% | সীমিত হারে, আইন অনুযায়ী |

**গুরুত্বপূর্ণ:** মুনাফা বণ্টনের আগে স্থানীয় সমবায় আইন, ট্যাক্স ও রেজিস্ট্রার অফিসের নিয়ম জেনে নিন।

---

# ৩. আইনি ও রেগুলেটরি চেকলিস্ট

এটি আইনি পরামর্শ নয়। বাংলাদেশ বা আপনার দেশের আইন অনুযায়ী আইনজীবী ও সমবায় বিভাগের সঙ্গে কথা বলুন।

## ৩.১ সমবায় নিবন্ধন

সম্ভাব্য ধাপ:

1. প্রতিষ্ঠাতা সদস্য দল গঠন
2. সমবায়ের নাম নির্বাচন
3. বাইলজ/গঠনতন্ত্র তৈরি
4. সদস্য শেয়ার মূলধন সংগ্রহ
5. সমবায় বিভাগে নিবন্ধন আবেদন
6. নিবন্ধন সনদ প্রাপ্তি
7. ব্যাংক অ্যাকাউন্ট খোলা
8. টিন/ভ্যাট/ট্যাক্স নিবন্ধন, প্রযোজ্য হলে
9. বার্ষিক রিটার্ন ও হিসাব নিরীক্ষা প্রক্রিয়া

---

## ৩.২ রাইডশেয়ারিং/ট্রান্সপোর্ট অনুমতি

প্রযোজ্য হলে:

- সড়ক পরিবহন কর্তৃপক্ষ/সিটি কর্পোরেশন/স্থানীয় কর্তৃপক্ষের অনুমতি
- বাণিজ্যিক লাইসেন্স
- ড্রাইভার লাইসেন্স ও ভেরিফিকেশন
- গাড়ির ফিটনেস/রেজিস্ট্রেশন
- বীমা
- যাত্রী নিরাপত্তা নীতি
- ডেটা সুরক্ষা নীতি
- পেমেন্ট/ওয়ালেট নিয়ম, বিশেষ করে টাকা জমা রাখলে আর্থিক প্রতিষ্ঠান/পেমেন্ট লাইসেন্স সংক্রান্ত বিষয়

---

## ৩.৩ ডেটা ও প্রাইভেসি

যেহেতু লোকেশন, ফোন নম্বর, যাত্রা তথ্য সংবেদনশীল:

- প্রাইভেসি পলিসি
- ডেটা রিটেনশন নীতি
- ডেটা মুছে ফেলার নিয়ম
- সদস্য/ব্যবহারকারীর সম্মতি
- ফোন নম্বর মাস্কিং
- লোকেশন ট্র্যাকিং শুধু ট্রিপ চলাকালীন সীমিত রাখা
- আইনি চাহিদা ছাড়া ডেটা প্রকাশ না করা

---

# ৪. পণ্যের রূপরেখা: অ্যাপে কী কী থাকবে?

আপনার কমপক্ষে চারটি অংশ থাকবে:

1. **যাত্রী অ্যাপ**
2. **ড্রাইভার অ্যাপ**
3. **অ্যাডমিন/অপারেশন ওয়েব প্যানেল**
4. **সমবায় সদস্য পোর্টাল**

---

## ৪.১ যাত্রী অ্যাপ

### মৌলিক ফিচার

- ফোন নম্বর/ইমেইল দিয়ে সাইনআপ
- OTP লগইন
- প্রোফাইল, প্রোফাইল ছবি
- গন্তব্য সার্চ, ম্যাপে পিন করা
- রাইড ফেয়ার আনুমানিক দেখা
- রাইড টাইপ নির্বাচন: বাইক, অটো, কার, প্রিমিয়াম, নারী-চালিত, শেয়ারড ইত্যাদি
- রাইড রিকোয়েস্ট
- ড্রাইভার অ্যাসাইনমেন্ট
- লাইভ ড্রাইভার ট্র্যাকিং
- রাইড শেয়ারিং: পরিবার/বন্ধুকে লাইভ লিংক পাঠানো
- কল/মেসেজ, ফোন নম্বর মাস্কিং
- রাইড শুরু/শেষ নিশ্চিতকরণ
- রেটিং ও রিভিউ
- পেমেন্ট: ক্যাশ, ওয়ালেট, মোবাইল মানি, কার্ড
- ট্রিপ হিস্ট্রি
- রসিদ/ইনভয়েস
- প্রিয় ঠিকানা
- প্রোমো/সদস্য রিবেট
- নিরাপত্তা বাটন: SOS, পুলিশ/সাপোর্ট/জরুরি যোগাযোগ
- অভিযোগ/রিপোর্ট

### সদস্য হলে অতিরিক্ত

- সদস্যপদ আবেদন
- শেয়ার কেনা
- প্যাট্রোনেজ/লভ্যাংশ দেখা
- গভর্নেন্স পোর্টাল
- ভোট/মিটিং নোটিশ
- সমবায়ের আর্থিক সারাংশ

---

## ৪.২ ড্রাইভার অ্যাপ

### মৌলিক ফিচার

- ড্রাইভার নিবন্ধন
- লাইসেন্স, গাড়ির কাগজ, বীমা আপলোড
- ভেরিফিকেশন স্ট্যাটাস
- অনলাইন/অফলাইন টগল
- রাইড অফার গ্রহণ/বাতিল
- যাত্রীর পিকআপ লোকেশন
- নেভিগেশন
- ট্রিপ স্ট্যাটাস: পিকআপে যাচ্ছি, পৌঁছেছি, ট্রিপ শুরু, ট্রিপ শেষ
- আয়ের ড্যাশবোর্ড
- দৈনিক/সাপ্তাহিক পেমেন্ট হিস্ট্রি
- কমিশন/প্ল্যাটফর্ম ফি স্বচ্ছভাবে দেখা
- ক্যাশ কালেকশন রিপোর্ট
- ওয়ালেট
- রেটিং
- ইনসেনটিভ/বোনাস
- দুর্ঘটনা/জরুরি বাটন
- যাত্রী নিরাপত্তা চেকলিস্ট
- ট্রেনিং মডিউল
- সমবায় সদস্যপদ
- শেয়ার/লভ্যাংশ ড্যাশবোর্ড

### গুরুত্বপূর্ণ ড্রাইভার-বান্ধব নীতি

- কমিশন ফর্মুলা স্বচ্ছ হতে হবে
- অ্যালগরিদম কেন কোনো রাইড দিল/দিল না, তা ড্রাইভার বুঝতে পারবে
- মিথ্যা রিপোর্টের বিরুদ্ধে আপিল
- দুর্ঘটনা বীমা
- অতিরিক্ত কাজ প্রতিরোধে ফ্যাটিগ সতর্কতা
- ক্যাশ সংগ্রহের নিরাপত্তা ও হিসাব সহজ করা

---

## ৪.৩ অ্যাডমিন/অপারেশন প্যানেল

### মূল মডিউল

- ড্যাশবোর্ড: লাইভ রাইড, রেভিনিউ, সক্রিয় ড্রাইভার, যাত্রী
- ইউজার ম্যানেজমেন্ট
- ড্রাইভার ভেরিফিকেশন
- গাড়ি/ডকুমেন্ট অডিট
- রাইড মনিটরিং
- ম্যানুয়াল রাইড অ্যাসাইনমেন্ট
- প্রাইসিং কনফিগারেশন
- জোন/সার্জ নিয়ন্ত্রণ
- প্রোমো/কুপন
- পেমেন্ট/রিফান্ড
- কমিশন সেটিং
- ড্রাইভার পেআউট
- অভিযোগ/ডিসপিউট
- সেফটি ইনসিডেন্ট ম্যানেজমেন্ট
- মেম্বারশিপ ম্যানেজমেন্ট
- শেয়ার লেজার
- প্যাট্রোনেজ ক্যালকুলেশন
- ভোট/মিটিং ম্যানেজমেন্ট
- নোটিশ বোর্ড
- রিপোর্ট ও অডিট
- রোল ও পারমিশন

---

## ৪.৪ সমবায় সদস্য পোর্টাল

এটি কো-অপারেটিভ অ্যাপের অন্যতম পার্থক্য।

### ফিচার

- সদস্য প্রোফাইল
- শেয়ার সার্টিফিকেট
- শেয়ার কেনার হিস্ট্রি
- সদস্যপদ ফি
- ক্যাপিটাল অ্যাকাউন্ট
- প্যাট্রোনেজ পয়েন্ট
- বার্ষিক মুনাফা বণ্টন
- ডিভিডেন্ড হিস্ট্রি
- মিটিং নোটিশ
- ই-ভোট বা মতামত
- সমবায়ের আর্থিক রিপোর্ট
- বাইলজ/নীতিমালা ডকুমেন্ট
- অভিযোগ/আপিল
- সদস্যপদ স্থগিত/সক্রিয় স্ট্যাটাস

---

# ৫. রাইড লাইফসাইকেল

একটি রাইডের স্টেট মেসিন এরকম হতে পারে:

| স্টেট | বর্ণনা |
|---|---|
| REQUESTED | যাত্রী রাইড চেয়েছে |
| QUOTED | ফেয়ার আনুমানিক দেওয়া হয়েছে |
| SEARCHING | ড্রাইভার খোঁজা হচ্ছে |
| OFFERED | নির্দিষ্ট ড্রাইভারকে অফার পাঠানো হয়েছে |
| ACCEPTED | ড্রাইভার গ্রহণ করেছে |
| ARRIVED | ড্রাইভার পিকআপে পৌঁছেছে |
| PICKUP_CONFIRMED | যাত্রী উঠেছে |
| ONGOING | ট্রিপ চলছে |
| NEAR_DROP | গন্তব্যের কাছে |
| COMPLETED | ট্রিপ শেষ |
| PAYMENT_PENDING | পেমেন্ট বাকি |
| PAID | পেমেন্ট সম্পন্ন |
| RATED | রেটিং সম্পন্ন |
| CANCELLED | বাতিল |
| DISPUTED | বিরোধ/অভিযোগ |
| INCIDENT | দুর্ঘটনা/নিরাপত্তা ঘটনা |

---

# ৬. টেকনিক্যাল আর্কিটেকচার

প্রাথমিক পর্যায়ে **মডুলার মনোলিথ** দিয়ে শুরু করা ভালো। পরে প্রয়োজনে মাইক্রোসার্ভিসে যাওয়া যায়।

## ৬.১ উচ্চ-স্তরের আর্কিটেকচার

```
যাত্রী অ্যাপ  +  ড্রাইভার অ্যাপ  +  ওয়েব অ্যাডমিন
                ↓
            API Gateway / REST + WebSocket
                ↓
        ব্যাকএন্ড অ্যাপ্লিকেশন
        ├─ Auth Service
        ├─ Member/Co-op Service
        ├─ Ride Service
        ├─ Matching/Dispatch Service
        ├─ Pricing Service
        ├─ Payment/Ledger Service
        ├─ Notification Service
        ├─ Safety Service
        ├─ Admin/Reporting Service
        └─ Document Verification Service
                ↓
┌──────────────┬──────────────┬──────────────┐
│ PostgreSQL + │ Redis        │ Message Queue │
│ PostGIS      │ Cache/Geo    │ RabbitMQ/Kafka│
└──────────────┴──────────────┴──────────────┘
                ↓
    Maps/Routing, SMS/Push, Payment Gateway,
    Object Storage, Monitoring, Logs
```

---

## ৬.২ সম্ভাব্য টেক স্ট্যাক

### মোবাইল অ্যাপ

- **Flutter**: একটি কোডবেজে Android + iOS, দ্রুত ডেভেলপমেন্ট
- অথবা **React Native**
- ড্রাইভার অ্যাপে ব্যাকগ্রাউন্ড লোকেশন, লো-ডাটা মোড, অফলাইন কুই জরুরি

### ব্যাকএন্ড

- **Node.js + NestJS/Express** অথবা
- **Python + Django REST Framework / FastAPI** অথবা
- **Go + PostgreSQL**

### ডেটাবেস

- **PostgreSQL + PostGIS**: লোকেশন, জোন, জিওফেন্সিং
- **Redis**: ক্যাশ, রেট লিমিটিং, রিয়েলটাইম লোকেশন, সেশন
- প্রয়োজনে **TimescaleDB** বা অ্যানালিটিক্স ডেটাবেস

### রিয়েলটাইম

- WebSocket / Socket.IO
- MQTTও ব্যবহার করা যায়, বিশেষ করে ড্রাইভার লোকেশন স্ট্রিমের জন্য

### ম্যাপ ও রাউটিং

শুরুতে দুটি পথ:

1. **সহজ ও নির্ভরযোগ্য কিন্তু খরচ বেশি:**
   - Google Maps Platform
   - Distance Matrix, Directions, Places, Geocoding

2. **খরচ কমানোর জন্য ওপেন-সোর্স/সেমি-ওপেন:**
   - MapLibre GL
   - OpenStreetMap
   - OSRM / Valhalla / GraphHopper
   - Nominatim বা বাণিজ্যিক জিওকোডিং
   - বাংলাদেশে ঠিকানা ডেটা উন্নত করতে নিজস্ব ঠিকানা ডেটা/ল্যান্ডমার্ক যোগ করা লাগবে

### নোটিফিকেশন

- Firebase Cloud Messaging
- Apple Push Notification Service
- SMS Gateway: স্থানীয় প্রোভাইডার বা ইনফোবিপ/টুইলিও
- ইমেইল: SES/Resend/Postmark

### পেমেন্ট

- ক্যাশ
- মোবাইল মানি: বিকাশ/নগদ/রকেট, যদি প্রযোজ্য হয়
- কার্ড: পেমেন্ট গেটওয়ে ব্যবহার করুন; নিজে কার্ড ডেটা রাখবেন না
- ওয়ালেট: লেজার-ভিত্তিক, কিন্তু আর্থিক নিয়ম মেনে
- পিসিআই-ডিএসএস কমপ্লায়েন্স

### অবকাঠামো

- শুরুতে: একটি ক্লাউড ভিপিএস বা ম্যানেজড ভিএম
- পরে: Docker + Kubernetes
- CI/CD: GitHub Actions/GitLab CI
- Monitoring: Prometheus + Grafana
- Logs: Loki/ELK
- Error tracking: Sentry

---

# ৭. মূল সার্ভিস/মডিউল ভেঙে দেখা

## ৭.১ অথেনটিকেশন ও প্রোফাইল

- ফোন + OTP
- ইমেইল + পাসওয়ার্ড ঐচ্ছিক
- রোল: Rider, Driver, Admin, Support, Auditor, Board, Member
- KYC স্ট্যাটাস
- ডিভাইস বাইন্ডিং, বিশেষ করে ড্রাইভার অ্যাপে
- ফোন পরিবর্তন করলে সেকেন্ডারি ভেরিফিকেশন

---

## ৭.২ মেম্বারশিপ/কো-অপারেটিভ লেজার

সবচেয়ে গুরুত্বপূর্ণ পার্থক্য। প্রতিটি আর্থিক ঘটনার ডাবল-এন্ট্রি ধাঁচের লেজার রাখা ভালো।

### প্রয়োজনীয় সত্তা

- Member
- Share
- ShareTransaction
- CapitalAccount
- PatronageAccount
- Dividend
- GeneralMeeting
- Motion
- Vote
- BylawDocument
- MemberWallet
- MemberContribution
- RefundRequest

---

## ৭.৩ ট্রিপ সার্ভিস

- রাইড রিকোয়েস্ট
- ফেয়ার এস্টিমেট
- ড্রাইভার সার্চ রেডিয়াস
- অফার টাইমআউট
- ট্রিপ স্ট্যাটাস ইভেন্ট
- রুট পয়েন্ট
- ট্রিপ টাইমলাইন
- ক্যানসেলেশন কারণ
- ডিসপিউট
- অডিট লগ

---

## ৭.৪ ম্যাচিং/ডিসপ্যাচ

প্রাথমিক মডেল:

1. রাইড রিকোয়েস্ট আসবে
2. কাছের সক্রিয় ড্রাইভারদের তালিকা তৈরি হবে
3. স্কোরিং করে সেরা ড্রাইভার বেছে নেওয়া হবে
4. ড্রাইভারকে অফার পাঠানো হবে
5. নির্দিষ্ট সেকেন্ডে গ্রহণ না করলে পরের ড্রাইভার
6. ৩–৫ বার চেষ্টা করে না পেলে যাত্রীকে জানানো হবে

### স্কোরিং উদাহরণ

```
score =
  w1 × distance_to_pickup
+ w2 × eta
+ w3 × driver_rating
+ w4 × acceptance_rate
+ w5 × idle_time_bonus
+ w6 × co_op_priority
− penalty × recent_cancellations
```

তবে কো-অপারেটিভ হিসেবে শুধু “কাছের ড্রাইভার” নয়, **ন্যায্য বণ্টন**ও বিবেচনা করুন।

---

## ৭.৫ প্রাইসিং

স্বচ্ছ ফেয়ার ফর্মুলা থাকা ভালো।

উদাহরণ:

```
fare =
  base_fare
+ per_km × distance_km
+ per_minute × trip_duration_min
+ demand_adjustment
+ tolls/parking_extra
```

সীমা:

- minimum_fare
- maximum_cap
- cancellation_fee, যদি প্রযোজ্য হয়
- night_fee
- peak_time_cap

প্ল্যাটফর্ম ফি:

```
platform_fee = fare × platform_percentage
driver_net = fare − platform_fee − payment_gateway_fee
```

কো-অপারেটিভ হিসেবে ফর্মুলা সদস্যদের কাছে প্রকাশ করাই ভালো।

---

## ৭.৬ পেমেন্ট ও লেজার

প্রতিটি লেনদেনের জন্য আলাদা লেজার এন্ট্রি রাখুন।

উদাহরণ:

| ঘটনা | ডেবিট | ক্রেডিট |
|---|---|---|
| যাত্রী ক্যাশে পেমেন্ট করল | ড্রাইভার ক্যাশ কালেকশন | রাইড রেভিনিউ |
| ড্রাইভার প্ল্যাটফর্ম ফি পরিশোধ করল | প্ল্যাটফর্ম ফি অ্যাকাউন্ট | ড্রাইভার ওয়ালেট/ক্যাশ অ্যাডজাস্টমেন্ট |
| যাত্রী কার্ডে পেমেন্ট করল | পেমেন্ট গেটওয়ে রিসিভেবল | রাইড রেভিনিউ |
| ড্রাইভার পেআউট | ড্রাইভার পেয়েবল | ব্যাংক/মোবাইল মানি |
| সদস্য শেয়ার কিনল | ব্যাংক | শেয়ার ক্যাপিটাল |

### পেমেন্ট পদ্ধতি

- ক্যাশ: বাংলাদেশে শুরুতে সবচেয়ে বাস্তবসম্মত
- মোবাইল মানি: বিকাশ/নগদ/রকেট
- কার্ড: পরে
- কর্পোরেট অ্যাকাউন্ট: পরে
- ওয়ালেট: সতর্কভাবে, আইনি পরামর্শ নিয়ে

---

## ৭.৭ নিরাপত্তা ও সেফটি সার্ভিস

### ফিচার

- লাইভ ট্রিপ শেয়ার
- জরুরি বাটন
- জরুরি যোগাযোগ তালিকা
- ট্রিপ অ্যানোমালি ডিটেকশন: অস্বাভাবিক রুট, দীর্ঘ সময় স্থির থাকা
- ড্রাইভার ফ্যাটিগ সতর্কতা
- গতি সীমা লঙ্ঘন সতর্কতা
- দুর্ঘটনা রিপোর্ট
- ইনসিডেন্ট টিকেট
- অডিও রেকর্ডিং ঐচ্ছিক ও সম্মতিভিত্তিক
- সার্ভিসে অযাচিত আচরণের রিপোর্ট
- যাত্রী/ড্রাইভার ব্লক
- আপিল প্রক্রিয়া

### সেফটি অপারেশন SOP

1. SOS চাপলে অ্যাডমিন অ্যালার্ট
2. যাত্রী/ড্রাইভারকে কল
3. জরুরি যোগাযোগকে জানানো
4. প্রয়োজনে স্থানীয় জরুরি সেবা
5. ট্রিপ ডেটা সংরক্ষণ
6. ইনসিডেন্ট রিপোর্ট
7. পরবর্তী পদক্ষেপ: সতর্কতা, স্থগিত, বহিষ্কার, আইনি পদক্ষেপ

---

# ৮. ডেটা মডেল: মূল টেবিলগুলো

নিচে সংক্ষিপ্ত কিন্তু বাস্তব ডেটা মডেল দেওয়া হলো।

## ৮.১ ব্যবহারকারী ও সদস্য

```text
users
- id
- name
- phone
- email
- password_hash
- role
- status
- created_at

rider_profiles
- user_id
- emergency_contact_name
- emergency_contact_phone
- is_member
- preferred_language

driver_profiles
- user_id
- license_number
- license_expiry
- vehicle_id
- verification_status
- online_status
- rating
- acceptance_rate
- total_rides
- member_id
```

---

## ৮.২ সমবায় সদস্য

```text
members
- id
- user_id
- member_type
- join_date
- status
- share_capital
- member_number

shares
- id
- member_id
- share_amount
- issued_at
- status

share_transactions
- id
- member_id
- type: purchase, refund, transfer
- amount
- created_at

patronage_ledger
- id
- member_id
- period
- rides_completed
- fare_generated
- patronage_points
- payout_amount
- status

general_meetings
- id
- title
- agenda
- starts_at
- ends_at

votes
- id
- meeting_id
- member_id
- motion_id
- choice
- cast_at
```

---

## ৮.৩ ভেহিকেল

```text
vehicles
- id
- driver_id
- type: bike, auto, car, micro, scooter
- make
- model
- year
- color
- plate_number
- registration_expiry
- insurance_expiry
- fitness_status
- photos
- status
```

---

## ৮.৪ রাইড

```text
ride_requests
- id
- rider_id
- pickup_lat
- pickup_lng
- pickup_address
- drop_lat
- drop_lng
- drop_address
- ride_type
- fare_estimate_min
- fare_estimate_max
- status
- created_at

rides
- id
- request_id
- rider_id
- driver_id
- vehicle_id
- fare_amount
- platform_fee
- driver_net_amount
- payment_method
- payment_status
- distance_km
- duration_min
- started_at
- completed_at
- cancellation_reason
- rating_by_rider
- rating_by_driver
- safety_flags
- created_at

ride_events
- id
- ride_id
- event_type
- lat
- lng
- timestamp
- metadata

route_points
- id
- ride_id
- point_sequence
- lat
- lng
- timestamp
```

---

## ৮.৫ পেমেন্ট

```text
payments
- id
- ride_id
- rider_id
- amount
- method
- status
- gateway_reference
- created_at

ledger_entries
- id
- reference_type
- reference_id
- account_type
- debit
- credit
- created_at

driver_payouts
- id
- driver_id
- period_start
- period_end
- gross_amount
- deductions
- net_amount
- status
- paid_at
- payment_method

refunds
- id
- payment_id
- reason
- amount
- status
- created_at
```

---

## ৮.৬ নিরাপত্তা ও ডিসপিউট

```text
incidents
- id
- ride_id
- reported_by
- type: accident, harassment, theft, medical, other
- description
- evidence_urls
- status
- created_at

disputes
- id
- ride_id
- raised_by
- reason
- status
- resolution_notes
- resolved_by
- resolved_at

reports
- id
- user_id
- target_type
- target_id
- reason
- status
```

---

# ৯. অ্যাপ ফ্লো

## ৯.১ যাত্রীর রাইড বুকিং ফ্লো

1. যাত্রী অ্যাপ খুলছে
2. পিকআপ লোকেশন স্বয়ংক্রিয় বা ম্যাপে নির্বাচন
3. গন্তব্য লিখছে
4. রাইড টাইপ ও আনুমানিক ফেয়ার দেখছে
5. পেমেন্ট মেথড নির্বাচন
6. রাইড রিকোয়েস্ট
7. সার্ভার ড্রাইভার খুঁজছে
8. ড্রাইভার অ্যাসাইন
9. ড্রাইভারের নাম, ছবি, গাড়ি, রেটিং, প্লেট নম্বর দেখানো
10. লাইভ ট্র্যাকিং
11. রাইড শুরু
12. রাইড শেষ
13. পেমেন্ট
14. রেটিং
15. রসিদ

---

## ৯.২ ড্রাইভার রাইড গ্রহণ ফ্লো

1. ড্রাইভার অনলাইন হয়
2. লোকেশন সার্ভারকে পাঠায়
3. রাইড অফার পায়
4. দূরত্ব, আনুমানিক আয়, গন্তব্য দেখে
5. নির্দিষ্ট সময়ের মধ্যে গ্রহণ/বাতিল
6. পিকআপে নেভিগেশন
7. যাত্রী ওঠে
8. ট্রিপ শুরু
9. গন্তব্যে পৌঁছায়
10. ট্রিপ শেষ
11. পেমেন্ট কনফার্ম
12. রেটিং

---

## ৯.৩ সদস্য হওয়ার ফ্লো

1. অ্যাপে “সদস্য হোন”
2. শর্তাবলি ও বাইলজ সম্মতি
3. ব্যক্তিগত তথ্য ও জাতীয় পরিচয়/ডকুমেন্ট
4. শেয়ার কেনার জন্য পেমেন্ট
5. অ্যাডমিন/বোর্ড অনুমোদন
6. সদস্য নম্বর ও ডিজিটাল সার্টিফিকেট
7. সমবায় পোর্টাল অ্যাক্সেস

---

# ১০. ডিজাইন ও ইউএক্স নীতি

## যাত্রী অ্যাপে

- বড় বাটন, সহজ ভাষা
- ঠিকানা খোঁজা সহজ
- নিরাপত্তা ফিচার দৃশ্যমান
- কম ডেটা ব্যবহার
- অফলাইন রসিদ/হিস্ট্রি

## ড্রাইভার অ্যাপে

- বড় টেক্সট, হাই কনট্রাস্ট
- রাইড অফার কার্ডে পরিষ্কার আয়
- এক হাতে ব্যবহারযোগ্য
- নেভিগেশন দ্রুত
- ব্যাকগ্রাউন্ড লোকেশন নির্ভরযোগ্য
- ডাটা/ব্যাটারি সাশ্রয়ী

## অ্যাডমিনে

- লাইভ ম্যাপ
- ইনসিডেন্ট অ্যালার্ট
- ভেরিফিকেশন কিউ
- পেমেন্ট রিকনসিলিয়েশন
- রিপোর্ট এক্সপোর্ট

---

# ১১. প্রাইসিং, কমিশন ও ইনসেনটিভ নীতি

কো-অপারেটিভ হিসেবে সবচেয়ে বড় সুবিধা: **ন্যায্যতা ও স্বচ্ছতা**।

## ১১.১ কমিশন মডেল

উদাহরণ:

| ফেয়ার স্ল্যাব | প্ল্যাটফর্ম ফি |
|---|---:|
| ৳০–১০০ | ১০% |
| ১০১–৩০০ | ১২% |
| ৩০১+ | ১৫% |

অথবা ফ্ল্যাট:

- বাইকে ১০%
- অটোতে ১২%
- কারে ১৫%

তবে শুরুতে আকর্ষণীয় করতে কমিশন ৫–১০% রাখা যেতে পারে।

---

## ১১.২ ড্রাইভার ইনসেনটিভ

- নির্দিষ্ট সংখ্যক রাইড সম্পন্ন করলে বোনাস
- পিক আওয়ার বোনাস
- নারী ড্রাইভারদের বিশেষ সহায়তা
- নিরাপদ ড্রাইভিং স্কোর
- সদস্য ড্রাইভারদের প্যাট্রোনেজ
- বাতিল কম হলে বোনাস

---

## ১১.৩ যাত্রী রিবেট

- সদস্য যাত্রীরা মাসিক/বার্ষিক ব্যবহার অনুযায়ী রিবেট পেতে পারেন
- রেফারেল রিওয়ার্ড
- পরিবেশবান্ধব রাইডে ছাড়
- শেয়ারড রাইডে ছাড়

---

# ১২. অপারেশনাল ব্লুপ্রিন্ট

## ১২.১ ড্রাইভার অনবোর্ডিং

ধাপ:

1. আবেদন ফর্ম
2. মোবাইল নম্বর ও জাতীয় পরিচয়
3. ড্রাইভিং লাইসেন্স
4. গাড়ির কাগজ
5. বীমা
6. গাড়ির ছবি
7. ব্যাকগ্রাউন্ড চেক, সম্ভব হলে
8. প্রশিক্ষণ
9. অ্যাপ ট্রেনিং
10. পাইলট রাইড
11. সদস্যপদ ঐচ্ছিক/বাধ্যতামূলক
12. অ্যাক্টিভেশন

---

## ১২.২ ড্রাইভার প্রশিক্ষণ

বিষয়:

- অ্যাপ ব্যবহার
- রাস্তা ও নেভিগেশন
- যাত্রী সেবা
- নিরাপত্তা
- নারী যাত্রী ও সংবেদনশীলতা
- দুর্ঘটনা প্রোটোকল
- ক্যাশ হ্যান্ডলিং
- সমবায়ের মূলনীতি
- অধিকার ও দায়িত্ব

---

## ১২.৩ সাপোর্ট

- ইন-অ্যাপ চ্যাট
- হটলাইন
- ইমেইল
- অফিস/হেল্পডেস্ক
- ড্রাইভার হাব
- জরুরি সেফটি টিম
- এসএলএ: সাধারণ অভিযোগ ২৪ ঘণ্টা, সেফটি ১ ঘণ্টা

---

# ১৩. নিরাপত্তা, ঝুঁকি ও কমপ্লায়েন্স

## ১৩.১ প্রযুক্তিগত নিরাপত্তা

- HTTPS বাধ্যতামূলক
- টোকেন-ভিত্তিক অথেনটিকেশন
- রোল-ভিত্তিক অ্যাক্সেস কন্ট্রোল
- রেট লিমিটিং
- ডিভাইস ট্রাস্ট স্কোর
- লগইন অ্যালার্ট
- সেনসিটিভ ডেটা এনক্রিপশন
- লোকেশন ডেটা অ্যাক্সেস লগ
- নিয়মিত পেনটেস্ট
- ব্যাকআপ
- ডিজাস্টার রিকভারি
- অডিট লগ

---

## ১৩.২ প্রতারণা প্রতিরোধ

প্রধান ঝুঁকি:

- ফেক রাইড
- জিপিএস স্পুফিং
- প্রোমো অপব্যবহার
- ড্রাইভার অ্যাকাউন্ট শেয়ার
- যাত্রীর মিথ্যা অভিযোগ
- ড্রাইভারের মিথ্যা আচরণ রিপোর্ট
- ক্যাশ আত্মসাৎ
- পেমেন্ট চার্জব্যাক

প্রতিরোধ:

- ডিভাইস বাইন্ডিং
- বায়োমেট্রিক/সেলফি ভেরিফিকেশন
- রাইড শুরুতে যাত্রী OTP
- ট্রিপ প্যাটার্ন ডিটেকশন
- অস্বাভাবিক আয় ফ্ল্যাগ
- রেটিং অ্যানোমালি
- ডকুমেন্ট ভেরিফিকেশন
- ক্যাশ রিকনসিলিয়েশন
- অভিযোগে উভয় পক্ষের শুনানি

---

# ১৪. অ্যানালিটিক্স ও KPI

## ১৪.১ ব্যবসায়িক মেট্রিক্স

- দৈনিক রাইড সংখ্যা
- মাসিক সক্রিয় যাত্রী
- মাসিক সক্রিয় ড্রাইভার
- গড় রাইড ফেয়ার
- প্ল্যাটফর্ম রেভিনিউ
- ড্রাইভার গড় আয়
- কমিশন হার
- কাস্টমার অ্যাকুইজিশন খরচ
- ড্রাইভার অ্যাকুইজিশন খরচ
- চার্জব্যাক/রিফান্ড হার

## ১৪.২ অপারেশনাল মেট্রিক্স

- রাইড অ্যাকসেপ্ট্যান্স রেট
- ক্যানসেলেশন রেট
- গড় এটিএ
- পিকআপ টাইম
- রাইড সম্পন্ন হার
- ম্যাচিং সফলতা
- ড্রাইভার অনলাইন ঘণ্টা
- ড্রাইভার প্রতি রাইড

## ১৪.৩ সমবায় মেট্রিক্স

- মোট সদস্য
- ড্রাইভার সদস্য সংখ্যা
- যাত্রী সদস্য সংখ্যা
- শেয়ার মূলধন
- প্যাট্রোনেজ বণ্টন
- ভোটদান হার
- সদস্য সন্তুষ্টি
- ড্রাইভার টার্ণওভার

## ১৪.৪ সেফটি মেট্রিক্স

- দুর্ঘটনা সংখ্যা
- জরুরি বাটন ব্যবহার
- হয়রানি অভিযোগ
- সমাধান সময়
- বীমা ক্লেম
- সেফটি রেটিং

---

# ১৫. রোডম্যাপ: ধাপে ধাপে বাস্তবায়ন

## ফেজ ০: প্রস্তুতি ও গবেষণা

সময়: ৪–৬ সপ্তাহ

কাজ:

- বাজার গবেষণা
- প্রতিযোগিতা বিশ্লেষণ
- ড্রাইভার/যাত্রী সাক্ষাৎকার
- লক্ষ্য শহর ও এলাকা নির্বাচন
- সমবায় কাঠামো নির্ধারণ
- বাইলজ খসড়া
- আইনি পরামর্শ
- আর্থিক মডেল
- ব্র্যান্ডিং
- প্রাথমিক বাজেট

আউটপুট:

- ফিজিবিলিটি রিপোর্ট
- সমবায় গঠনতন্ত্র
- প্রোডাক্ট রিকোয়ারমেন্ট ডকুমেন্ট
- প্রাথমিক আর্থিক প্রক্ষেপণ

---

## ফেজ ১: ন্যূনতম ভিয়েবল প্রোডাক্ট

সময়: ৩–৪ মাস

### MVP-তে যা থাকবে

- যাত্রী: সাইনআপ, রাইড বুকিং, লাইভ ট্র্যাকিং, রেটিং, ক্যাশ পেমেন্ট
- ড্রাইভার: অনলাইন/অফলাইন, রাইড অ্যাসাইন, ট্রিপ স্ট্যাটাস, আয় ড্যাশবোর্ড
- অ্যাডমিন: ইউজার, রাইড, ড্রাইভার ভেরিফিকেশন, ফেয়ার সেটিং, রিপোর্ট
- সমবায়: সদস্য আবেদন, শেয়ার রেকর্ড, সদস্য তালিকা
- নিরাপত্তা: লাইভ শেয়ার, SOS, ইনসিডেন্ট টিকেট

### যা বাদ রাখা যাবে

- জটিল প্যাট্রোনেজ অটোমেশন
- ই-ভোট
- মাল্টি-সিটি
- অ্যাডভান্সড ডাইনামিক প্রাইসিং
- শেয়ারড রাইড
- কর্পোরেট পেমেন্ট
- কার্ড পেমেন্ট

---

## ফেজ ২: বন্ধ বীটা পাইলট

সময়: ৬–৮ সপ্তাহ

- ২০–৫০ ড্রাইভার
- ৩০০–১,০০০ যাত্রী
- নির্দিষ্ট ২–৩ এলাকা
- দিনে নির্দিষ্ট সময়
- সেফটি ও অপারেশন টিম সক্রিয়
- প্রতিদিন বাগ ফিক্স
- ফিডব্যাক লুপ

---

## ফেজ ৩: পাবলিক লঞ্চ

সময়: ফেজ ২ সফল হলে

- এলাকা সম্প্রসারণ
- মার্কেটিং
- সদস্যপদ ক্যাম্পেইন
- ড্রাইভার রিক্রুটমেন্ট
- পেমেন্ট অপশন বাড়ানো
- সেফটি অডিট
- প্রেস/কমিউনিটি ইভেন্ট

---

## ফেজ ৪: স্কেল

- নতুন শহর
- নতুন ভেহিকেল টাইপ
- শেয়ারড রাইড
- সাবস্ক্রিপশন
- কর্পোরেট যাতায়াত
- ডেলিভারি/লজিস্টিক্স সংযুক্তি
- ই-ভোট ও পূর্ণ গভর্নেন্স
- ডেটা অ্যানালিটিক্স
- নিজস্ব ম্যাপ ডেটা উন্নয়ন

---

# ১৬. টিম

প্রাথমিক লিড টিম:

| ভূমিকা | দায়িত্ব |
|---|---|
| প্রোজেক্ট লিড/সিইও | কৌশল, সমন্বয়, ফান্ডিং |
| অপারেশন ম্যানেজার | ড্রাইভার, ফ্লিট, সেফটি |
| কমিউনিটি/কো-অপ ম্যানেজার | সদস্যপদ, গভর্নেন্স |
| আইনি উপদেষ্টা | সমবায়, ট্রান্সপোর্ট, ডেটা |
| প্রোডাক্ট ম্যানেজার | রোডম্যাপ, ইউএক্স |
| ইউআই/ইউএক্স ডিজাইনার | অ্যাপ ডিজাইন |
| ব্যাকএন্ড ডেভেলপার | সার্ভার, এপিআই |
| মোবাইল ডেভেলপার | রাইডার/ড্রাইভার অ্যাপ |
| ফ্রন্টএন্ড ডেভেলপার | অ্যাডমিন প্যানেল |
| কোয়ালিটি অ্যাসুরেন্স | টেস্টিং |
- পরে: ডেভঅপস, ডেটা অ্যানালিস্ট, সাপোর্ট এজেন্ট, সেফটি অফিসার

---

# ১৭. আনুমানিক খরচ

খরচ দেশ, টিম, স্কোপ অনুযায়ী অনেক বদলাবে। নিচেরটি একটি ধারণা।

## ১৭.১ প্রাথমিক খরচ

| খাত | আনুমানিক পরিসর |
|---|---:|
| সমবায় নিবন্ধন ও আইনি | ৫০,০০০–৩,০০,০০০ টাকা |
| ব্র্যান্ডিং ও ডিজাইন | ৫০,০০০–২,০০,০০০ টাকা |
| প্রোডাক্ট রিসার্চ | ৫০,০০০–১,৫০,০০০ টাকা |
| এমভিপি ডেভেলপমেন্ট | ২৫,০০,০০০–৮০,০০,০০০ টাকা |
| পাইলট অপারেশন | ৩,০০,০০০–১০,০০,০০০ টাকা |
| মার্কেটিং | ১,০০,০০০–৫,০০,০০০ টাকা |
| বীমা/কমপ্লায়েন্স | প্রকল্পভেদে |

## ১৭.২ মাসিক চলমান খরচ

- ক্লাউড/সার্ভার
- ম্যাপ এপিআই
- এসএমএস
- পেমেন্ট গেটওয়ে ফি
- সাপোর্ট টিম
- অফিস/হেল্পডেস্ক
- মার্কেটিং
- বীমা
- অডিট/অ্যাকাউন্টিং

খরচ কমাতে:

- ওপেন-সোর্স ম্যাপ/রাউটিং
- সীমিত এলাকায় পাইলট
- ম্যানুয়াল সদস্যপদ শুরুতে
- ক্যাশ-ফার্স্ট পেমেন্ট
- ফ্রিল্যান্স/হাইব্রিড টিম

---

# ১৮. ঝুঁকি ও প্রতিকার

| ঝুঁকি | প্রতিকার |
|---|---|
| যাত্রী পায় না, ড্রাইভার পায় না | এক এলাকায় ঘন পাইলট, ইনসেনটিভ, কমিউনিটি মার্কেটিং |
| ড্রাইভার দ্রুত চলে যায় | ন্যায্য কমিশন, স্বচ্ছ আয়, সদস্যপদ, বীমা |
| নিরাপত্তা ঘটনা | SOS, ট্রিপ শেয়ার, ভেরিফিকেশন, দ্রুত রেসপন্স টিম |
| আইনি বাধা | আগে আইনি পরামর্শ, স্থানীয় কর্তৃপক্ষের সঙ্গে যোগাযোগ |
| পেমেন্ট জটিলতা | শুরুতে ক্যাশ, পরে লাইসেন্সধারী পেমেন্ট অংশীদার |
| সমবায় রাজনীতি/দ্বন্দ্ব | স্বচ্ছ বাইলজ, নিয়মিত মিটিং, অডিট, আপিল প্রক্রিয়া |
| ম্যাপ/ঠিকানা সমস্যা | ল্যান্ডমার্ক, ম্যানুয়াল পিন, লোকাল ম্যাপ ডেটা সংশোধন |
| প্রযুক্তি ব্যর্থতা | মনিটরিং, ব্যাকআপ, ইনসিডেন্ট রেসপন্স, ফলব্যাক সার্ভিস |
| ফ্রড | ডিভাইস বাইন্ডিং, রাইড ভেরিফিকেশন, লেজার অডিট |

---

# ১৯. সমবায় গভর্নেন্স ডিজাইন

## ১৯.১ বোর্ড

প্রস্তাবিত:

- সভাপতি
- সহ-সভাপতি
- সাধারণ সম্পাদক
- কোষাধ্যক্ষ
- সদস্য
- ড্রাইভার প্রতিনিধি
- যাত্রী প্রতিনিধি
- নারী প্রতিনিধি
- নির্বাহী/প্রযুক্তি উপদেষ্টা, অ-ভোটাধিকার থাকতে পারে

## ১৯.২ নীতিগত সিদ্ধান্ত

সাধারণ পরিষদে ভোট হতে পারে:

- কমিশন হার পরিবর্তন
- ফেয়ার ফর্মুলা
- মুনাফা বণ্টন
- নতুন সদস্য ক্যাটাগরি
- বার্ষিক বাজেট
- নির্বাহী নিয়োগ
- সেফটি নীতি
- বীমা পরিকল্পনা

## ১৯.৩ স্বচ্ছতা

প্রকাশ করা উচিত:

- মাসিক রাইড সারাংশ
- কমিশন আয়
- ড্রাইভার গড় আয়
- প্ল্যাটফর্ম খরচ
- সেফটি ইনসিডেন্ট
- মুনাফা বণ্টন
- অডিট রিপোর্ট

---

# ২০. কারিগরি বাস্তবায়ন: প্রথম ১২ সপ্তাহের পরিকল্পনা

## সপ্তাহ ১–২: ফিজিবিলিটি ও দল

- সমবায় আইনজ্ঞের সঙ্গে বৈঠক
- বাইলজ খসড়া
- প্রতিষ্ঠাতা সদস্য সংগ্রহ
- লক্ষ্য এলাকা নির্বাচন
- প্রতিযোগিতা ও ফেয়ার গবেষণা

## সপ্তাহ ৩–৪: প্রোডাক্ট ডিজাইন

- ইউজার ফ্লো
- ওয়্যারফ্রেম
- ফেয়ার মডেল
- কমিশন মডেল
- সেফটি নীতি
- ডেটা মডেল খসড়া

## সপ্তাহ ৫–৮: কোর ডেভেলপমেন্ট

- অথেনটিকেশন
- লোকেশন সার্ভিস
- রাইড রিকোয়েস্ট
- ম্যাচিং
- ড্রাইভার অ্যাপ বেসিক
- অ্যাডমিন রাইড মনিটরিং

## সপ্তাহ ৯–১০: পেমেন্ট, সেফটি, অ্যাডমিন

- ক্যাশ লেজার
- ফেয়ার ক্যালকুলেশন
- SOS
- ট্রিপ শেয়ার
- রিপোর্টিং
- ডকুমেন্ট আপলোড

## সপ্তাহ ১১: টেস্টিং

- অভ্যন্তরীণ টেস্ট
- ফিল্ড টেস্ট
- দুর্বল নেটওয়ার্ক টেস্ট
- সিকিউরিটি বেসিক টেস্ট
- পেমেন্ট রিকনসিলিয়েশন টেস্ট

## সপ্তাহ ১২: পাইলট প্রস্তুতি

- ড্রাইভার ট্রেনিং
- হেল্পলাইন চালু
- সেফটি চেকলিস্ট
- মার্কেটিং ম্যাটেরিয়াল
- সীমিত লঞ্চ

---

# ২১. পাইলটের জন্য সুপারিশ

## এলাকা

একটি সীমিত কিন্তু ঘন এলাকা ভালো:

- বিশ্ববিদ্যালয় ক্যাম্পাস/আশেপাশে
- নির্দিষ্ট আবাসিক এলাকা
- অফিস পাড়া
- মেডিকেল/নার্সিং রুট
- নারী-নিরাপদ যাতায়াত প্রয়োজন এমন এলাকা

## যানবাহন

শুরুতে:

- মোটরসাইকেল/বাইক: দ্রুত ও কম খরচ
- অটোরিকশা: স্থানীয় গ্রহণযোগ্যতা
- কার: পরে
- নারী-চালিত বা নারী-বিশেষ রাইড: নিরাপত্তা ও বৈষম্য কমাতে

## সময়

- সকাল অফিস/স্কুল
- সন্ধ্যা ফেরত
- বৃষ্টি/রাতে আলাদা নিরাপত্তা প্রোটোকল

---

# ২২. আর্থিক মডেল উদাহরণ

ধরা যাক একটি রাইড:

- যাত্রী ফেয়ার: ৳১০০
- প্ল্যাটফর্ম ফি: ১০% = ৳১০
- পেমেন্ট ফি: ৳২
- ড্রাইভার পায়: ৳৮৮

মাসে ১০,০০০ রাইড হলে:

- মোট ফেয়ার ভলিউম: ৳১০,০০,০০০
- প্ল্যাটফর্ম ফি ১০%: ৳১,০০,০০০
- পেমেন্ট/এসএমএস/ম্যাপ খরচ ধরা যাক ৳২০,০০০
- অপারেশন/সাপোর্ট/টেক খরচ: ৳৬০,০০০
- নিট উদ্বৃত্ত: ৳২০,০০০

এই উদ্বৃত্ত রিজার্ভ, প্রযুক্তি, প্যাট্রোনেজ ও কল্যাণে বণ্টন হবে।

---

# ২৩. পণ্যের পার্থক্যকারী বৈশিষ্ট্য

আপনার কো-অপ অ্যাপকে আলাদা করতে পারে:

1. **স্বচ্ছ ফেয়ার ফর্মুলা**
2. **কম ও ন্যায্য কমিশন**
3. **ড্রাইভার মালিকানা**
4. **যাত্রী সদস্য রিবেট**
5. **নিরাপত্তা প্রথম**
6. **স্থানীয় ভাষা ও ঠিকানা সহায়তা**
7. **নারী-নিরাপদ রাইড**
8. **পরিবেশবান্ধব রাইড**
9. **অডিটযোগ্য আর্থিক রিপোর্ট**
10. **গণতান্ত্রিক সিদ্ধান্ত**

---

# ২৪. প্রযুক্তিগত ঝুঁকি কমানোর টিপস

- শুরু থেকে সবকিছু পারফেক্ট বানানোর চেষ্টা করবেন না
- প্রথমে ম্যানুয়াল অপারেশন চলতে পারে
- ড্রাইভার অ্যাপে লো-ডাটা মোড রাখুন
- ম্যাপ এপিআই ফলব্যাক রাখুন
- লোকেশন আপডেট ব্যাচ করুন, প্রতি সেকেন্ডে নয়, প্রয়োজন অনুযায়ী
- ব্যাটারি অপ্টিমাইজেশন করুন
- অফলাইন ইভেন্ট কুই রাখুন
- রাইড ডেটা এনক্রিপ্ট করুন
- অডিট লগ রাখুন
- ডেটাবেসে জিও-ইনডেক্স ব্যবহার করুন

---

# ২৫. বাস্তবায়নের জন্য ডকুমেন্ট তালিকা

আপনার এগুলো তৈরি করতে হবে:

1. ফিজিবিলিটি রিপোর্ট
2. সমবায় বাইলজ
3. সদস্যপদ চুক্তি
4. ড্রাইভার চুক্তি/অংশগ্রহণ শর্ত
5. যাত্রী শর্তাবলি
6. প্রাইভেসি পলিসি
7. সেফটি পলিসি
8. ডেটা রিটেনশন পলিসি
9. অভিযোগ প্রতিকার নীতি
10. মুনাফা বণ্টন নীতি
11. ফেয়ার ফর্মুলা
12. কমিশন নীতি
13. ইনসিডেন্ট রেসপন্স প্লে
14. অ্যাডমিন রোল পারমিশন ম্যাট্রিক্স
15. পিআরডি
16. টেকনিক্যাল ডিজাইন ডকুমেন্ট
17. টেস্ট প্ল্যান
18. পাইলট পরিকল্পনা
19. মার্কেটিং প্ল্যান
20. আর্থিক প্রক্ষেপণ

---

# ২৬. প্রাথমিক এপিআই নকশা উদাহরণ

নিচে কিছু উদাহরণ এপিআই দেওয়া হলো:

```text
POST /api/v1/auth/otp/request
POST /api/v1/auth/otp/verify

GET  /api/v1/profile
PUT  /api/v1/profile

POST /api/v1/rides/estimate
POST /api/v1/rides
GET  /api/v1/rides/{id}
POST /api/v1/rides/{id}/cancel

POST /api/v1/driver/online
POST /api/v1/driver/offline
POST /api/v1/driver/location
POST /api/v1/driver/rides/{id}/accept
POST /api/v1/driver/rides/{id}/decline
POST /api/v1/driver/rides/{id}/arrived
POST /api/v1/driver/rides/{id}/start
POST /api/v1/driver/rides/{id}/complete

POST /api/v1/payments/initiate
POST /api/v1/payments/webhook

GET  /api/v1/member/profile
POST /api/v1/member/join
POST /api/v1/member/shares/purchase
GET  /api/v1/member/patronage

POST /api/v1/safety/sos
POST /api/v1/incidents
GET  /api/v1/admin/rides
GET  /api/v1/admin/drivers/pending
```

---

# ২৭. সেফটি ফিচারের বিশদ

## যাত্রীর জন্য

- রাইড শেয়ার লিংক
- ট্রিপ কোড
- SOS
- জরুরি যোগাযোগ
- বিশ্বস্ত ব্যক্তি
- ড্রাইভার প্রোফাইল ভেরিফিকেশন ব্যাজ
- রিপোর্ট
- ব্লক

## ড্রাইভারের জন্য

- জরুরি বাটন
- মিথ্যা অভিযোগে আপিল
- নিরাপদ পিকআপ পয়েন্ট
- ক্যাশ সংগ্রহ সতর্কতা
- সহায়তা হটলাইন
- দুর্ঘটনা বীমা

## প্ল্যাটফর্মের জন্য

- ইনসিডেন্ট ড্যাশবোর্ড
- অ্যালার্ট রুল
- অডিট লগ
- ফরেনসিক ডেটা সংরক্ষণ
- নিরপেক্ষ তদন্ত

---

# ২৮. কো-অপ প্রযুক্তিতে বিশেষ মডিউল: প্যাট্রোনেজ ইঞ্জিন

এটি আপনার অ্যাপকে প্রকৃত কো-অপারেটিভ করবে।

### ইনপুট

- ড্রাইভার সম্পন্ন রাইড
- ড্রাইভার আয়
- যাত্রীর মোট ফেয়ার
- সদস্যপদ সময়কাল
- সদস্য শেয়ার
- নীতিমালা

### আউটপুট

- প্যাট্রোনেজ পয়েন্ট
- বণ্টনযোগ্য অর্থ
- সদস্যভিত্তিক বণ্টন
- অনুমোদন ওয়ার্কফ্লো
- ট্যাক্স রিপোর্ট
- অডিট ট্রেইল

উদাহরণ সূত্র:

```
driver_patronage_share =
  total_driver_pool ×
  (driver_completed_rides / all_driver_completed_rides)

rider_patronage_share =
  total_rider_pool ×
  (rider_paid_fare / all_rider_paid_fare)
```

তবে আইন ও বাইলজ অনুযায়ী ক্যাপ বা ন্যূনতম সীমা রাখা যেতে পারে।

---

# ২৯. লঞ্চের আগে চূড়ান্ত চেকলিস্ট

## আইনি

- সমবায় নিবন্ধন সম্পন্ন/প্রক্রিয়াধীন
- বাইলজ অনুমোদিত
- ব্যাংক অ্যাকাউন্ট
- ট্যাক্স/TIN প্রস্তুত
- পরিবহন/স্থানীয় অনুমতি পর্যালোচিত
- ডেটা প্রাইভেসি নীতি প্রকাশিত

## প্রোডাক্ট

- যাত্রী অ্যাপ টেস্ট সম্পন্ন
- ড্রাইভার অ্যাপ টেস্ট সম্পন্ন
- অ্যাডমিন প্যানেল প্রস্তুত
- রাইড লাইফসাইকেল টেস্ট সম্পন্ন
- ক্যানসেলেশন/রিফান্ড ফ্লো টেস্ট সম্পন্ন

## অপারেশন

- ড্রাইভার ভেরিফিকেশন প্রক্রিয়া
- ট্রেনিং সম্পন্ন
- হেল্পলাইন চালু
- সেফটি টিম
- ইনসিডেন্ট প্লে
- বীমা প্রস্তুত, সম্ভব হলে

## আর্থিক

- প্ল্যাটফর্ম ফি সেট
- ড্রাইভার পেআউট প্রক্রিয়া
- ক্যাশ রিকনসিলিয়েশন
- লেজার অডিট
- পাইলট বাজেট অনুমোদিত

---

# ৩০. সবচেয়ে গুরুত্বপূর্ণ পরামর্শ

1. **প্রযুক্তির আগে সমবায়ের আইনি ও গভর্নেন্স কাঠামো ঠিক করুন।**
2. **একটি ছোট এলাকায় পাইলট করুন।**
3. **ড্রাইভারদের মালিকানা ও স্বচ্ছ আয় দিন।**
4. **যাত্রীদের নিরাপত্তা সর্বোচ্চ অগ্রাধিকার দিন।**
5. **ক্যাশ-ফার্স্ট শুরু করুন; জটিল পেমেন্ট পরে যোগ করুন।**
6. **সব আর্থিক লেনদেন লেজারভিত্তিক রাখুন।**
7. **সদস্যদের কাছে নিয়মিত রিপোর্ট প্রকাশ করুন।**
8. **ম্যাপ/ঠিকানা সমস্যা স্থানীয়ভাবে সমাধান করুন।**
9. **অ্যাডমিন প্যানেলে সেফটি ও ডিসপিউট ম্যানেজমেন্ট শক্ত রাখুন।**
10. **ধাপে ধাপে স্কেল করুন; একবারে বড় শহরজুড়ে যাবেন না।**

---

# ৩১. সম্ভাব্য সর্বনিম্ন কার্যকর সংস্করণ

যদি খুব দ্রুত শুরু করতে চান, তাহলে এমভিপি হতে পারে:

- ফোন + OTP
- রাইডার রাইড বুকিং
- ড্রাইভার অ্যাপে রাইড গ্রহণ
- লাইভ লোকেশন
- ক্যাশ পেমেন্ট
- রেটিং
- অ্যাডমিন ড্যাশবোর্ড
- ড্রাইভার ডকুমেন্ট আপলোড
- সদস্য আবেদন ফর্ম
- SOS বাটন
- ট্রিপ শেয়ার

পরে যোগ করবেন:

- পূর্ণ সদস্য লেজার
- প্যাট্রোনেজ অটোমেশন
- ই-ভোট
- ওয়ালেট
- মোবাইল মানি/কার্ড
- শেয়ারড রাইড
- মাল্টি-সিটি

---

# ৩২. আপনার জন্য পরবর্তী ধাপ

আপনি চাইলে এখান থেকে এগোতে পারেন এভাবে:

1. একটি ১০–২০ জন ড্রাইভার ও ১০০ জন যাত্রীর সমীক্ষা করুন
2. লক্ষ্য এলাকা নির্বাচন করুন
3. সমবায় রেজিস্ট্রেশন প্রক্রিয়া শুরু করুন
4. ফেয়ার ও কমিশন মডেল খসড়া করুন
5. অ্যাপের ওয়্যারফ্রেম বানান
6. ডেভেলপমেন্ট টিম বা ভেন্ডর নির্বাচন করুন
7. ৯০ দিনের পাইলট পরিকল্পনা চূড়ান্ত করুন

---

চাইলে আমি পরের ধাপে আপনার জন্য আলাদাভাবে তৈরি করে দিতে পারি:

1. **বাংলাদেশ-নির্দিষ্ট সমবায় নিবন্ধন চেকলিস্ট**  
2. **সম্পূর্ণ প্রোডাক্ট রিকোয়ারমেন্ট ডকুমেন্ট**  
3. **ডেটাবেস স্কিমা ও এপিআই স্পেসিফিকেশন**  
4. **পাইলট লঞ্চ প্ল্যান ও বাজেট শিট**  
5. **ড্রাইভার/যাত্রী অ্যাপের ওয়্যারফ্রেম তালিকা**


অবশ্যই। ভারতে প্রাথমিকভাবে চালানোর জন্য এবং **একটি কোডবেস থেকে Android, iOS ও ওয়েব অ্যাডমিন প্যানেল**—এই চাহিদা মাথায় রেখে নিচে একটি বাস্তবায়নযোগ্য, বিস্তারিত মোবাইল অ্যাপ ব্লুপ্রিন্ট দিচ্ছি।

> গুরুত্বপূর্ণ: এটি টেকনিক্যাল/প্রোডাক্ট ব্লুপ্রিন্ট। ভারতে সমবায়, পরিবহন, পেমেন্ট, ডেটা প্রোটেকশন ইত্যাদি বিষয়ে নিবন্ধনের আগে একজন **ভারতীয় আইনজীবী + চার্টার্ড অ্যাকাউন্ট্যান্ট + রেজিস্ট্রার-অভিজ্ঞ উপদেষ্টা** নিয়ে কাজ করবেন।

---

# ১. মূল সুপারিশ: এক কোডবেসের জন্য সেরা পদ্ধতি

আপনি চান:

1. গ্রাহক/যাত্রী অ্যাপ  
2. ড্রাইভার অ্যাপ  
3. মোবাইল অ্যাডমিন অ্যাপ  
4. ওয়েব অ্যাডমিন প্যানেল  
5. সব Android + iOS + Web এ চলবে  
6. সব একটি কোডবেসে থাকবে  

এর জন্য সবচেয়ে বাস্তবসম্মত টেকনোলজি হলো:

## প্রস্তাবিত ফ্রন্টএন্ড: **Flutter**

কারণ:

- একই Dart কোডবেস থেকে Android ও iOS অ্যাপ হয়
- Flutter Web দিয়ে ওয়েব অ্যাডমিন প্যানেলও বানানো যায়
- একই ডিজাইন সিস্টেম, একই বিজনেস লজিক, একই নেটওয়ার্ক লেয়ার শেয়ার করা যায়
- দ্রুত ডেভেলপমেন্ট হয়
- ভারতে ড্রাইভার অ্যাপের জন্য ব্যাকগ্রাউন্ড লোকেশন, ফোরগ্রাউন্ড সার্ভিস, পুশ নোটিফিকেশন—এগুলো সামলানো সম্ভব

তবে একটি গুরুত্বপূর্ণ কথা:

> **“এক কোডবেস” মানে একই অ্যাপ বাইনারি নয়।**  
> প্রোডাকশনের জন্য একই রিপোজিটরি/কোডবেস থেকে আলাদা আলাদা অ্যাপ বিল্ড করা ভালো:
> - Rider App
> - Driver App
> - Admin Mobile App
> - Admin Web Panel

কারণ ড্রাইভার অ্যাপে ব্যাকগ্রাউন্ড লোকেশন, ক্যামেরা, স্টোরেজ, নোটিফিকেশন বেশি লাগে। যাত্রী অ্যাপে কম পারমিশন লাগে। অ্যাডমিন অ্যাপে সম্পূর্ণ ভিন্ন পারমিশন ও নিরাপত্তা লাগে। তাই একই কোডবেস থেকে আলাদা টার্গেট/এন্ট্রি পয়েন্ট দিয়ে আলাদা অ্যাপ বানানো সবচেয়ে ভালো।

---

# ২. প্রস্তাবিত সিস্টেম আর্কিটেকচার

## ২.১ সামগ্রিক চিত্র

```text
Rider App (Android/iOS)
Driver App (Android/iOS)
Admin Mobile App (Android/iOS)
Admin Web Panel (Flutter Web)
        ↓
   API Gateway / REST + WebSocket/MQTT
        ↓
Backend Services
├─ Auth Service
├─ User & Role Service
├─ KYC Service
├─ Fleet/Driver Service
├─ Geo/Location Service
├─ Ride Service
├─ Dispatch/Matching Service
├─ Pricing Service
├─ Payment Service
├─ Ledger/Finance Service
├─ Notification Service
├─ Safety Service
├─ Admin Service
├─ Cooperative Membership Service
└─ Analytics/Audit Service
        ↓
PostgreSQL + PostGIS + Redis + Object Storage + Queue
        ↓
Third-party India Integrations
├─ Google Maps Platform
├─ SMS DLT Provider
├─ Razorpay / Cashfree / Paytm
├─ FCM / APNs
├─ Digilocker / KYC partners
├─ PAN/Bank verification
└─ Sentry / Analytics
```

---

# ৩. এক কোডবেস: প্রস্তাবিত Flutter Monorepo কাঠামো

একটি বড় একক প্রজেক্টও করা যায়, কিন্তু আমি **Monorepo + Packages** পদ্ধতি সুপারিশ করছি। এতে কোড পরিষ্কার থাকে এবং একই কোড থেকে একাধিক অ্যাপ বিল্ড করা যায়।

## ৩.১ প্রস্তাবিত রিপো স্ট্রাকচার

```text
coop_ride/
├─ melos.yaml
├─ pubspec.yaml
├─ .github/
│  └─ workflows/
├─ packages/
│  ├─ core/
│  │  ├─ network/
│  │  ├─ auth/
│  │  ├─ storage/
│  │  ├─ location/
│  │  ├─ maps/
│  │  ├─ realtime/
│  │  ├─ permissions/
│  │  ├─ analytics/
│  │  ├─ payments/
│  │  ├─ kyc/
│  │  └─ i18n/
│  │
│  ├─ design_system/
│  │  ├─ buttons/
│  │  ├─ colors/
│  │  ├─ typography/
│  │  ├─ cards/
│  │  ├─ forms/
│  │  └─ responsive/
│  │
│  ├─ features/
│  │  ├─ rider/
│  │  ├─ driver/
│  │  ├─ admin/
│  │  ├─ coop_member/
│  │  ├─ safety/
│  │  ├─ payments/
│  │  ├─ support/
│  │  └─ onboarding/
│  │
│  └─ api_client/
│     ├─ generated_openapi/
│     ├─ interceptors/
│     └─ models/
│
├─ apps/
│  ├─ rider_app/
│  │  ├─ lib/
│  │  │  ├─ main_dev.dart
│  │  │  ├─ main_prod.dart
│  │  │  └─ app/
│  │  ├─ android/
│  │  └─ ios/
│  │
│  ├─ driver_app/
│  │  ├─ lib/
│  │  │  ├─ main_dev.dart
│  │  │  ├─ main_prod.dart
│  │  │  └─ app/
│  │  ├─ android/
│  │  └─ ios/
│  │
│  ├─ admin_app/
│  │  ├─ lib/
│  │  │  ├─ main_dev.dart
│  │  │  ├─ main_prod.dart
│  │  │  └─ app/
│  │  ├─ android/
│  │  └─ ios/
│  │
│  └─ admin_web/
│     ├─ lib/
│     │  ├─ main_dev.dart
│     │  ├─ main_prod.dart
│     │  └─ app/
│     └─ web/
│
├─ backend/
│  ├─ api/
│  ├─ dispatch_worker/
│  ├─ geo_service/
│  ├─ payment_service/
│  └─ infra/
│
└─ docs/
   ├─ api.md
   ├─ database.md
   ├─ india_compliance.md
   ├─ release_process.md
   └─ runbooks/
```

---

# ৪. একই কোডবেস থেকে কীভাবে আলাদা অ্যাপ বিল্ড করবেন

## ৪.১ Entry point পদ্ধতি

প্রতিটি অ্যাপের জন্য আলাদা `main` ফাইল থাকবে।

### উদাহরণ

```text
apps/rider_app/lib/main_prod.dart
apps/driver_app/lib/main_prod.dart
apps/admin_app/lib/main_prod.dart
apps/admin_web/lib/main_prod.dart
```

প্রতিটি `main` ফাইল আলাদা ভূমিকা লোড করবে।

### উদাহরণ কোড

```dart
import 'package:coop_ride_core/bootstrap.dart';
import 'package:features_rider/rider_app.dart';

void main() {
  bootstrap(
    appRole: AppRole.rider,
    builder: () => const RiderApp(),
  );
}
```

ড্রাইভার অ্যাপ:

```dart
import 'package:coop_ride_core/bootstrap.dart';
import 'package:features_driver/driver_app.dart';

void main() {
  bootstrap(
    appRole: AppRole.driver,
    builder: () => const DriverApp(),
  );
}
```

অ্যাডমিন মোবাইল:

```dart
import 'package:coop_ride_core/bootstrap.dart';
import 'package:features_admin/admin_mobile_app.dart';

void main() {
  bootstrap(
    appRole: AppRole.admin,
    builder: () => const AdminMobileApp(),
  );
}
```

অ্যাডমিন ওয়েব:

```dart
import 'package:coop_ride_core/bootstrap.dart';
import 'package:features_admin/admin_web_app.dart';

void main() {
  bootstrap(
    appRole: AppRole.adminWeb,
    builder: () => const AdminWebApp(),
  );
}
```

---

## ৪.২ বিল্ড কমান্ড

### Rider Android

```bash
cd apps/rider_app
flutter build appbundle --release --target lib/main_prod.dart --dart-define=ENV=prod
```

### Driver Android

```bash
cd apps/driver_app
flutter build appbundle --release --target lib/main_prod.dart --dart-define=ENV=prod
```

### Admin Mobile Android/iOS

```bash
cd apps/admin_app
flutter build appbundle --release --target lib/main_prod.dart --dart-define=ENV=prod
```

### Admin Web

```bash
cd apps/admin_web
flutter build web --release --target lib/main_prod.dart --dart-define=ENV=prod
```

---

# ৫. প্রস্তাবিত প্রযুক্তি স্ট্যাক

## ৫.১ ফ্রন্টএন্ড / মোবাইল / ওয়েব

| অংশ | প্রযুক্তি |
|---|---|
| মোবাইল অ্যাপ | Flutter |
| ওয়েব অ্যাডমিন | Flutter Web |
| State Management | Riverpod বা Bloc |
| Navigation | go_router |
| Networking | Dio + Retrofit |
| Model | freezed + json_serializable |
| Local Storage | SharedPreferences + flutter_secure_storage |
| Maps | google_maps_flutter |
| Location | geolocator + permission_handler |
| Background Location | flutter_background_service / native channel |
| Push Notification | Firebase Cloud Messaging + APNs |
| Crash/Errors | Sentry বা Firebase Crashlytics |
| Analytics | Firebase Analytics / PostHog |
| Localizations | flutter_localizations + intl / slang |
| Monorepo management | Melos |

আমি ব্যক্তিগতভাবে প্রস্তাব করি:

- **Riverpod + go_router + freezed**
- অথবা টিম যদি আগে থেকে Bloc জানে, তাহলে **Bloc + go_router**

---

## ৫.২ ব্যাকএন্ড

| অংশ | প্রস্তাব |
|---|---|
| ভাষা | Node.js / TypeScript বা Python / Go |
| Framework | NestJS বা FastAPI বা Gin |
| Database | PostgreSQL |
| Geo/Location | PostGIS |
| Cache | Redis |
| Queue | RabbitMQ বা Redis Streams বা Kafka |
| Realtime | WebSocket / MQTT / Ably / Socket.IO |
| Object Storage | AWS S3 / GCP Cloud Storage / Cloudflare R2 |
| Hosting Region | AWS Mumbai বা GCP Mumbai |
| API Docs | OpenAPI/Swagger |
| Auth | JWT + Refresh Token |
| Admin RBAC | Role-based access control |

প্রাথমিক পর্যায়ে খুব বড় মাইক্রোসার্ভিস না বানিয়ে **Modular Monolith** দিয়ে শুরু করা ভালো। পরে দরকার হলে আলাদা সার্ভিসে ভাগ করা যাবে।

---

# ৬. অ্যাপগুলা কী কী করবে?

---

## ৬.১ গ্রাহক/যাত্রী অ্যাপ

### মূল স্ক্রিন

1. Splash
2. Onboarding
3. Language Selection  
   - English  
   - Hindi  
   - Bengali  
   - প্রয়োজনে অন্য ভারতীয় ভাষা
4. Phone OTP Login
5. Home Map
6. Pickup Location Select
7. Destination Search
8. Ride Type Select
9. Fare Estimate
10. Promo/Member Benefit
11. Ride Confirmation
12. Searching Driver
13. Driver Assigned
14. Live Tracking
15. Driver Details
16. Call/Chat masked
17. Trip Started
18. Safety/SOS
19. Trip Completed
20. Payment
21. Rating
22. Trip History
23. Receipt/GST Invoice
24. Profile
25. Membership/Co-op Dashboard
26. Support
27. Emergency Contacts
28. Referral
29. Notifications

---

### যাত্রী অ্যাপের গুরুত্বপূর্ণ ফিচার

- Indian phone number validation: `+91` + 10 digits
- Google Places Autocomplete with India bias
- Saved addresses: Home, Work, Other
- Landmark support
- Live driver location
- Trip share link
- SOS button
- Emergency contact quick dial
- Ride OTP
- Ride start confirmation
- Cash payment
- UPI payment
- Card payment later
- Ride rating
- Report issue
- GST invoice download
- Cooperative member dashboard
- Language switch
- Low data mode

---

## ৬.২ ড্রাইভার অ্যাপ

### মূল স্ক্রিন

1. Driver Splash
2. Driver Onboarding
3. Phone OTP Login
4. KYC Document Upload
5. Vehicle Document Upload
6. Application Status
7. Training Module
8. Driver Profile
9. Online/Offline Toggle
10. Home Map
11. Ride Request Card
12. Accept/Reject
13. Pickup Navigation
14. Arrived
15. Passenger OTP Verify
16. Trip Started
17. Drop Navigation
18. Trip Completed
19. Payment Collection
20. Earnings Dashboard
21. Daily/Weekly Summary
22. Payout/Bank Details
23. Documents Expiry Alerts
24. Ratings
25. Support
26. Safety Incident
27. Cooperative Membership
28. Share/Dividend/Patronage Dashboard
29. Notifications

---

### ড্রাইভার অ্যাপের গুরুত্বপূর্ণ ফিচার

- Background location
- Foreground service notification on Android
- iOS background location permission
- Battery optimization handling
- Low network retry queue
- Ride request sound/vibration
- Accept/reject timer
- Route navigation
- Offline event queue
- Earnings breakdown
- Commission transparency
- Daily payout summary
- Document re-upload
- Document expiry alerts
- Driver availability heat map later
- Training videos
- Co-op membership
- SOS
- Panic/support call

---

## ৬.৩ মোবাইল অ্যাডমিন অ্যাপ

এটি মূলত অ্যাডমিন/অপারেশন টিমের মাঠপর্যায়ের অ্যাপ।

### মূল ফিচার

- অ্যাডমিন লগইন + 2FA
- Role-based access
- Live ride map
- Active drivers
- Ride requests
- SOS/incident alerts
- Ride details
- Driver details
- Rider details
- Verification queue
- Document review
- Support tickets
- Call rider/driver
- Cancel/reassign ride
- Refund initiation
- Broadcast notification
- Daily ops summary
- Safety incident workflow
- Audit log view

---

## ৬.৪ ওয়েব অ্যাডমিন প্যানেল

ওয়েব অ্যাডমিন হবে সবচেয়ে বড় অংশ।

### প্রধান মডিউল

1. Dashboard
2. Live Operations Map
3. Riders Management
4. Drivers Management
5. Vehicles Management
6. KYC Verification
7. Ride Management
8. Dispatch Monitoring
9. Pricing Configuration
10. Zone/Geo-fencing
11. Promo/Coupon Management
12. Payment & Settlements
13. Driver Payouts
14. Refunds
15. GST/Invoice Management
16. Support Tickets
17. Safety Incidents
18. Ratings & Reports
19. Cooperative Membership
20. Shares Ledger
21. Patronage/Dividend
22. Meetings & Voting
23. Finance Reports
24. Audit Logs
25. Role & Permission Management
26. Notification Center
27. App Configuration
28. Feature Flags
29. Analytics
30. Settings

---

# ৭. ভারত-নির্দিষ্ট গুরুত্বপূর্ণ বিষয়

ভারতে রাইডশেয়ারিং/কো-অপারেটিভ অ্যাপ বানাতে গেলে শুধু টেকনোলজি নয়, কমপ্লায়েন্স খুব গুরুত্বপূর্ণ।

---

## ৭.১ আইনি/প্রাতিষ্ঠানিক বিষয়

### সমবায় নিবন্ধন

ভারতে আপনি সাধারণত দুই ধরনের পথে ভাবতে পারেন:

1. **State Cooperative Society**  
   যদি প্রাথমিকভাবে একটি রাজ্যে কাজ করেন।

2. **Multi-State Cooperative Society**  
   যদি একাধিক রাজ্যে সদস্য/কার্যক্রম থাকে।  
   এটি **Multi-State Cooperative Societies Act, 2002** এর অধীনে আসে।

তবে বাস্তবে অনেক উদ্যোগ শুরুতে একটি রাজ্যে নিবন্ধন করে, পরে অন্য রাজ্যে সম্প্রসারণ করে।

### যা করা লাগতে পারে

- Cooperative Society registration
- Bye-laws
- Share capital rules
- Membership rules
- Voting rights
- Profit distribution
- Board election
- Audit
- Registrar compliance
- PAN/TAN
- GST registration
- Bank account
- MSME/Udyam registration, যদি প্রযোজ্য হয়
- Startup India recognition, যদি প্রযোজ্য হয়

---

## ৭.২ পরিবহন/রাইডশেয়ারিং কমপ্লায়েন্স

ভারতে রাজ্যভেদে নিয়ম আলাদা হতে পারে। সাধারণত যা যা দেখতে হবে:

- Motor Vehicles Act ও সংশ্লিষ্ট রাজ্য নিয়ম
- Aggregator guidelines, যদি প্রযোজ্য হয়
- Commercial vehicle permit
- Driver licence validity
- Transport badge/permit, যদি রাজ্যে লাগে
- Vehicle fitness certificate
- Insurance
- Pollution certificate/PUC
- Radio taxi/ride-sharing category, যদি প্রযোজ্য হয়
- State transport authority approval
- Police verification/background check, যদি প্রযোজ্য হয়

---

## ৭.৩ ডেটা প্রোটেকশন

ভারতে এখন ডেটা প্রাইভেসি খুব গুরুত্বপূর্ণ।

প্রযোজ্য আইন/নিয়ম:

- **Digital Personal Data Protection Act, 2023**
- IT Act
- SPDI Rules, যেখানে প্রযোজ্য
- TRAI/DoT নিয়ম, বিশেষ করে টেলিকম/SMS ক্ষেত্রে

আপনার অ্যাপে থাকতে হবে:

- স্পষ্ট সম্মতি
- ডেটা সংগ্রহের উদ্দেশ্য
- ডেটা মুছে ফেলার অধিকার
- ডেটা সংশোধনের অধিকার
- ডেটা ব্রিচ প্রক্রিয়া
- Consent withdrawal
- Data retention policy
- Third-party data sharing disclosure
- Child data policy
- Sensitive data protection

---

## ৭.৪ SMS OTP ও TRAI DLT

ভারতে বাণিজ্যিক/লেনদেনমূলক SMS পাঠাতে **DLT registration** লাগে।

আপনাকে করতে হবে:

1. DLT platform-এ entity registration
2. Header registration
3. Template registration
4. OTP template approval
5. Consent management
6. SMS provider integration

Provider উদাহরণ:

- MSG91
- Gupshup
- ValueFirst
- Kaleyra
- Twilio India, যদি প্রযোজ্য হয়

> শুধু টুইলিও বা অন্য প্রোভাইডার ব্যবহার করলেই হবে না; ভারতে OTP/promotional SMS-এর জন্য DLT নিয়ম মানতে হবে।

---

## ৭.৫ পেমেন্ট

ভারতে পেমেন্টের জন্য সাধারণত এই পদ্ধতিগুলো থাকবে:

### গ্রাহক পেমেন্ট

- Cash
- UPI
- Card
- Netbanking
- Wallet, যদি লাইসেন্সধারী wallet partner ব্যবহার করেন
- Corporate billing later

### প্রস্তাবিত পেমেন্ট গেটওয়ে

- Razorpay
- Cashfree
- Paytm Payment Gateway
- PhonePe Payment Gateway
- BillDesk

### ড্রাইভার payout

- Bank transfer
- UPI payout
- RazorpayX / Cashfree Payouts / Paytm Payouts
- NEFT/IMPS/UPI transfer
- TDS/commission accounting

### সতর্কতা

- নিজে বড় ধরনের ওয়ালেট বা stored value চালাতে গেলে RBI নিয়ম আসতে পারে
- শুরুতে লাইসেন্সধারী PSP বা নোডাল অ্যাকাউন্ট ব্যবহার করা ভালো
- ক্যাশ রাইডের জন্য আলাদা ledger রাখতে হবে
- GST invoice ও accountant workflow আগে থেকেই ঠিক করুন

---

## ৭.৬ KYC ও India Stack

ভারতে ড্রাইভার KYC-এর জন্য যা যা ব্যবহার করা যায়:

### ড্রাইভার KYC ডকুমেন্ট

- Driving Licence
- Vehicle RC
- Insurance
- Fitness certificate
- Permit, যদি প্রযোজ্য হয়
- PUC
- Address proof
- Photo
- PAN for payouts
- Bank account proof
- Police verification, যদি প্রযোজ্য হয়

### ডিজিটাল যাচাই

- Digilocker document fetch
- Driving Licence verification via authorized APIs
- RC verification via authorized APIs
- PAN verification
- Bank account penny-drop verification
- Aadhaar eKYC, শুধু অনুমোদিত ও আইনসম্মত পথে
- eSign for agreements

> Aadhaar ব্যবহার করলে খুব সতর্ক থাকতে হবে। Aadhaar ডেটা সংরক্ষণ, ব্যবহার ও গোপনীয়তার জন্য নির্দিষ্ট আইনি শর্ত আছে।

---

# ৮. প্রস্তাবিত ফ্রন্টএন্ড আর্কিটেকচার

## ৮.১ প্যাটার্ন

প্রস্তাবিত:

- **Clean Architecture**
- Feature-first modules
- Shared design system
- Role-based routing
- Responsive UI for mobile admin and web admin

Layers:

```text
presentation/
  - screens
  - widgets
  - controllers/viewmodels

domain/
  - entities
  - repositories interfaces
  - usecases

data/
  - api
  - local storage
  - repositories implementation
  - models
```

---

## ৮.২ State Management

প্রস্তাবিত:

### Riverpod

কারণ:

- ডিপেন্ডেন্সি ইনজেকশন সহজ
- টেস্টিং সহজ
- async state ভালো হ্যান্ডেল হয়
- modular architecture ভালো হয়

বিকল্প:

- Bloc
- Provider + ChangeNotifier, তবে বড় অ্যাপে Riverpod/Bloc ভালো

---

## ৮.৩ Navigation

প্রস্তাবিত:

- **go_router**

কারণ:

- deep link
- role-based redirect
- nested routes
- web URL handling
- declarative navigation

---

## ৮.৪ Responsive Layout

যেহেতু মোবাইল অ্যাডমিন ও ওয়েব অ্যাডমিন একই কোডবেসে থাকবে, আপনাকে responsive করতে হবে।

### উদাহরণ breakpoints

```dart
enum ScreenSize { mobile, tablet, desktop }
```

### Layout strategy

- Mobile admin: bottom navigation, compact cards, quick actions
- Tablet admin: split view
- Web admin: sidebar + data table + filters + bulk actions

---

# ৯. প্রয়োজনীয় Flutter প্যাকেজ

নিচে প্রস্তাবিত প্যাকেজের তালিকা দিলাম। সবগুলো একবারে ব্যবহার না করে প্রয়োজন অনুযায়ী নেবেন।

```yaml
dependencies:
  flutter:
    sdk: flutter

  # State & DI
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # Routing
  go_router: ^14.2.0

  # Networking
  dio: ^5.4.3
  retrofit: ^4.1.0

  # Models
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0

  # Local storage
  shared_preferences: ^2.2.3
  flutter_secure_storage: ^9.2.2

  # Maps & location
  google_maps_flutter: ^2.6.1
  geolocator: ^12.0.0
  permission_handler: ^11.3.1
  google_places_flutter: ^2.0.9

  # Background service
  flutter_background_service: ^5.0.7

  # Push
  firebase_core: ^2.31.0
  firebase_messaging: ^14.9.4

  # Notifications
  flutter_local_notifications: ^17.1.2

  # Connectivity
  connectivity_plus: ^6.0.3

  # Device/package info
  device_info_plus: ^10.1.0
  package_info_plus: ^8.0.0

  # UI
  flutter_svg: ^2.0.10
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  intl: ^0.19.0

  # Localization
  flutter_localizations:
    sdk: flutter

  # Webview
  webview_flutter: ^4.7.0

  # Error monitoring
  sentry_flutter: ^8.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
  build_runner: ^2.4.11
  freezed: ^2.5.2
  json_serializable: ^6.8.0
  retrofit_generator: ^8.1.0
  riverpod_generator: ^2.4.0
  melos: ^6.1.0
```

> ভার্সন সময়ের সঙ্গে বদলাবে। প্রজেক্ট শুরুর আগে সাম্প্রতিক স্থিতিশীল ভার্সন দেখে নিন।

---

# ১০. রাইড লাইফসাইকেল

একটি রাইডের অবস্থাগুলো পরিষ্কারভাবে ডিজাইন করতে হবে।

## ১০.১ রাইড স্টেট

```text
DRAFT
ESTIMATED
REQUESTED
SEARCHING
OFFERED_TO_DRIVER
DRIVER_ASSIGNED
DRIVER_EN_ROUTE
ARRIVED_AT_PICKUP
PASSENGER_BOARDED
ONGOING
NEAR_DROP
COMPLETED
PAYMENT_PENDING
PAID
RATED
CANCELLED
DISPUTED
SAFETY_INCIDENT
```

---

## ১০.২ স্টেট ট্রানজিশন উদাহরণ

```text
REQUESTED -> SEARCHING -> OFFERED_TO_DRIVER -> DRIVER_ASSIGNED
DRIVER_ASSIGNED -> DRIVER_EN_ROUTE -> ARRIVED_AT_PICKUP
ARRIVED_AT_PICKUP -> PASSENGER_BOARDED -> ONGOING
ONGOING -> NEAR_DROP -> COMPLETED -> PAYMENT_PENDING -> PAID -> RATED
```

বাতিল হলে:

```text
REQUESTED -> CANCELLED
SEARCHING -> CANCELLED
DRIVER_ASSIGNED -> CANCELLED
ONGOING -> SAFETY_INCIDENT / DISPUTED
```

---

# ১১. লোকেশন ও রিয়েলটাইম ট্র্যাকিং

## ১১.১ ড্রাইভার লোকেশন আপডেট

ড্রাইভার অ্যাপ যখন অনলাইন থাকবে:

- লোকেশন আপডেট ৩–৫ সেকেন্ডে একবার, চলমান অবস্থায়
- স্থির অবস্থায় ১০–১৫ সেকেন্ডে একবার
- ব্যাটারি ও ডেটা বাঁচাতে অ্যাডাপটিভ ইন্টারভ্যাল
- লোকেশন পাঠানোর সময়:
  - latitude
  - longitude
  - heading/bearing
  - speed
  - accuracy
  - timestamp
  - battery level
  - network type
  - app version
  - driver status

---

## ১১.২ লোকেশন প্যাকেট উদাহরণ

```json
{
  "event_id": "uuid",
  "driver_id": "drv_123",
  "ride_id": null,
  "type": "location_update",
  "lat": 22.5726,
  "lng": 88.3639,
  "heading": 92.4,
  "speed_mps": 6.2,
  "accuracy_m": 12.5,
  "battery": 78,
  "network": "4g",
  "app_version": "1.2.0",
  "ts": "2026-06-16T10:20:30+05:30"
}
```

---

## ১১.৩ Realtime টেকনোলজি

প্রস্তাব:

1. **WebSocket**  
2. অথবা **MQTT**  
3. অথবা managed service: Ably / Pusher / Supabase Realtime

প্রাথমিক MVP-তে:

- WebSocket + Redis pub/sub ভালো
- পরে scale হলে MQTT বা managed realtime service

---

## ১১.৪ Rider live tracking flow

```text
Driver App -> Backend Geo Service -> Redis -> WebSocket -> Rider App
```

### Rider subscription

```text
ride:{ride_id}:location
ride:{ride_id}:status
```

### Admin subscription

```text
city:{city_id}:live_rides
safety:incidents
driver:{driver_id}:location
```

---

# ১২. ম্যাপ ও রাউটিং

ভারতে শুরু করার জন্য সবচেয়ে বাস্তবসম্মত:

## প্রস্তাব: Google Maps Platform

ব্যবহার:

- `google_maps_flutter`
- Places API
- Geocoding API
- Directions API
- Distance Matrix API
- Roads API, যদি প্রয়োজন হয়

### কেন Google Maps?

- ভারতে ঠিকানা ও রুট ডেটা মোটামুটি ভালো
- ট্রাফিক-সহ ETA পাওয়া যায়
- ডেভেলপারদের জন্য সহজ
- ব্যবহারকারীরা অভ্যস্ত

### খরচ নিয়ন্ত্রণের টিপস

- Places Autocomplete-এ session token ব্যবহার করুন
- অপ্রয়োজনীয় Directions call কমাবেন
- Cache 자주 ব্যবহৃত রাউট, যদি সম্ভব হয়
- Fare estimate server-side করুন
- Map styling কম জটিল রাখুন
- Tile caching নীতি বুঝে ব্যবহার করুন

---

## বিকল্প/ভবিষ্যৎ

- OpenStreetMap + MapLibre
- OSRM / Valhalla / GraphHopper
- নিজস্ব ঠিকানা/ল্যান্ডমার্ক ডেটা

তবে প্রাথমিক ভারত পাইলটে Google Maps ব্যবহার করাই সহজ হবে।

---

# ১৩. Authentication ও নিরাপত্তা

## ১৩.১ লগইন

প্রস্তাবিত:

- Phone number + OTP
- JWT access token
- Refresh token
- Device binding
- Role-based access

### Token flow

```text
OTP request -> OTP verify -> access + refresh token
Access token: short-lived, 10–20 min
Refresh token: secure storage, rotation enabled
```

---

## ১৩.২ Role

```text
RIDER
DRIVER
ADMIN
SUPPORT
FINANCE
SAFETY
AUDITOR
BOARD_MEMBER
SUPER_ADMIN
```

---

## ১৩.৩ Driver app security

- Device binding
- Login change alert
- Suspicious device detection
- Background location permission justification
- Ride OTP
- Fraud detection
- Fake GPS detection
- Document verification
- Session timeout for admin

---

## ১৩.৪ Admin security

- 2FA mandatory
- IP allowlist, সম্ভব হলে
- Session activity log
- Role permission matrix
- Audit log
- Sensitive action confirmation
- Export watermarking, যদি প্রয়োজন হয়

---

# ১৪. ভারতীয় পেমেন্ট ফ্লো

## ১৪.১ গ্রাহক পেমেন্ট পদ্ধতি

### প্রাথমিক

1. Cash
2. UPI

### পরে

3. Card
4. Netbanking
5. Corporate wallet/invoice

---

## ১৪.২ UPI পেমেন্ট অপশন

আপনি কয়েকটি পথ ব্যবহার করতে পারেন:

1. Payment Gateway Checkout  
   - Razorpay/Cashfree/Paytm
2. UPI Collect request
3. UPI Intent on Android
4. UPI QR
5. UPI link/web fallback iOS-এর জন্য

### iOS সতর্কতা

iOS-এ UPI intent সবসময় সরাসরি সহজ নয়। তাই:

- Payment gateway webview/redirect
- UPI link
- QR display
- বা অন্য পেমেন্ট পদ্ধতি

---

## ১৪.৩ ক্যাশ রাইড লেজার

ভারতে ক্যাশ খুব গুরুত্বপূর্ণ হবে।

উদাহরণ:

| ঘটনা | হিসাব |
|---|---|
| যাত্রী ক্যাশে ₹150 দিল | ড্রাইভার ক্যাশ কালেকশন ₹150 |
| প্ল্যাটফর্ম কমিশন 10% | প্ল্যাটফর্ম আয় ₹15 |
| ড্রাইভার নেট আয় | ₹135 |
| ড্রাইভারের কাছে ক্যাশ আছে | প্ল্যাটফর্ম পাওনা/ড্রাইভার পাওনা লেজার |

প্রতিটি রাইডের জন্য ডাবল-এন্ট্রি ধাঁচের লেজার রাখা ভালো।

---

## ১৪.৪ GST Invoice

প্রতিটি রাইড/সার্ভিসে প্রয়োজন হলে:

- Invoice number
- Date
- Customer name, যদি দেয়
- GSTIN of cooperative/company
- SAC code
- Taxable value
- GST amount
- Total amount
- Ride ID
- Service period
- Payment method

> GST/SAC/ট্যাক্স নীতি চূড়ান্ত করার আগে একজন ভালো CA নিন।

---

# ১৫. ডেটাবেস ব্লুপ্রিন্ট

নিচে মূল টেবিলগুলো দিলাম।

---

## ১৫.১ ব্যবহারকারী

```text
users
- id
- phone_country_code
- phone_number
- email
- full_name
- role
- status
- preferred_language
- created_at
- updated_at
```

---

## ১৫.২ রাইডার প্রোফাইল

```text
rider_profiles
- user_id
- emergency_contact_name
- emergency_contact_phone
- default_payment_method
- is_member
- membership_id
- rating
- total_rides
```

---

## ১৫.৩ ড্রাইভার প্রোফাইল

```text
driver_profiles
- user_id
- licence_number_masked
- licence_expiry
- verification_status
- on_duty_status
- current_lat
- current_lng
- last_location_at
- rating
- acceptance_rate
- total_rides
- member_id
```

---

## ১৫.৪ ভেহিকেল

```text
vehicles
- id
- driver_id
- vehicle_type
- make
- model
- year
- color
- registration_number
- registration_expiry
- insurance_expiry
- fitness_expiry
- permit_expiry
- puc_expiry
- status
```

---

## ১৫.৫ KYC ডকুমেন্ট

```text
kyc_documents
- id
- user_id
- doc_type
- doc_number_masked
- issuer
- issue_date
- expiry_date
- file_url
- thumbnail_url
- status
- rejection_reason
- verified_by
- verified_at
- verification_provider_ref
```

Doc types:

```text
DRIVING_LICENCE
VEHICLE_RC
INSURANCE
FITNESS_CERTIFICATE
PERMIT
PUC
ADDRESS_PROOF
PAN
BANK_PROOF
PHOTO
POLICE_VERIFICATION
```

---

## ১৫.৬ রাইড

```text
ride_requests
- id
- rider_id
- pickup_lat
- pickup_lng
- pickup_address
- pickup_landmark
- drop_lat
- drop_lng
- drop_address
- drop_landmark
- ride_type
- fare_estimate_min
- fare_estimate_max
- status
- created_at

rides
- id
- request_id
- rider_id
- driver_id
- vehicle_id
- city_id
- zone_id
- status
- fare_amount
- gst_amount
- platform_fee
- driver_net_amount
- payment_method
- payment_status
- distance_km
- duration_min
- pickup_at
- started_at
- completed_at
- cancelled_by
- cancellation_reason
- rating_by_rider
- rating_by_driver
- safety_flags
- created_at
```

---

## ১৫.৭ লোকেশন ইভেন্ট

```text
ride_location_events
- id
- ride_id
- driver_id
- lat
- lng
- heading
- speed_mps
- accuracy_m
- battery
- network
- event_at
```

বড় ডেটা হলে পার্টিশন করা ভালো।

---

## ১৫.৮ পেমেন্ট

```text
payments
- id
- ride_id
- payer_user_id
- amount
- currency
- method
- status
- gateway_provider
- gateway_payment_id
- gateway_signature
- created_at
- updated_at

ledger_entries
- id
- reference_type
- reference_id
- account_id
- debit
- credit
- balance_after
- created_at
- created_by

driver_payouts
- id
- driver_id
- period_start
- period_end
- gross_amount
- platform_fee
- adjustments
- tds_amount
- net_amount
- status
- payment_reference
- paid_at
```

---

## ১৫.৯ সমবায় সদস্য

```text
members
- id
- user_id
- member_number
- member_type
- status
- joined_at
- share_capital
- kyc_status

shares
- id
- member_id
- share_amount
- issued_at
- status

share_transactions
- id
- member_id
- type
- amount
- payment_reference
- created_at

patronage_ledger
- id
- member_id
- period
- rides_completed
- fare_generated
- patronage_points
- payout_amount
- status

general_meetings
- id
- title
- agenda
- starts_at
- ends_at

motions
- id
- meeting_id
- title
- description

votes
- id
- motion_id
- member_id
- choice
- cast_at
```

---

## ১৫.১০ নিরাপত্তা/সাপোর্ট

```text
incidents
- id
- ride_id
- reported_by_user_id
- type
- description
- evidence_urls
- status
- assigned_to
- resolved_at

support_tickets
- id
- user_id
- ride_id
- category
- subject
- description
- status
- priority
- created_at

notifications
- id
- user_id
- title
- body
- type
- action_url
- read_at
```

---

# ১৬. API ব্লুপ্রিন্ট

## ১৬.১ Auth

```text
POST /v1/auth/otp/request
POST /v1/auth/otp/verify
POST /v1/auth/refresh
POST /v1/auth/logout
GET  /v1/auth/me
```

---

## ১৬.২ Rider

```text
GET  /v1/rider/profile
PUT  /v1/rider/profile
GET  /v1/rider/saved-addresses
POST /v1/rider/saved-addresses
PUT  /v1/rider/saved-addresses/{id}
DELETE /v1/rider/saved-addresses/{id}

POST /v1/rides/estimate
POST /v1/rides
GET  /v1/rides/{id}
POST /v1/rides/{id}/cancel
POST /v1/rides/{id}/rating
POST /v1/rides/{id}/share-link
POST /v1/rides/{id}/sos
```

---

## ১৬.৩ Driver

```text
POST /v1/driver/onboard
POST /v1/driver/documents
GET  /v1/driver/documents
POST /v1/driver/online
POST /v1/driver/offline
POST /v1/driver/location/batch

GET  /v1/driver/ride-requests/current
POST /v1/driver/rides/{id}/accept
POST /v1/driver/rides/{id}/reject
POST /v1/driver/rides/{id}/arrived
POST /v1/driver/rides/{id}/start
POST /v1/driver/rides/{id}/complete
POST /v1/driver/rides/{id}/payment-confirm

GET  /v1/driver/earnings/summary
GET  /v1/driver/payouts
```

---

## ১৬.৪ Payment

```text
POST /v1/payments/initiate
GET  /v1/payments/{id}
POST /v1/payments/webhook/razorpay
POST /v1/payments/webhook/cashfree
POST /v1/payments/refund
```

---

## ১৬.৫ KYC

```text
POST /v1/kyc/documents
GET  /v1/kyc/documents/{id}
POST /v1/kyc/documents/{id}/resubmit
```

Admin side:

```text
GET  /v1/admin/kyc/pending
POST /v1/admin/kyc/{id}/approve
POST /v1/admin/kyc/{id}/reject
```

---

## ১৬.৬ Admin

```text
GET  /v1/admin/dashboard
GET  /v1/admin/live-rides
GET  /v1/admin/rides
GET  /v1/admin/rides/{id}
POST /v1/admin/rides/{id}/cancel
POST /v1/admin/rides/{id}/reassign

GET  /v1/admin/drivers
GET  /v1/admin/drivers/{id}
POST /v1/admin/drivers/{id}/suspend
POST /v1/admin/drivers/{id}/activate

GET  /v1/admin/riders
GET  /v1/admin/riders/{id}

GET  /v1/admin/incidents
POST /v1/admin/incidents/{id}/assign
POST /v1/admin/incidents/{id}/resolve

GET  /v1/admin/finance/summary
GET  /v1/admin/payouts
POST /v1/admin/payouts/process
```

---

## ১৬.৭ Cooperative

```text
POST /v1/coop/join
GET  /v1/coop/member/profile
POST /v1/coop/shares/purchase
GET  /v1/coop/shares/transactions
GET  /v1/coop/patronage
GET  /v1/coop/meetings
POST /v1/coop/meetings/{id}/vote
```

---

# ১৭. Ride Matching/Dispatch লজিক

## ১৭.১ প্রাথমিক অ্যালগরিদম

```text
1. রাইড রিকোয়েস্ট আসবে
2. সার্ভার pickup location থেকে nearby active drivers খুঁজবে
3. ড্রাইভার ফিল্টার:
   - online
   - verified
   - vehicle type match
   - not on another ride
   - within radius
   - sufficient rating
   - no active safety block
4. Scoring করবে
5. সেরা ড্রাইভারকে অফার পাঠাবে
6. টাইমআউট হলে পরের ড্রাইভার
7. ৩–৫ বার চেষ্টা করে না পেলে রাইডারকে জানাবে
```

---

## ১৭.২ Scoring উদাহরণ

```text
score =
  weight_distance × normalized_distance
+ weight_eta × normalized_eta
+ weight_rating × driver_rating
+ weight_acceptance × acceptance_rate
+ weight_idle × idle_time_bonus
- weight_cancel × recent_cancellations
```

কো-অপারেটিভ হিসেবে শুধু কাছের ড্রাইভার নয়, ন্যায্য বণ্টনও বিবেচনা করুন।

---

## ১৭.৩ ডিসপ্যাচ সার্ভিস ডেটা

Redis-এ:

```text
driver:{driver_id}:location
driver:{driver_id}:status
city:{city_id}:drivers_geo_set
ride:{ride_id}:status
ride:{ride_id}:driver_assignment
```

PostGIS-এ:

```text
driver current location
geofences
zones
city boundaries
blackspots
```

---

# ১৮. Fare/প্রাইসিং মডেল

## ১৮.১ ভারতীয় প্রেক্ষাপটে ফেয়ার ফর্মুলা

```text
fare =
  base_fare
+ per_km × distance_km
+ per_minute × duration_min
+ night_surcharge
+ toll_charges
+ platform_fee
+ gst
```

অথবা যদি যাত্রীকে সব মিলিয়ে একটা ভাড়া দেখান:

```text
customer_payable =
  base_fare
+ distance_charge
+ time_charge
+ night_surcharge
+ toll
+ gst
```

এবং ড্রাইভারের আয়:

```text
driver_net =
  customer_payable
- gst
- platform_fee
- payment_gateway_fee
- other_adjustments
```

---

## ১৮.২ Pricing config উদাহরণ

```json
{
  "city_id": "kol",
  "ride_type": "bike",
  "currency": "INR",
  "base_fare": 25,
  "per_km": 10,
  "per_minute": 1.5,
  "minimum_fare": 40,
  "cancellation_fee": 10,
  "night_start": "22:00",
  "night_end": "05:00",
  "night_multiplier": 1.1,
  "platform_fee_percentage": 10,
  "gst_percentage": 5,
  "toll_policy": "actuals",
  "max_surge_multiplier": 1.3
}
```

> GST হার ও প্রযোজ্যতা পরিষেবার ধরন অনুযায়ী বদলাতে পারে। CA দিয়ে ঠিক করাবেন।

---

# ১৯. নিরাপত্তা ফিচার

## ১৯.১ যাত্রী নিরাপত্তা

- SOS button
- Emergency contacts
- Live trip share
- Ride OTP
- Driver details
- Vehicle details
- Call masking
- Chat masking
- Route deviation alert
- Long stop alert
- Incident report
- Women safety mode, যদি প্রযোজ্য হয়
- Quick dial 112, যদি সম্ভব হয়

---

## ১৯.২ ড্রাইভার নিরাপত্তা

- SOS
- Support hotline
- False complaint appeal
- Incident evidence upload
- Cash safety guidance
- Fatigue alert
- Night ride safety checklist

---

## ১৯.৩ অ্যাডমিন সেফটি ড্যাশবোর্ড

- Live SOS feed
- Incident priority
- Call rider/driver
- Track ride live
- Assign safety officer
- Mark resolved
- Export incident report
- Audit trail

---

# ২০. Admin Web Panel-এর বিস্তারিত মডিউল

## ২০.১ Dashboard

- Today rides
- Active drivers
- Online riders
- Revenue
- Completed rides
- Cancellation rate
- SOS alerts
- Verification pending
- Support tickets pending

---

## ২০.২ Live Ops Map

- সব সক্রিয় রাইড
- ড্রাইভার লোকেশন
- রাইড ফিল্টার
- রাইড সিলেক্ট করে বিস্তারিত
- SOS হাইলাইট
- Zone overlay
- Heatmap later

---

## ২০.৩ Driver Management

- Pending KYC
- Approved drivers
- Suspended drivers
- Document expiry
- Rating
- Earnings
- Payout history
- Ride history
- Device/session info

---

## ২০.৴ Finance

- Ride ledger
- Cash collection
- Gateway settlements
- Refunds
- Driver payouts
- GST reports
- Commission reports
- Cooperative patronage report

---

## ২০.৫ Cooperative Management

- Member list
- Share ledger
- Share purchase
- Share transfer/refund
- Patronage calculation
- Dividend
- Meeting management
- Voting
- Resolution records

---

## ২০.৬ Settings

- City config
- Ride type config
- Pricing config
- Commission config
- Cancellation policy
- Promo config
- Notification templates
- Feature flags
- Roles/permissions
- Audit logs

---

# ২১. UI/UX নীতি

## ২১.১ ভাষা

প্রাথমিক:

- বাংলা
- হিন্দি
- ইংরেজি

পরে:

- ওড়িয়া
- তামিল
- তেলুগু
- কন্নড়
- মারাঠি
- ইত্যাদি

---

## ২১.২ ভারতীয় ফরম্যাট

- মুদ্রা: ₹
- তারিখ: ভারতীয় সময়, IST
- ফোন: +91
- ঠিকানা: locality, landmark, pin code
- GST invoice format
- Language switch সহজ জায়গায়

---

## ২১.৩ অ্যাক্সেসিবিলিটি

- বড় টেক্সট
- উচ্চ কনট্রাস্ট
- স্ক্রিন রিডার সাপোর্ট
- বাটন ট্যাপ এরিয়া বড়
- ড্রাইভার অ্যাপে এক হাতে ব্যবহারযোগ্য ডিজাইন
- কম আলো/রাতের মোড

---

## ২১.৪ লো-এন্ড ডিভাইস অপ্টিমাইজেশন

ভারতে অনেক ড্রাইভার লো-এন্ড/মিড-রেঞ্জ অ্যান্ড্রয়েড ব্যবহার করতে পারেন।

- অ্যাপ সাইজ কমান
- অ্যানিমেশন কমান
- ইমেজ কমপ্রেস করুন
- লোকেশন পোলিং অপ্টিমাইজ করুন
- অফলাইন কুই রাখুন
- দুর্বল নেটওয়ার্কে রিট্রাই করুন
- ব্যাটারি ড্রেইন কমান

---

# ২২. অফলাইন ও দুর্বল নেটওয়ার্ক হ্যান্ডলিং

## ২২.১ ড্রাইভার অ্যাপে

- লোকেশন ইভেন্ট লোকালি কুই করুন
- রাইড স্ট্যাটাস পরিবর্তন কুই করুন
- ইন্টারনেট ফিরলে ব্যাচ আপলোড
- প্রতিটি ইভেন্টে unique event_id দিন, যাতে ডুপ্লিকেট না হয়
- Conflict resolution server-side করুন

---

## ২২.২ রাইডার অ্যাপে

- রাইড স্ট্যাটাস পোলিং + WebSocket
- WebSocket না হলে polling fallback
- Trip history local cache
- Receipt local cache

---

# ২৩. Push Notification

## প্রয়োজনীয় নোটিফিকেশন

### রাইডার

- Driver assigned
- Driver arriving
- Driver arrived
- Trip started
- Trip completed
- Payment reminder
- Safety alert
- Support reply
- Membership update

### ড্রাইভার

- New ride request
- Ride reminder
- Document expiry
- Payout processed
- Verification update
- Safety alert
- Training update

### অ্যাডমিন

- SOS alert
- High risk ride
- Verification pending
- Settlement failure
- System alert

---

## টেকনোলজি

- Android: FCM
- iOS: APNs via FCM
- Admin web: Web push later, বা in-app alert

---

# ২৪. Analytics ও Monitoring

## অ্যাপ অ্যানালিটিক্স

- Firebase Analytics
- PostHog
- Mixpanel
- Amplitude

প্রস্তাব: শুরুতে **Firebase + Sentry**। পরে product analytics যোগ করা যাবে।

## মনিটরিং

- Sentry for crash
- Backend logs: Grafana Loki / ELK
- Metrics: Prometheus/Grafana
- Uptime monitoring
- Error alerting
- API latency monitoring
- Ride matching failure alert
- Payment webhook failure alert

---

# ২৫. টেস্টিং পরিকল্পনা

## ২৫.১ ইউনিট টেস্ট

- fare calculation
- cancellation policy
- ledger entries
- auth token handling
- validation
- state transitions

## ২৫.২ Widget tests

- buttons
- forms
- ride card
- OTP screen
- admin tables

## ২৫.৩ Integration tests

- login flow
- ride request flow
- driver accept flow
- payment flow
- KYC upload flow

## ২৫.৪ E2E tests

- Patrol বা Appium
- critical ride lifecycle
- background location sanity
- push notification smoke test

## ২৫.৫ Field testing

ভারতে বাস্তব মাঠ পরীক্ষা খুব জরুরি:

- কলকাতা/লক্ষ্মৌ/জয়পুর/তিরুবনন্তপুরম নয়, বরং আপনার লক্ষ্য শহরে
- বিভিন্ন অ্যান্ড্রয়েড ডিভাইস
- কম নেটওয়ার্ক
- বৃষ্টি/রাতে
- GPS accuracy
- battery drain
- UPI payment timeout
- SMS OTP delay
- map route deviation

---

# ২৬. Android ও iOS কনফিগারেশন

## ২৬.১ Android

### প্রয়োজনীয় পারমিশন

```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
```

### গুরুত্বপূর্ণ

- targetSdk সর্বশেষ রাখুন
- background location declaration স্পষ্ট করুন
- Play Console data safety form সঠিকভাবে পূরণ করুন
- UPI/payment external checkout policy বুঝুন
- location background use justification দিন

---

## ২৬.২ iOS

`Info.plist`-এ reason strings আবশ্যক:

```text
NSLocationWhenInUseUsageDescription
NSLocationAlwaysAndWhenInUseUsageDescription
NSCameraUsageDescription
NSPhotoLibraryUsageDescription
NSUserNotificationsUsageDescription
```

Background modes:

```text
location
fetch
remote-notification
```

### সতর্কতা

- App Store background location justification কঠিন হতে পারে
- UPI payment flow iOS-এ carefully handle করুন
- OTP autofill support রাখুন
- Apple privacy nutrition label সঠিকভাবে দিন

---

# ২৭. ওয়েব অ্যাডমিনের জন্য বিশেষ নির্দেশনা

Flutter Web ব্যবহার করলে কিছু বিষয় খেয়াল রাখবেন:

## সুবিধা

- একই কোডবেস
- একই ডিজাইন সিস্টেম
- দ্রুত ডেভেলপমেন্ট

## সীমাবদ্ধতা

- প্রাথমিক লোড টাইম বেশি হতে পারে
- জটিল ডেটা টেবিলে পারফরম্যান্স অপ্টিমাইজ করতে হতে পারে
- SEO সাধারণত অ্যাডমিন প্যানেলের জন্য দরকার হয় না
- Accessibility ভালোভাবে টেস্ট করতে হবে

## সমাধান

- Lazy load modules
- Code splitting
- CanvasKit optimization
- Server hosting CDN দিয়ে
- বড় টেবিলে pagination
- Virtualized list
- Export async রাখুন

---

# ২৮. প্রস্তাবিত MVP Scope

সবকিছু একবারে বানাবেন না। প্রথম সংস্করণে এগুলো রাখুন।

## ২৮.১ যাত্রী অ্যাপ

- বাংলা/হিন্দি/ইংরেজি
- ফোন OTP লগইন
- ম্যাপে পিকআপ ও ড্রপ
- ফেয়ার এস্টিমেট
- রাইড বুকিং
- ড্রাইভার অ্যাসাইনমেন্ট
- লাইভ ট্র্যাকিং
- রাইড শেয়ার
- ক্যাশ পেমেন্ট
- রেটিং
- ট্রিপ হিস্ট্রি
- প্রোফাইল
- সাপোর্ট
- SOS

## ২৮.২ ড্রাইভার অ্যাপ

- ফোন লগইন
- ডকুমেন্ট আপলোড
- অনলাইন/অফলাইন
- রাইড অফার
- রাইড গ্রহণ
- নেভিগেশন
- রাইড সম্পন্ন
- আয়ের সারাংশ
- ক্যাশ হিসাব
- নোটিফিকেশন
- সাপোর্ট
- ডকুমেন্ট স্ট্যাটাস

## ২৮.৩ অ্যাডমিন

- লগইন + 2FA
- রাইড মনিটরিং
- ড্রাইভার তালিকা
- রাইডার তালিকা
- ডকুমেন্ট approval/rejection
- রাইড বাতিল/পুনরায় অ্যাসাইন
- বেসিক রিপোর্ট
- সেফটি ইনসিডেন্ট তালিকা

## ২৮.৪ ওয়েব অ্যাডমিন

- ড্যাশবোর্ড
- লাইভ ম্যাপ
- ইউজার/ড্রাইভার ম্যানেজমেন্ট
- রাইড ম্যানেজমেন্ট
- ডকুমেন্ট ভেরিফিকেশন
- প্রাইসিং কনফিগ
- ফাইন্যান্স বেসিক
- সাপোর্ট টিকেট
- অডিট লগ

## ২৮.৫ পরে যোগ করবেন

- পূর্ণ কো-অপ ভোট
- প্যাট্রোনেজ অটোমেশন
- শেয়ারড রাইড
- মাল্টি-সিটি
- অ্যাডভান্সড ডাইনামিক প্রাইসিং
- কর্পোরেট অ্যাকাউন্ট
- ডেলিভারি
- রিওয়ার্ড/রেফারেল
- ড্রাইভার লিডারবোর্ড
- অ্যাডভান্সড অ্যানালিটিক্স

---

# ২৯. ডেভেলপমেন্ট রোডম্যাপ

## ফেজ ১: আবিষ্কার ও ডিজাইন

সময়: ৩–৫ সপ্তাহ

কাজ:

- ভারতীয় আইনি কাঠামো বোঝা
- সমবায় নিবন্ধন প্রক্রিয়া শুরু
- লক্ষ্য শহর নির্বাচন
- ড্রাইভার ও যাত্রী সাক্ষাৎকার
- ফেয়ার মডেল
- কমিশন মডেল
- ইউজার ফ্লো
- ওয়্যারফ্রেম
- ডিজাইন সিস্টেম
- প্রযুক্তি চূড়ান্তকরণ
- ডেটাবেস স্কিমা
- এপিআই কন্ট্রাক্ট

---

## ফেজ ২: কোর প্ল্যাটফর্ম

সময়: ৬–৮ সপ্তাহ

কাজ:

- অথেনটিকেশন
- প্রোফাইল
- ম্যাপ ইন্টিগ্রেশন
- লোকেশন পারমিশন
- রাইড রিকোয়েস্ট
- ড্রাইভার অনলাইন স্ট্যাটাস
- ড্রাইভার লোকেশন আপডেট
- অ্যাডমিন লগইন
- বেসিক ড্যাশবোর্ড

---

## ফেজ ৩: রাইড ম্যাচিং ও ট্রিপ

সময়: ৪–৬ সপ্তাহ

কাজ:

- ডিসপ্যাচ ইঞ্জিন
- রাইড অফার
- অ্যাকসেপ্ট/রিজেক্ট
- ট্রিপ স্টেট
- লাইভ ট্র্যাকিং
- ফেয়ার ক্যালকুলেশন
- ক্যাশ লেজার
- রেটিং

---

## ফেজ ৪: পেমেন্ট, KYC, সেফটি

সময়: ৪–৬ সপ্তাহ

কাজ:

- KYC আপলোড
- অ্যাডমিন ভেরিফিকেশন
- UPI পেমেন্ট
- পেমেন্ট ওয়েবহুক
- রসিদ
- সেফটি ফিচার
- সাপোর্ট টিকেট
- নোটিফিকেশন
- অডিট লগ

---

## ফেজ ৫: পাইলট ও স্থিতিশীলকরণ

সময়: ৪–৮ সপ্তাহ

কাজ:

- বন্ধ বীটা
- ২০–৫০ ড্রাইভার
- ৩০০–১০০০ যাত্রী
- নির্দিষ্ট এলাকা
- ফিল্ড সাপোর্ট
- বাগ ফিক্স
- পারফরম্যান্স অপ্টিমাইজেশন
- ব্যাটারি অপ্টিমাইজেশন
- নেটওয়ার্ক টেস্ট

---

# ৩০. টিম

প্রাথমিক টিম:

| ভূমিকা | সংখ্যা | দায়িত্ব |
|---|---:|---|
| Product Manager | ১ | রোডম্যাপ, সমন্বয় |
| Flutter Developer | ২–৩ | অ্যাপ ডেভেলপমেন্ট |
| Backend Developer | ২ | API, dispatch, ledger |
| UI/UX Designer | ১ | ওয়্যারফ্রেম, ডিজাইন সিস্টেম |
| QA Engineer | ১ | টেস্টিং |
| DevOps | ১, আংশিক | CI/CD, cloud |
| Operations Lead | ১ | ড্রাইভার, ফিল্ড |
| Legal/Compliance Advisor | বাইরে | সমবায়, পরিবহন, ডেটা |
| CA/Accountant | বাইরে | GST, ledger, payout |
| Community/Co-op Manager | ১ | সদস্যপদ, গভর্নেন্স |

---

# ৩১. আনুমানিক খরচ, ভারতে

খরচ অনেক বিষয়ের উপর নির্ভর করবে। নিচেরটি একটি বাস্তবসম্মত ধারণা।

## ৩১.১ প্রাথমিক ডেভেলপমেন্ট খরচ

| অংশ | আনুমানিক খরচ |
|---|---:|
| আবিষ্কার, ডিজাইন, আইনি প্রাথমিক পরামর্শ | ₹১,০০,০০০ – ₹৫,০০,০০০ |
| UI/UX | ₹১,৫০,০০০ – ₹৫,০০,০০০ |
| Flutter MVP | ₹১২,০০,০০০ – ₹৪০,০০,০০০ |
| Backend MVP | ₹৮,০০,০০০ – ₹৩০,০০,০০০ |
| Admin panel | ₹৫,০০,০০০ – ₹১৫,০০,০০০ |
| QA ও টেস্টিং | ₹২,০০,০০০ – ₹৮,০০,০০০ |
| Pilot operations | ₹৩,০০,০০০ – ₹১০,০০,০০০ |
| Marketing | ₹১,০০,০০০ – ₹৮,০০,০০০ |

মোটামুটি একটি সিরিয়াস ভারতীয় পাইলটের জন্য প্রথম বছরে **₹৩০ লক্ষ থেকে ₹১ কোটি+** পর্যন্ত যেতে পারে, যদি পূর্ণাঙ্গ পেশাদার টিম নেন। তবে ছোট টিম, ওপেন-সোর্স, সীমিত স্কোপে খরচ কমানো সম্ভব।

---

## ৩১.২ মাসিক চলমান খরচ

| খাত | আনুমানিক |
|---|---:|
| ক্লাউড | ₹১০,০০০ – ₹১,০০,০০০ |
| Google Maps API | ₹২০,০০০ – ₹২,০০,০০০+ |
| SMS/OTP | রাইড ভলিউম অনুযায়ী |
| পেমেন্ট গেটওয়ে ফি | লেনদেনের শতাংশ |
| মনিটরিং | ₹৫,০০০ – ₹৫০,০০০ |
| সাপোর্ট/অপারেশন | টিম অনুযায়ী |

Google Maps খরচ দ্রুত বাড়তে পারে, তাই শুরু থেকেই অপ্টিমাইজেশন জরুরি।

---

# ৩২. ঝুঁকি ও সমাধান

| ঝুঁকি | সমাধান |
|---|---|
| ড্রাইভার যোগ না হওয়া | এক এলাকায় পাইলট, ইনসেনটিভ, স্বচ্ছ আয় |
| যাত্রী না পাওয়া | লোকাল কমিউনিটি, অফিস/কলেজ টার্গেট, রেফারেল |
| ব্যাকগ্রাউন্ড লোকেশন সমস্যা | native testing, foreground service, battery optimization |
| Google Maps খরচ | caching, session tokens, estimate optimization |
| OTP delay | DLT approved provider, fallback voice OTP |
| পেমেন্ট ব্যর্থতা | retry, manual reconciliation, multiple PSP fallback |
| আইনি জটিলতা | আগে আইনজীবী/রেজিস্ট্রার উপদেষ্টা |
| সমবায় বিরোধ | স্বচ্ছ বাইলজ, নিয়মিত মিটিং, অডিট |
| নিরাপত্তা ঘটনা | SOS, live tracking, fast response team |
| ডেটা প্রাইভেসি | DPDP compliance, consent, minimal data |

---

# ৩৩. প্রকাশনার জন্য চেকলিস্ট

## Android

- Play Console account
- App signing
- Data safety form
- Background location declaration
- Target SDK compliance
- Internal testing track
- Closed testing
- Production rollout
- Crashlytics/Sentry
- ANR/crash threshold monitoring

---

## iOS

- Apple Developer account
- App Store Connect
- Privacy nutrition labels
- Location permission strings
- Background modes justification
- TestFlight beta
- App review notes
- Payment flow review

---

## Web Admin

- Hosting
- HTTPS
- Authentication
- 2FA
- Rate limiting
- Admin IP restriction, সম্ভব হলে
- Error monitoring
- Backup
- Audit logs

---

# ৩৪. প্রস্তাবিত প্রথম কারিগরি সিদ্ধান্ত

যদি আপনি দ্রুত শুরু করতে চান, তাহলে এই স্ট্যাক দিয়ে শুরু করতে পারেন:

| স্তর | প্রস্তাব |
|---|---|
| Mobile + Web | Flutter |
| State | Riverpod |
| Routing | go_router |
| Backend | NestJS + PostgreSQL + PostGIS + Redis |
| Realtime | WebSocket + Redis Pub/Sub |
| Maps | Google Maps Platform |
| SMS | MSG91/Gupshup with DLT |
| Payments | Razorpay বা Cashfree |
| Push | Firebase Cloud Messaging |
| Crash | Sentry |
| Cloud | AWS Mumbai বা GCP Mumbai |
| Storage | S3/GCS |
| CI/CD | GitHub Actions + Fastlane |
| Monorepo | Melos |

---

# ৩৫. সবচেয়ে গুরুত্বপূর্ণ পরামর্শ

1. **একই কোডবেস ঠিক আছে, কিন্তু একই অ্যাপ বাইনারি নয়।**  
   একই রিপো থেকে আলাদা অ্যাপ বিল্ড করুন।

2. **ড্রাইভার অ্যাপে ব্যাকগ্রাউন্ড লোকেশন সবচেয়ে কঠিন অংশ।**  
   প্রথম সপ্তাহ থেকেই রিয়েল ডিভাইসে পরীক্ষা শুরু করুন।

3. **ভারতে DLT SMS, UPI পেমেন্ট, KYC ও পরিবহন নিয়ম আগে ঠিক করুন।**

4. **Google Maps খরচ নিয়ন্ত্রণ করুন।**

5. **ক্যাশ রাইডের জন্য শক্তিশালী লেজার রাখুন।**

6. **সমবায় অংশটিকে শুধু “পরে যোগ করব” ভাববেন না।**  
   শুরু থেকেই সদস্যপদ, শেয়ার লেজার ও গভর্নেন্স ডিজাইন করুন।

7. **একটি ছোট শহর/এলাকায় পাইলট করুন।**  
   একবারে বড় শহরজুড়ে যাবেন না।

8. **নিরাপত্তা ফিচার MVP-তেই রাখুন।**  
   পরে যোগ করলে ঝুঁকি বাড়ে।

---

# ৩৬. আপনার জন্য পরবর্তী ৩০ দিনের অ্যাকশন প্ল্যান

### সপ্তাহ ১

- সমবায় নিবন্ধন সম্পর্কে আইনজীবী/উপদেষ্টার সঙ্গে কথা বলুন
- লক্ষ্য শহর ও পাইলট এলাকা নির্বাচন করুন
- ড্রাইভার ও যাত্রী সমীক্ষা শুরু করুন
- প্রাথমিক বাজেট ঠিক করুন

### সপ্তাহ ২

- ফেয়ার ফর্মুলা ও কমিশন মডেল তৈরি করুন
- রাইডার/ড্রাইভার/অ্যাডমিন ইউজার ফ্লো তৈরি করুন
- ওয়্যারফ্রেম শুরু করুন
- ডেটাবেস স্কিমা খসড়া করুন

### সপ্তাহ ৩

- Flutter monorepo সেটআপ করুন
- ডিজাইন সিস্টেম তৈরি করুন
- অথেনটিকেশন স্ক্রিন শুরু করুন
- ম্যাপ ও লোকেশন প্রোটোটাইপ শুরু করুন

### সপ্তাহ ৪

- ড্রাইভার লোকেশন প্রোটোটাইপ
- রাইড রিকোয়েস্ট প্রোটোটাইপ
- অ্যাডমিন লগইন ও বেসিক ড্যাশবোর্ড
- পাইলট KPI ঠিক করুন

---

চাইলে আমি পরের ধাপে আপনার জন্য আলাদাভাবে তৈরি করে দিতে পারি:

1. **সম্পূর্ণ Flutter প্রজেক্ট ফোল্ডার স্ট্রাকচার + কোড স্টার্টার**  
2. **রাইডার/ড্রাইভার/অ্যাডমিন স্ক্রিনের বিস্তারিত স্ক্রিন ম্যাপ**  
3. **ভারতের জন্য আইনি কমপ্লায়েন্স চেকলিস্ট**  
4. **ডেটাবেস টেবিল ও এপিআই-এর পূর্ণ স্পেসিফিকেশন**  
5. **MVP ডেভেলপমেন্টের জন্য সপ্তাহভিত্তিক টাস্ক প্ল্যান**

নিচে বাকি ৪টি ডেলিভারেবল বিস্তারিতভাবে তৈরি করে দিলাম। সবকিছু **ভারতীয় প্রেক্ষাপট + কো-অপারেটিভ মডেল + এক কোডবেস (Flutter Monorepo)**-এর জন্য উপযোগী।

---

# 📦 পার্ট ১: সম্পূর্ণ Flutter প্রজেক্ট ফোল্ডার স্ট্রাকচার + কোড স্টার্টার

## ১.১ Monorepo রুট স্ট্রাকচার

```text
coop_ride/
│
├── .github/
│   ├── workflows/
│   │   ├── rider-android.yml
│   │   ├── rider-ios.yml
│   │   ├── driver-android.yml
│   │   ├── driver-ios.yml
│   │   ├── admin-mobile.yml
│   │   ├── admin-web.yml
│   │   └── backend-tests.yml
│   └── CODEOWNERS
│
├── .vscode/
│   ├── settings.json
│   ├── launch.json
│   └── extensions.json
│
├── packages/                    # সকল শেয়ার্ড কোড এখানে
│   ├── core/
│   ├── design_system/
│   ├── features/
│   └── api_client/
│
├── apps/                        # আলাদা আলাদা অ্যাপ এন্ট্রি পয়েন্ট
│   ├── rider/
│   ├── driver/
│   ├── admin_mobile/
│   └── admin_web/
│
├── backend/                     # ব্যাকএন্ড সার্ভিস (আলাদা রিপো হতে পারে)
│
├── tools/
│   ├── scripts/
│   └── code_gen/
│
├── docs/
│   ├── architecture.md
│   ├── api-contracts.md
│   ├── database.md
│   ├── onboarding.md
│   └── runbooks/
│
├── melos.yaml
├── pubspec.yaml
├── analysis_options.yaml
├── .gitignore
├── .env.example
├── README.md
└── LICENSE
```

---

## ১.২ `packages/` ভেতরের বিস্তারিত স্ট্রাকচার

### `packages/core/`

```text
packages/core/
├── lib/
│   ├── core.dart
│   ├── src/
│   │   ├── bootstrap/
│   │   │   ├── bootstrap.dart
│   │   │   ├── app_config.dart
│   │   │   └── dependency_injection.dart
│   │   ├── config/
│   │   │   ├── env.dart
│   │   │   ├── flavor.dart
│   │   │   └── app_role.dart
│   │   ├── network/
│   │   │   ├── http_client.dart
│   │   │   ├── interceptors/
│   │   │   │   ├── auth_interceptor.dart
│   │   │   │   ├── refresh_token_interceptor.dart
│   │   │   │   ├── logging_interceptor.dart
│   │   │   │   └── error_interceptor.dart
│   │   │   ├── api_result.dart
│   │   │   └── exceptions.dart
│   │   ├── auth/
│   │   │   ├── auth_repository.dart
│   │   │   ├── auth_state.dart
│   │   │   ├── auth_providers.dart
│   │   │   └── session_manager.dart
│   │   ├── storage/
│   │   │   ├── secure_storage.dart
│   │   │   ├── preferences_storage.dart
│   │   │   └── cache_storage.dart
│   │   ├── location/
│   │   │   ├── location_service.dart
│   │   │   ├── location_permission.dart
│   │   │   ├── background_location.dart
│   │   │   └── geocoding_service.dart
│   │   ├── maps/
│   │   │   ├── map_service.dart
│   │   │   ├── google_maps_config.dart
│   │   │   └── places_service.dart
│   │   ├── realtime/
│   │   │   ├── websocket_service.dart
│   │   │   ├── realtime_events.dart
│   │   │   └── reconnect_strategy.dart
│   │   ├── permissions/
│   │   │   └── permission_handler.dart
│   │   ├── notifications/
│   │   │   ├── push_service.dart
│   │   │   ├── local_notification_service.dart
│   │   │   └── notification_handlers.dart
│   │   ├── analytics/
│   │   │   ├── analytics_service.dart
│   │   │   └── crash_reporting.dart
│   │   ├── payments/
│   │   │   ├── payment_service.dart
│   │   │   ├── upi_handler.dart
│   │   │   └── payment_status.dart
│   │   ├── kyc/
│   │   │   ├── kyc_service.dart
│   │   │   └── digilocker_handler.dart
│   │   ├── i18n/
│   │   │   ├── localization.dart
│   │   │   └── locales/
│   │   │       ├── en.json
│   │   │       ├── hi.json
│   │   │       └── bn.json
│   │   └── utils/
│   │       ├── formatters.dart
│   │       ├── validators.dart
│   │       ├── date_utils.dart
│   │       ├── currency.dart
│   │       └── phone_utils.dart
│   └── test/
└── pubspec.yaml
```

---

### `packages/design_system/`

```text
packages/design_system/
├── lib/
│   ├── design_system.dart
│   └── src/
│       ├── theme/
│       │   ├── app_theme.dart
│       │   ├── app_colors.dart
│       │   ├── app_typography.dart
│       │   ├── app_spacing.dart
│       │   └── breakpoints.dart
│       ├── components/
│       │   ├── buttons/
│       │   │   ├── primary_button.dart
│       │   │   ├── secondary_button.dart
│       │   │   ├── icon_button.dart
│       │   │   └── loading_button.dart
│       │   ├── cards/
│       │   │   ├── ride_card.dart
│       │   │   ├── driver_card.dart
│       │   │   └── info_card.dart
│       │   ├── forms/
│       │   │   ├── text_field.dart
│       │   │   ├── phone_input.dart
│       │   │   ├── otp_input.dart
│       │   │   └── search_bar.dart
│       │   ├── dialogs/
│       │   │   ├── confirm_dialog.dart
│       │   │   ├── alert_dialog.dart
│       │   │   └── bottom_sheet.dart
│       │   ├── feedback/
│       │   │   ├── snackbar.dart
│       │   │   ├── loading_overlay.dart
│       │   │   └── error_view.dart
│       │   └── layout/
│       │       ├── app_scaffold.dart
│       │       ├── responsive_layout.dart
│       │       ├── sidebar.dart
│       │       └── bottom_nav.dart
│       └── icons/
│           └── app_icons.dart
└── pubspec.yaml
```

---

### `packages/features/`

```text
packages/features/
├── auth/
│   ├── lib/
│   │   ├── auth_feature.dart
│   │   └── src/
│   │       ├── login/
│   │       ├── otp/
│   │       └── onboarding/
│   └── pubspec.yaml
│
├── rider/
│   ├── lib/
│   │   ├── rider_feature.dart
│   │   └── src/
│   │       ├── home/
│   │       ├── pickup_selection/
│   │       ├── destination_search/
│   │       ├── ride_type_selection/
│   │       ├── fare_estimate/
│   │       ├── ride_confirmation/
│   │       ├── driver_searching/
│   │       ├── driver_assigned/
│   │       ├── live_tracking/
│   │       ├── trip_active/
│   │       ├── payment/
│   │       ├── rating/
│   │       ├── trip_history/
│   │       ├── ride_receipt/
│   │       └── saved_addresses/
│   └── pubspec.yaml
│
├── driver/
│   ├── lib/
│   │   ├── driver_feature.dart
│   │   └── src/
│   │       ├── onboarding/
│   │       ├── document_upload/
│   │       ├── application_status/
│   │       ├── home/
│   │       ├── online_status/
│   │       ├── ride_request/
│   │       ├── pickup_navigation/
│   │       ├── ride_otp_verification/
│   │       ├── trip_active/
│   │       ├── drop_navigation/
│   │       ├── trip_completion/
│   │       ├── earnings/
│   │       ├── payouts/
│   │       ├── documents/
│   │       ├── training/
│   │       └── cooperative_membership/
│   └── pubspec.yaml
│
├── admin/
│   ├── lib/
│   │   ├── admin_feature.dart
│   │   └── src/
│   │       ├── dashboard/
│   │       ├── live_ops_map/
│   │       ├── ride_management/
│   │       ├── driver_management/
│   │       ├── rider_management/
│   │       ├── kyc_verification/
│   │       ├── pricing_config/
│   │       ├── zone_geofence/
│   │       ├── finance/
│   │       ├── payouts/
│   │       ├── support_tickets/
│   │       ├── safety_incidents/
│   │       ├── cooperative/
│   │       │   ├── members/
│   │       │   ├── shares/
│   │       │   ├── patronage/
│   │       │   ├── meetings/
│   │       │   └── voting/
│   │       ├── reports/
│   │       ├── settings/
│   │       └── audit_logs/
│   └── pubspec.yaml
│
├── cooperative_member/
│   ├── lib/
│   │   ├── coop_feature.dart
│   │   └── src/
│   │       ├── membership_dashboard/
│   │       ├── share_purchase/
│   │       ├── share_transactions/
│   │       ├── patronage_report/
│   │       ├── dividend_history/
│   │       ├── meetings/
│   │       └── voting/
│   └── pubspec.yaml
│
├── safety/
│   ├── lib/
│   │   ├── safety_feature.dart
│   │   └── src/
│   │       ├── sos_button/
│   │       ├── emergency_contacts/
│   │       ├── trip_share/
│   │       ├── incident_report/
│   │       └── safety_dashboard/
│   └── pubspec.yaml
│
├── payments/
│   ├── lib/
│   │   ├── payments_feature.dart
│   │   └── src/
│   │       ├── payment_methods/
│   │       ├── upi_checkout/
│   │       ├── cash_flow/
│   │       ├── wallet/
│   │       └── payment_history/
│   └── pubspec.yaml
│
└── support/
    ├── lib/
    │   ├── support_feature.dart
    │   └── src/
    │       ├── help_center/
    │       ├── create_ticket/
    │       └── ticket_details/
    └── pubspec.yaml
```

---

### `packages/api_client/`

```text
packages/api_client/
├── lib/
│   ├── api_client.dart
│   └── src/
│       ├── generated/           # OpenAPI থেকে জেনারেট
│       │   ├── api/
│       │   │   ├── auth_api.dart
│       │   │   ├── rides_api.dart
│       │   │   ├── drivers_api.dart
│       │   │   ├── payments_api.dart
│       │   │   └── admin_api.dart
│       │   └── models/
│       ├── endpoints.dart
│       └── dto/
│           ├── ride_dto.dart
│           ├── driver_dto.dart
│           ├── user_dto.dart
│           └── payment_dto.dart
└── pubspec.yaml
```

---

## ১.৩ `apps/` ফোল্ডার স্ট্রাকচার

```text
apps/
├── rider/
│   ├── android/
│   │   ├── app/
│   │   │   ├── src/main/
│   │   │   │   ├── AndroidManifest.xml
│   │   │   │   └── res/
│   │   │   └── build.gradle
│   │   └── build.gradle
│   ├── ios/
│   │   ├── Runner/
│   │   ├── Runner.xcodeproj/
│   │   ├── Podfile
│   │   └── Info.plist
│   ├── lib/
│   │   ├── main_dev.dart
│   │   ├── main_staging.dart
│   │   ├── main_prod.dart
│   │   └── app/
│   │       ├── rider_app.dart
│   │       └── rider_router.dart
│   ├── assets/
│   └── pubspec.yaml
│
├── driver/
│   ├── android/
│   ├── ios/
│   ├── lib/
│   │   ├── main_dev.dart
│   │   ├── main_staging.dart
│   │   ├── main_prod.dart
│   │   └── app/
│   │       ├── driver_app.dart
│   │       └── driver_router.dart
│   ├── assets/
│   └── pubspec.yaml
│
├── admin_mobile/
│   ├── android/
│   ├── ios/
│   ├── lib/
│   │   ├── main_dev.dart
│   │   ├── main_staging.dart
│   │   ├── main_prod.dart
│   │   └── app/
│   │       ├── admin_mobile_app.dart
│   │       └── admin_mobile_router.dart
│   └── pubspec.yaml
│
└── admin_web/
    ├── web/
    │   ├── index.html
    │   ├── favicon.png
    │   └── manifest.json
    ├── lib/
    │   ├── main_dev.dart
    │   ├── main_staging.dart
    │   ├── main_prod.dart
    │   └── app/
    │       ├── admin_web_app.dart
    │       └── admin_web_router.dart
    └── pubspec.yaml
```

---

## ১.৪ গুরুত্বপূর্ণ কনফিগারেশন ফাইল

### `melos.yaml` (মূল রুটে)

```yaml
name: coop_ride
repository: https://github.com/your-org/coop_ride

packages:
  - packages/**
  - apps/**

command:
  bootstrap:
    runPubGetInParallel: false
    usePubspecOverrides: true

scripts:
  analyze:
    exec: dart analyze --fatal-infos
  format:
    exec: dart format .
  build_runner:
    exec: dart run build_runner build --delete-conflicting-outputs
    packageFilters:
      dependsOn: build_runner
  test:
    exec: flutter test
  test:all:
    run: melos run test --no-select
  
  build:rider:android:
    run: cd apps/rider && flutter build appbundle --release --target lib/main_prod.dart --dart-define=ENV=prod
  build:rider:ios:
    run: cd apps/rider && flutter build ipa --release --target lib/main_prod.dart --dart-define=ENV=prod
  
  build:driver:android:
    run: cd apps/driver && flutter build appbundle --release --target lib/main_prod.dart --dart-define=ENV=prod
  build:driver:ios:
    run: cd apps/driver && flutter build ipa --release --target lib/main_prod.dart --dart-define=ENV=prod
  
  build:admin:android:
    run: cd apps/admin_mobile && flutter build appbundle --release --target lib/main_prod.dart --dart-define=ENV=prod
  
  build:admin:web:
    run: cd apps/admin_web && flutter build web --release --target lib/main_prod.dart --dart-define=ENV=prod
```

---

### `packages/core/lib/src/bootstrap/bootstrap.dart`

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:sentry_flutter/sentry_flutter.dart';
import 'package:firebase_core/firebase_core.dart';
import 'package:firebase_messaging/firebase_messaging.dart';

import '../config/app_config.dart';
import '../config/app_role.dart';
import '../config/env.dart';
import '../analytics/analytics_service.dart';
import '../analytics/crash_reporting.dart';
import '../auth/session_manager.dart';
import '../network/http_client.dart';
import '../storage/secure_storage.dart';
import 'dependency_injection.dart';

Future<void> bootstrap({
  required AppRole appRole,
  required Widget Function() builder,
}) async {
  WidgetsFlutterBinding.ensureInitialized();
  
  // Environment init
  await Env.init();
  
  // Firebase init
  await Firebase.initializeApp();
  
  // Sentry init
  await SentryFlutter.init(
    (options) {
      options.dsn = Env.sentryDsn;
      options.environment = Env.environment;
      options.release = Env.appVersion;
    },
    appRunner: () async {
      // Session manager
      await SessionManager.init();
      
      // Push notifications
      await FirebaseMessaging.instance.requestPermission();
      
      // Analytics
      await AnalyticsService.init(appRole);
      
      // Run app
      runApp(
        ProviderScope(
          overrides: [
            appRoleProvider.overrideWithValue(appRole),
            ...dependencyOverrides(appRole),
          ],
          child: builder(),
        ),
      );
    },
  );
}
```

---

### `apps/rider/lib/main_prod.dart`

```dart
import 'package:core/core.dart';
import 'app/rider_app.dart';

void main() {
  bootstrap(
    appRole: AppRole.rider,
    builder: () => const RiderApp(),
  );
}
```

---

### `apps/rider/lib/app/rider_app.dart`

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_localizations/flutter_localizations.dart';
import 'package:design_system/design_system.dart';
import 'package:core/core.dart';

import 'rider_router.dart';

class RiderApp extends ConsumerWidget {
  const RiderApp({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final router = ref.watch(riderRouterProvider);
    final locale = ref.watch(currentLocaleProvider);

    return MaterialApp.router(
      title: 'CoopRide - Rider',
      theme: AppTheme.light(),
      darkTheme: AppTheme.dark(),
      themeMode: ThemeMode.system,
      routerConfig: router,
      localizationsDelegates: const [
        GlobalMaterialLocalizations.delegate,
        GlobalWidgetsLocalizations.delegate,
        GlobalCupertinoLocalizations.delegate,
        AppLocalizations.delegate,
      ],
      supportedLocales: const [
        Locale('en'),
        Locale('hi'),
        Locale('bn'),
      ],
      locale: locale,
    );
  }
}
```

---

### `apps/rider/lib/app/rider_router.dart`

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';
import 'package:auth_feature/auth_feature.dart';
import 'package:rider_feature/rider_feature.dart';
import 'package:safety_feature/safety_feature.dart';
import 'package:coop_feature/coop_feature.dart';
import 'package:support_feature/support_feature.dart';
import 'package:core/core.dart';

final riderRouterProvider = Provider<GoRouter>((ref) {
  final authState = ref.watch(authStateProvider);
  
  return GoRouter(
    initialLocation: '/',
    redirect: (context, state) {
      final isLoggedIn = authState.isAuthenticated;
      final isAuthRoute = state.matchedLocation.startsWith('/auth');
      
      if (!isLoggedIn && !isAuthRoute) return '/auth/login';
      if (isLoggedIn && isAuthRoute) return '/';
      return null;
    },
    routes: [
      GoRoute(
        path: '/',
        builder: (context, state) => const RiderHomeScreen(),
        routes: [
          GoRoute(path: 'ride/:id', builder: (_, s) => RideTrackingScreen(rideId: s.pathParameters['id']!)),
          GoRoute(path: 'history', builder: (_, __) => const TripHistoryScreen()),
          GoRoute(path: 'receipt/:id', builder: (_, s) => RideReceiptScreen(rideId: s.pathParameters['id']!)),
          GoRoute(path: 'addresses', builder: (_, __) => const SavedAddressesScreen()),
          GoRoute(path: 'profile', builder: (_, __) => const ProfileScreen()),
          GoRoute(path: 'membership', builder: (_, __) => const MembershipDashboardScreen()),
          GoRoute(path: 'safety', builder: (_, __) => const SafetyScreen()),
          GoRoute(path: 'support', builder: (_, __) => const SupportScreen()),
        ],
      ),
      GoRoute(
        path: '/auth',
        routes: [
          GoRoute(path: 'login', builder: (_, __) => const LoginScreen()),
          GoRoute(path: 'otp', builder: (_, __) => const OtpScreen()),
          GoRoute(path: 'onboarding', builder: (_, __) => const OnboardingScreen()),
        ],
      ),
    ],
    errorBuilder: (context, state) => ErrorScreen(error: state.error),
  );
});
```

---

### `apps/driver/android/app/src/main/AndroidManifest.xml` (গুরুত্বপূর্ণ পারমিশন)

```xml
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="CoopRide Driver"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">
        
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
            
        <service
            android:name=".LocationForegroundService"
            android:foregroundServiceType="location"
            android:exported="false" />
    </application>
</manifest>
```

---

### `apps/rider/ios/Runner/Info.plist` (গুরুত্বপূর্ণ কী)

```xml
<key>NSLocationWhenInUseUsageDescription</key>
<string>CoopRide needs your location to show nearby drivers and track your ride.</string>
<key>NSLocationAlwaysAndWhenInUseUsageDescription</key>
<string>CoopRide uses your location to match you with drivers and for safety features.</string>
<key>NSCameraUsageDescription</key>
<string>CoopRide needs camera access for document upload and profile photo.</string>
<key>NSPhotoLibraryUsageDescription</key>
<string>CoopRide needs photo library access to upload documents.</string>
<key>UIBackgroundModes</key>
<array>
    <string>fetch</string>
    <string>remote-notification</string>
    <string>location</string>
</array>
<key>LSApplicationQueriesSchemes</key>
<array>
    <string>phonepe</string>
    <string>tez</string>
    <string>paytm</string>
    <string>bhim</string>
    <string>upi</string>
</array>
```

---

### `.env.example`

```env
# Environment
ENV=dev
API_BASE_URL=https://api.dev.coopride.in
WEBSOCKET_URL=wss://ws.dev.coopride.in

# Google Maps
GOOGLE_MAPS_API_KEY_ANDROID=xxx
GOOGLE_MAPS_API_KEY_IOS=xxx
GOOGLE_MAPS_API_KEY_WEB=xxx

# Firebase
FIREBASE_PROJECT_ID=coop-ride-dev

# Sentry
SENTRY_DSN=https://xxx@sentry.io/xxx

# Payment Gateway
RAZORPAY_KEY_ID=rzp_test_xxx
CASHFREE_APP_ID=xxx

# SMS
MSG91_AUTH_KEY=xxx

# Feature Flags
ENABLE_COOP_FEATURES=true
ENABLE_UPI=true
ENABLE_CASH=true
```

---

## ১.৫ উদাহরণ Feature Module: `rider/home/`

```text
packages/features/rider/lib/src/home/
├── home_feature.dart
├── data/
│   ├── rider_home_repository.dart
│   └── rider_home_repository_impl.dart
├── domain/
│   ├── ride_estimate.dart
│   ├── nearby_driver.dart
│   └── usecases/
│       ├── get_ride_estimate.dart
│       └── get_nearby_drivers.dart
├── presentation/
│   ├── screens/
│   │   └── rider_home_screen.dart
│   ├── widgets/
│   │   ├── map_view.dart
│   │   ├── pickup_input.dart
│   │   ├── destination_input.dart
│   │   ├── ride_type_selector.dart
│   │   ├── fare_estimate_card.dart
│   │   ├── nearby_driver_marker.dart
│   │   └── sos_floating_button.dart
│   └── controllers/
│       └── rider_home_controller.dart
└── providers.dart
```

### `rider_home_controller.dart` উদাহরণ

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:freezed_annotation/freezed_annotation.dart';

part 'rider_home_controller.freezed.dart';

@freezed
class RiderHomeState with _$RiderHomeState {
  const factory RiderHomeState({
    @Default(null) Location? pickupLocation,
    @Default(null) Location? dropLocation,
    @Default(null) RideEstimate? estimate,
    @Default([]) List<NearbyDriver> nearbyDrivers,
    @Default(false) bool isLoading,
    @Default(null) String? errorMessage,
    @Default('bike') String selectedRideType,
  }) = _RiderHomeState;
}

@riverpod
class RiderHomeController extends _$RiderHomeController {
  @override
  RiderHomeState build() => const RiderHomeState();

  Future<void> updatePickup(Location location) async {
    state = state.copyWith(pickupLocation: location);
    await _refreshEstimate();
  }

  Future<void> updateDrop(Location location) async {
    state = state.copyWith(dropLocation: location);
    await _refreshEstimate();
  }

  Future<void> selectRideType(String type) async {
    state = state.copyWith(selectedRideType: type);
    await _refreshEstimate();
  }

  Future<void> _refreshEstimate() async {
    if (state.pickupLocation == null || state.dropLocation == null) return;
    
    state = state.copyWith(isLoading: true);
    try {
      final estimate = await ref.read(getRideEstimateUseCaseProvider).execute(
        pickup: state.pickupLocation!,
        drop: state.dropLocation!,
        rideType: state.selectedRideType,
      );
      state = state.copyWith(estimate: estimate, isLoading: false);
    } catch (e) {
      state = state.copyWith(errorMessage: e.toString(), isLoading: false);
    }
  }

  Future<String> requestRide() async {
    // ... ride request logic
    return 'ride_123';
  }
}
```

---

# 🗺️ পার্ট ২: রাইডার/ড্রাইভার/অ্যাডমিন অ্যাপের বিস্তারিত স্ক্রিন ম্যাপ

## ২.১ রাইডার অ্যাপের স্ক্রিন ম্যাপ (৩২টি স্ক্রিন)

### A. অনবোর্ডিং ও অথেনটিকেশন (৭টি)

| # | স্ক্রিন | উদ্দেশ্য | মূল উপাদান |
|---|---|---|---|
| 1 | **Splash** | অ্যাপ লোড | লোগো, স্লোগান |
| 2 | **Language Select** | ভাষা বাছাই | EN / HI / BN বাটন |
| 3 | **Onboarding Slider** | পরিচিতি | ৩-৪ স্লাইড |
| 4 | **Login** | ফোন নম্বর | +91, ১০-digit input |
| 5 | **OTP Verify** | OTP যাচাই | ৬-digit input, resend |
| 6 | **Profile Setup** | নাম/ইমেইল | ফর্ম |
| 7 | **Permissions** | লোকেশন/নোটিফিকেশন | Allow/Deny |

---

### B. হোম ও বুকিং ফ্লো (১০টি)

| # | স্ক্রিন | উদ্দেশ্য | মূল উপাদান |
|---|---|---|---|
| 8 | **Rider Home** | মূল ম্যাপ | ম্যাপ, pickup input, nearby drivers |
| 9 | **Pickup Selection** | পিকআপ ঠিক করুন | Search, saved, current location |
| 10 | **Destination Search** | গন্তব্য খোঁজা | Places API, history, saved |
| 11 | **Ride Type Selection** | গাড়ি টাইপ | Bike / Auto / Car / Premium |
| 12 | **Fare Estimate** | ভাড়া দেখানো | Fare breakdown, ETA, surge |
| 13 | **Payment Method Select** | পেমেন্ট বাছাই | Cash / UPI / Card / Wallet |
| 14 | **Ride Confirmation** | চূড়ান্ত নিশ্চিতকরণ | Pickup/drop, fare, driver ETA |
| 15 | **Searching Driver** | ড্রাইভার খোঁজা | Animation, cancel button |
| 16 | **Driver Assigned** | ড্রাইভার পাওয়া গেছে | Driver photo, name, rating, vehicle |
| 17 | **Cancel Reason** | বাতিল কারণ | Reason list |

---

### C. লাইভ রাইড ফ্লো (৬টি)

| # | স্ক্রিন | উদ্দেশ্য | মূল উপাদান |
|---|---|---|---|
| 18 | **Driver En Route** | ড্রাইভার আসছে | Live map, ETA, call/chat |
| 19 | **Driver Arrived** | ড্রাইভার পৌঁছেছে | Notification, wait timer |
| 20 | **Ride OTP Input** | যাত্রী OTP দিন | ৪-digit OTP |
| 21 | **Trip Active** | ট্রিপ চলছে | Live map, route, SOS button |
| 22 | **Trip Share** | লাইভ শেয়ার | Link, contact picker |
| 23 | **Near Drop** | গন্তব্যের কাছে | Warning card |

---

### D. পেমেন্ট ও রেটিং (৩টি)

| # | স্ক্রিন | উদ্দেশ্য | মূল উপাদান |
|---|---|---|---|
| 24 | **Payment Collection** | পেমেন্ট | Cash/UPI/QR |
| 25 | **Rating & Review** | রেটিং | ৫ star, tips, feedback |
| 26 | **Ride Receipt** | রসিদ | GST invoice, download PDF |

---

### E. হিস্ট্রি ও ম্যানেজমেন্ট (৩টি)

| # | স্ক্রিন | উদ্দেশ্য | মূল উপাদান |
|---|---|---|---|
| 27 | **Trip History** | সব রাইড | List with filters |
| 28 | **Saved Addresses** | সংরক্ষিত ঠিকানা | Home/Work/Other |
| 29 | **Edit Address** | ঠিকানা সম্পাদনা | Map pin, name, label |

---

### F. প্রোফাইল ও সেটিংস (৩টি)

| # | স্ক্রিন | উদ্দেশ্য | মূল উপাদান |
|---|---|---|---|
| 30 | **Profile** | ব্যক্তিগত তথ্য | Photo, name, phone, language |
| 31 | **Settings** | অ্যাপ সেটিংস | Language, notifications, privacy |
| 32 | **Emergency Contacts** | জরুরি যোগাযোগ | Add/edit/delete |

---

### G. কো-অপ ও সাপোর্ট (অতিরিক্ত)

| # | স্ক্রিন |
|---|---|
| 33 | **Membership Dashboard** |
| 34 | **Share Purchase** |
| 35 | **Patronage Report** |
| 36 | **Meetings & Voting** |
| 37 | **Support Home** |
| 38 | **Create Ticket** |
| 39 | **SOS Screen** |
| 40 | **Safety Incident Report** |

---

## ২.২ ড্রাইভার অ্যাপের স্ক্রিন ম্যাপ (৩৮টি স্ক্রিন)

### A. অনবোর্ডিং ও KYC (১২টি)

| # | স্ক্রিন |
|---|---|
| 1 | Splash |
| 2 | Language Select |
| 3 | Login |
| 4 | OTP Verify |
| 5 | **Driver Registration Form** (ব্যক্তিগত তথ্য) |
| 6 | **Driving Licence Upload** + OCR |
| 7 | **Vehicle Details Form** |
| 8 | **RC Upload** + OCR |
| 9 | **Insurance Upload** |
| 10 | **Fitness/PUC/Permit Upload** |
| 11 | **Bank Account for Payout** |
| 12 | **Application Status** (pending/approved/rejected) |

---

### B. ট্রেনিং (৩টি)

| # | স্ক্রিন |
|---|---|
| 13 | **Training Home** (মডিউল লিস্ট) |
| 14 | **Video/Article Viewer** |
| 15 | **Quiz / Certification** |

---

### C. হোম ও অনলাইন স্ট্যাটাস (৫টি)

| # | স্ক্রিন |
|---|---|
| 16 | **Driver Home** (offline mode) |
| 17 | **Online Dashboard** (earnings today, status) |
| 18 | **Driver Profile** |
| 19 | **Document Status** (expiry alerts) |
| 20 | **Location Permission Explanation** |

---

### D. রাইড রিকোয়েস্ট ও ট্রিপ (১০টি)

| # | স্ক্রিন |
|---|---|
| 21 | **Ride Request Card** (popup overlay) |
| 22 | **Ride Details View** (full-screen) |
| 23 | **Pickup Navigation** |
| 24 | **Arrived at Pickup** |
| 25 | **Wait for Passenger** (timer) |
| 26 | **Collect OTP from Passenger** |
| 27 | **Trip Active Navigation** |
| 28 | **Trip Completed Summary** |
| 29 | **Cash Collection Confirm** |
| 30 | **Rate Passenger** |

---

### E. আয় ও পেমেন্ট (৪টি)

| # | স্ক্রিন |
|---|---|
| 31 | **Earnings Dashboard** (daily/weekly/monthly) |
| 32 | **Trip Earnings Detail** |
| 33 | **Payout History** |
| 34 | **Bank/UPI Setup** |

---

### F. সেফটি ও সাপোর্ট (৪টি)

| # | স্ক্রিন |
|---|---|
| 35 | **Driver SOS** |
| 36 | **Report Incident** |
| 37 | **Support Tickets** |
| 38 | **Help Center** |

---

## ২.৩ মোবাইল অ্যাডমিন অ্যাপের স্ক্রিন ম্যাপ (২২টি স্ক্রিন)

### A. অথেনটিকেশন (৩টি)

| # | স্ক্রিন |
|---|---|
| 1 | Admin Login |
| 2 | 2FA Verify |
| 3 | Biometric Unlock |

---

### B. অপারেশনাল ড্যাশবোর্ড (৬টি)

| # | স্ক্রিন |
|---|---|
| 4 | **Ops Dashboard** (today rides, revenue, active drivers) |
| 5 | **Live Ops Map** (filtered) |
| 6 | **Active Rides List** |
| 7 | **SOS/Incident Alerts** |
| 8 | **Ride Detail** |
| 9 | **Driver Detail** |

---

### C. ভেরিফিকেশন (৪টি)

| # | স্ক্রিন |
|---|---|
| 10 | **KYC Queue** |
| 11 | **Document Review** |
| 12 | **Approve/Reject with Reason** |
| 13 | **Driver Profile View** |

---

### D. অ্যাকশন ও সাপোর্ট (৫টি)

| # | স্ক্রিন |
|---|---|
| 14 | **Cancel/Reassign Ride** |
| 15 | **Support Tickets** |
| 16 | **Ticket Detail & Reply** |
| 17 | **Broadcast Notification** |
| 18 | **Call Rider/Driver** |

---

### E. রিপোর্ট ও সেটিংস (৪টি)

| # | স্ক্রিন |
|---|---|
| 19 | **Daily Summary** |
| 20 | **Shift Report** |
| 21 | **Settings** |
| 22 | **Audit Log** |

---

## ২.৪ ওয়েব অ্যাডমিন প্যানেলের স্ক্রিন ম্যাপ (৫০+ স্ক্রিন)

### A. ড্যাশবোর্ড (৩টি)

- Executive Dashboard
- Operations Dashboard
- Finance Dashboard

### B. Live Operations (৪টি)

- Live Map
- Active Rides Table
- Driver Tracking
- Heatmap (after scale)

### C. User Management (৬টি)

- Riders List
- Rider Detail
- Drivers List
- Driver Detail
- KYC Pending Queue
- Blocked Users

### D. Ride Management (৫টি)

- All Rides
- Ride Detail
- Cancelled Rides
- Disputed Rides
- Ride Analytics

### E. Fleet Management (৪টি)

- Vehicles List
- Vehicle Detail
- Documents Expiry
- Fleet Analytics

### F. Pricing & Zones (৫টি)

- Pricing Configuration
- Ride Type Configuration
- Zone/Geofence Editor
- Surge Configuration
- Promo/Coupon Manager

### G. Finance (৭টি)

- Revenue Report
- Driver Settlements
- Payout Queue
- Refund Queue
- GST Reports
- Cash Reconciliation
- Platform Fee Report

### H. Cooperative Management (৬টি)

- Members List
- Member Detail
- Share Ledger
- Patronage Calculator
- Meetings Manager
- Voting Results

### I. Safety & Support (৫টি)

- Incidents Dashboard
- Incident Detail
- Support Tickets
- Ticket Detail
- Safety Analytics

### J. Reports & Analytics (৫টি)

- Business Reports
- Driver Analytics
- Rider Analytics
- City Performance
- Export Center

### K. System Settings (৫টি)

- Roles & Permissions
- Users (Admin)
- Notification Templates
- Feature Flags
- Audit Logs

---

# 🗃️ পার্ট ৩: সম্পূর্ণ ডেটাবেস স্কিমা ও API স্পেসিফিকেশন

## ৩.১ সম্পূর্ণ ডেটাবেস স্কিমা (PostgreSQL + PostGIS)

### `users` টেবিল

```sql
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    phone_country_code VARCHAR(4) DEFAULT '+91',
    phone_number VARCHAR(15) NOT NULL,
    email VARCHAR(255),
    full_name VARCHAR(255) NOT NULL,
    profile_photo_url VARCHAR(512),
    role VARCHAR(50) NOT NULL CHECK (role IN ('RIDER','DRIVER','ADMIN','SUPPORT','FINANCE','SAFETY','BOARD','SUPER_ADMIN')),
    status VARCHAR(20) DEFAULT 'ACTIVE' CHECK (status IN ('ACTIVE','PENDING','SUSPENDED','BLOCKED','DEACTIVATED')),
    preferred_language VARCHAR(10) DEFAULT 'en',
    preferred_currency VARCHAR(3) DEFAULT 'INR',
    timezone VARCHAR(50) DEFAULT 'Asia/Kolkata',
    device_fingerprint JSONB,
    last_login_at TIMESTAMPTZ,
    last_active_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(phone_country_code, phone_number)
);

CREATE INDEX idx_users_phone ON users(phone_country_code, phone_number);
CREATE INDEX idx_users_role ON users(role);
CREATE INDEX idx_users_status ON users(status);
```

---

### `rider_profiles` টেবিল

```sql
CREATE TABLE rider_profiles (
    user_id UUID PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
    emergency_contact_name VARCHAR(255),
    emergency_contact_phone VARCHAR(20),
    secondary_emergency_name VARCHAR(255),
    secondary_emergency_phone VARCHAR(20),
    default_payment_method VARCHAR(20) DEFAULT 'CASH',
    total_rides INTEGER DEFAULT 0,
    total_spent DECIMAL(12,2) DEFAULT 0,
    rating DECIMAL(3,2) DEFAULT 5.00,
    rating_count INTEGER DEFAULT 0,
    referral_code VARCHAR(20) UNIQUE,
    referred_by UUID REFERENCES users(id),
    is_coop_member BOOLEAN DEFAULT FALSE,
    member_id UUID,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

### `driver_profiles` টেবিল

```sql
CREATE TABLE driver_profiles (
    user_id UUID PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
    licence_number VARCHAR(50),
    licence_expiry DATE,
    kyc_status VARCHAR(20) DEFAULT 'PENDING' CHECK (kyc_status IN ('PENDING','SUBMITTED','APPROVED','REJECTED','SUSPENDED')),
    on_duty_status VARCHAR(20) DEFAULT 'OFFLINE' CHECK (on_duty_status IN ('ONLINE','OFFLINE','BUSY','BREAK')),
    current_lat DECIMAL(10,7),
    current_lng DECIMAL(10,7),
    current_heading DECIMAL(5,2),
    current_speed_mps DECIMAL(5,2),
    last_location_at TIMESTAMPTZ,
    rating DECIMAL(3,2) DEFAULT 5.00,
    rating_count INTEGER DEFAULT 0,
    acceptance_rate DECIMAL(5,2) DEFAULT 100.00,
    cancellation_rate DECIMAL(5,2) DEFAULT 0.00,
    total_rides INTEGER DEFAULT 0,
    total_earnings DECIMAL(12,2) DEFAULT 0,
    current_vehicle_id UUID,
    member_id UUID,
    bank_account_id UUID,
    upi_id VARCHAR(100),
    pan_number VARCHAR(20),
    aadhaar_last4 VARCHAR(4),
    police_verification_status VARCHAR(20),
    police_verification_expiry DATE,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_driver_profiles_location ON driver_profiles USING GIST (ST_SetSRID(ST_MakePoint(current_lng, current_lat), 4326));
CREATE INDEX idx_driver_profiles_status ON driver_profiles(on_duty_status, kyc_status);
```

---

### `vehicles` টেবিল

```sql
CREATE TABLE vehicles (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    driver_id UUID NOT NULL REFERENCES driver_profiles(user_id) ON DELETE CASCADE,
    vehicle_type VARCHAR(20) NOT NULL CHECK (vehicle_type IN ('BIKE','AUTO','CAR_MINI','CAR_SEDAN','CAR_SUV','CAR_PREMIUM','EV_BIKE','EV_AUTO','EV_CAR')),
    make VARCHAR(100),
    model VARCHAR(100),
    year INTEGER,
    color VARCHAR(50),
    registration_number VARCHAR(20) NOT NULL,
    registration_expiry DATE,
    insurance_provider VARCHAR(100),
    insurance_policy_number VARCHAR(100),
    insurance_expiry DATE,
    fitness_certificate_expiry DATE,
    permit_type VARCHAR(50),
    permit_expiry DATE,
    puc_expiry DATE,
    fuel_type VARCHAR(20) CHECK (fuel_type IN ('PETROL','DIESEL','CNG','ELECTRIC','HYBRID')),
    seating_capacity INTEGER,
    status VARCHAR(20) DEFAULT 'PENDING',
    photos JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(registration_number)
);

CREATE INDEX idx_vehicles_driver ON vehicles(driver_id);
CREATE INDEX idx_vehicles_expiry ON vehicles(registration_expiry, insurance_expiry, fitness_certificate_expiry, puc_expiry);
```

---

### `kyc_documents` টেবিল

```sql
CREATE TABLE kyc_documents (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    document_type VARCHAR(50) NOT NULL CHECK (document_type IN (
        'DRIVING_LICENCE','VEHICLE_RC','INSURANCE','FITNESS_CERTIFICATE',
        'PERMIT','PUC','ADDRESS_PROOF','PAN','BANK_PROOF',
        'PHOTO','POLICE_VERIFICATION','AADHAAR','SIGNATURE'
    )),
    document_number_masked VARCHAR(100),
    issuer VARCHAR(255),
    issue_date DATE,
    expiry_date DATE,
    file_url VARCHAR(512) NOT NULL,
    thumbnail_url VARCHAR(512),
    file_size_bytes INTEGER,
    mime_type VARCHAR(100),
    ocr_extracted_data JSONB,
    verification_status VARCHAR(20) DEFAULT 'PENDING' CHECK (verification_status IN ('PENDING','AUTO_APPROVED','MANUAL_REVIEW','APPROVED','REJECTED')),
    rejection_reason TEXT,
    verified_by UUID REFERENCES users(id),
    verified_at TIMESTAMPTZ,
    external_verification_ref VARCHAR(255),
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_kyc_documents_user ON kyc_documents(user_id);
CREATE INDEX idx_kyc_documents_status ON kyc_documents(verification_status);
CREATE INDEX idx_kyc_documents_type ON kyc_documents(document_type);
CREATE INDEX idx_kyc_documents_expiry ON kyc_documents(expiry_date);
```

---

### `cities` ও `zones` টেবিল

```sql
CREATE TABLE cities (
    id VARCHAR(50) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    state VARCHAR(100) NOT NULL,
    country VARCHAR(50) DEFAULT 'India',
    timezone VARCHAR(50) DEFAULT 'Asia/Kolkata',
    currency VARCHAR(3) DEFAULT 'INR',
    is_active BOOLEAN DEFAULT FALSE,
    default_language VARCHAR(10) DEFAULT 'en',
    supported_languages VARCHAR(10)[] DEFAULT ARRAY['en','hi'],
    launch_date DATE,
    operations_contact VARCHAR(50),
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE zones (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    city_id VARCHAR(50) NOT NULL REFERENCES cities(id),
    name VARCHAR(100) NOT NULL,
    polygon GEOGRAPHY(POLYGON, 4326) NOT NULL,
    is_active BOOLEAN DEFAULT TRUE,
    surge_enabled BOOLEAN DEFAULT FALSE,
    night_surcharge_enabled BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_zones_polygon ON zones USING GIST (polygon);
```

---

### `ride_types` টেবিল

```sql
CREATE TABLE ride_types (
    id VARCHAR(50) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    min_seating INTEGER,
    max_seating INTEGER,
    base_icon_url VARCHAR(512),
    is_active BOOLEAN DEFAULT TRUE,
    sort_order INTEGER,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

INSERT INTO ride_types (id, name, description, min_seating, max_seating, sort_order) VALUES
('bike', 'Bike', 'Fast and affordable', 1, 1, 1),
('auto', 'Auto', 'Three-wheeler', 1, 3, 2),
('car_mini', 'Mini Car', 'Compact car', 1, 4, 3),
('car_sedan', 'Sedan', 'Comfortable sedan', 1, 4, 4),
('car_suv', 'SUV', 'Spacious SUV', 1, 6, 5),
('ev_bike', 'EV Bike', 'Eco-friendly bike', 1, 1, 6),
('women_only', 'Women Safe Ride', 'Women driver', 1, 4, 7);
```

---

### `pricing_configs` টেবিল

```sql
CREATE TABLE pricing_configs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    city_id VARCHAR(50) NOT NULL REFERENCES cities(id),
    ride_type_id VARCHAR(50) NOT NULL REFERENCES ride_types(id),
    base_fare DECIMAL(10,2) NOT NULL,
    per_km_rate DECIMAL(10,2) NOT NULL,
    per_minute_rate DECIMAL(10,2) DEFAULT 0,
    minimum_fare DECIMAL(10,2) NOT NULL,
    cancellation_fee DECIMAL(10,2) DEFAULT 0,
    waiting_charge_per_min DECIMAL(10,2) DEFAULT 0,
    free_waiting_minutes INTEGER DEFAULT 3,
    night_start_time TIME DEFAULT '22:00:00',
    night_end_time TIME DEFAULT '05:00:00',
    night_multiplier DECIMAL(4,2) DEFAULT 1.25,
    peak_hours JSONB,
    peak_multiplier DECIMAL(4,2) DEFAULT 1.0,
    max_surge_multiplier DECIMAL(4,2) DEFAULT 1.5,
    platform_fee_percentage DECIMAL(5,2) DEFAULT 10.0,
    platform_fee_fixed DECIMAL(10,2) DEFAULT 0,
    gst_percentage DECIMAL(5,2) DEFAULT 5.0,
    gst_on_platform_fee_only BOOLEAN DEFAULT FALSE,
    toll_policy VARCHAR(20) DEFAULT 'ACTUALS' CHECK (toll_policy IN ('ACTUALS','INCLUDED','EXCLUDED')),
    outstation_per_km_rate DECIMAL(10,2),
    outstation_min_hours INTEGER,
    outstation_min_km INTEGER,
    is_active BOOLEAN DEFAULT TRUE,
    valid_from DATE,
    valid_until DATE,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(city_id, ride_type_id, valid_from)
);
```

---

### `ride_requests` টেবিল

```sql
CREATE TABLE ride_requests (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    rider_id UUID NOT NULL REFERENCES users(id),
    pickup_lat DECIMAL(10,7) NOT NULL,
    pickup_lng DECIMAL(10,7) NOT NULL,
    pickup_address TEXT,
    pickup_landmark VARCHAR(255),
    pickup_zone_id UUID REFERENCES zones(id),
    drop_lat DECIMAL(10,7) NOT NULL,
    drop_lng DECIMAL(10,7) NOT NULL,
    drop_address TEXT,
    drop_landmark VARCHAR(255),
    drop_zone_id UUID REFERENCES zones(id),
    ride_type_id VARCHAR(50) NOT NULL REFERENCES ride_types(id),
    estimated_distance_km DECIMAL(8,3),
    estimated_duration_min INTEGER,
    estimated_fare_min DECIMAL(10,2),
    estimated_fare_max DECIMAL(10,2),
    surge_multiplier DECIMAL(4,2) DEFAULT 1.0,
    promo_code VARCHAR(50),
    promo_discount DECIMAL(10,2),
    preferred_payment_method VARCHAR(20),
    special_instructions TEXT,
    is_scheduled BOOLEAN DEFAULT FALSE,
    scheduled_at TIMESTAMPTZ,
    status VARCHAR(30) DEFAULT 'DRAFT',
    notes TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    expired_at TIMESTAMPTZ
);

CREATE INDEX idx_ride_requests_rider ON ride_requests(rider_id);
CREATE INDEX idx_ride_requests_status ON ride_requests(status);
CREATE INDEX idx_ride_requests_pickup ON ride_requests USING GIST (ST_SetSRID(ST_MakePoint(pickup_lng, pickup_lat), 4326));
```

---

### `rides` টেবিল

```sql
CREATE TABLE rides (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    ride_number BIGSERIAL UNIQUE,
    request_id UUID REFERENCES ride_requests(id),
    rider_id UUID NOT NULL REFERENCES users(id),
    driver_id UUID REFERENCES users(id),
    vehicle_id UUID REFERENCES vehicles(id),
    city_id VARCHAR(50) REFERENCES cities(id),
    zone_id UUID REFERENCES zones(id),
    ride_type_id VARCHAR(50) REFERENCES ride_types(id),
    status VARCHAR(30) NOT NULL DEFAULT 'REQUESTED',
    
    pickup_lat DECIMAL(10,7) NOT NULL,
    pickup_lng DECIMAL(10,7) NOT NULL,
    pickup_address TEXT,
    drop_lat DECIMAL(10,7) NOT NULL,
    drop_lng DECIMAL(10,7) NOT NULL,
    drop_address TEXT,
    
    actual_distance_km DECIMAL(8,3),
    actual_duration_min INTEGER,
    
    base_fare DECIMAL(10,2) DEFAULT 0,
    distance_charge DECIMAL(10,2) DEFAULT 0,
    time_charge DECIMAL(10,2) DEFAULT 0,
    night_surcharge DECIMAL(10,2) DEFAULT 0,
    surge_charge DECIMAL(10,2) DEFAULT 0,
    toll_charges DECIMAL(10,2) DEFAULT 0,
    waiting_charges DECIMAL(10,2) DEFAULT 0,
    promo_discount DECIMAL(10,2) DEFAULT 0,
    fare_amount DECIMAL(10,2) DEFAULT 0,
    
    platform_fee DECIMAL(10,2) DEFAULT 0,
    gst_amount DECIMAL(10,2) DEFAULT 0,
    total_payable DECIMAL(10,2) DEFAULT 0,
    driver_net_amount DECIMAL(10,2) DEFAULT 0,
    
    payment_method VARCHAR(20),
    payment_status VARCHAR(20) DEFAULT 'PENDING' CHECK (payment_status IN ('PENDING','AUTHORIZED','CAPTURED','FAILED','REFUNDED')),
    payment_reference VARCHAR(255),
    
    ride_otp VARCHAR(6),
    pickup_at TIMESTAMPTZ,
    started_at TIMESTAMPTZ,
    completed_at TIMESTAMPTZ,
    cancelled_at TIMESTAMPTZ,
    cancelled_by VARCHAR(20),
    cancellation_reason TEXT,
    cancellation_fee DECIMAL(10,2) DEFAULT 0,
    
    rating_by_rider SMALLINT CHECK (rating_by_rider BETWEEN 1 AND 5),
    rating_comment_by_rider TEXT,
    rating_by_driver SMALLINT CHECK (rating_by_driver BETWEEN 1 AND 5),
    rating_comment_by_driver TEXT,
    
    safety_flags JSONB DEFAULT '{}',
    metadata JSONB DEFAULT '{}',
    
    invoice_number VARCHAR(50),
    gstin VARCHAR(50),
    sac_code VARCHAR(20),
    
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_rides_rider ON rides(rider_id);
CREATE INDEX idx_rides_driver ON rides(driver_id);
CREATE INDEX idx_rides_status ON rides(status);
CREATE INDEX idx_rides_city ON rides(city_id);
CREATE INDEX idx_rides_created ON rides(created_at DESC);
CREATE INDEX idx_rides_pickup ON rides USING GIST (ST_SetSRID(ST_MakePoint(pickup_lng, pickup_lat), 4326));
```

---

### `ride_status_history` টেবিল

```sql
CREATE TABLE ride_status_history (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    ride_id UUID NOT NULL REFERENCES rides(id) ON DELETE CASCADE,
    from_status VARCHAR(30),
    to_status VARCHAR(30) NOT NULL,
    triggered_by VARCHAR(20) NOT NULL,
    reason TEXT,
    metadata JSONB DEFAULT '{}',
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_ride_status_history_ride ON ride_status_history(ride_id, created_at);
```

---

### `ride_location_events` টেবিল (পার্টিশন করা)

```sql
CREATE TABLE ride_location_events (
    id UUID DEFAULT gen_random_uuid(),
    ride_id UUID NOT NULL,
    driver_id UUID NOT NULL,
    lat DECIMAL(10,7) NOT NULL,
    lng DECIMAL(10,7) NOT NULL,
    heading DECIMAL(5,2),
    speed_mps DECIMAL(5,2),
    accuracy_m DECIMAL(6,2),
    battery_percentage SMALLINT,
    network_type VARCHAR(20),
    app_version VARCHAR(20),
    device_id VARCHAR(100),
    event_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    PRIMARY KEY (id, event_at)
) PARTITION BY RANGE (event_at);

-- Monthly partitions (auto-create via cron)
CREATE TABLE ride_location_events_y2026m01 PARTITION OF ride_location_events
    FOR VALUES FROM ('2026-01-01') TO ('2026-02-01');
```

---

### `ledger_accounts` ও `ledger_entries` (Double-entry)

```sql
CREATE TABLE ledger_accounts (
    id VARCHAR(50) PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    type VARCHAR(30) NOT NULL CHECK (type IN ('ASSET','LIABILITY','EQUITY','REVENUE','EXPENSE')),
    subtype VARCHAR(50),
    description TEXT,
    owner_user_id UUID,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

INSERT INTO ledger_accounts (id, name, type, subtype) VALUES
('PLATFORM_REVENUE', 'Platform Revenue', 'REVENUE', 'RIDE_FEES'),
('GST_COLLECTED', 'GST Collected', 'LIABILITY', 'TAX'),
('DRIVER_PAYABLE', 'Driver Payable', 'LIABILITY', 'PAYABLE'),
('DRIVER_CASH_COLLECTED', 'Driver Cash Collected', 'ASSET', 'CASH'),
('PAYMENT_GATEWAY_RECEIVABLE', 'Payment Gateway Receivable', 'ASSET', 'RECEIVABLE'),
('PROMO_EXPENSE', 'Promotional Expense', 'EXPENSE', 'PROMO'),
('REFUND_PAYABLE', 'Refund Payable', 'LIABILITY', 'REFUND'),
('COOP_SHARE_CAPITAL', 'Cooperative Share Capital', 'EQUITY', 'SHARES'),
('COOP_RESERVE_FUND', 'Cooperative Reserve Fund', 'EQUITY', 'RESERVE'),
('COOP_PATRONAGE_PAYABLE', 'Cooperative Patronage Payable', 'LIABILITY', 'PATRONAGE');

CREATE TABLE ledger_entries (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    reference_type VARCHAR(50) NOT NULL,
    reference_id UUID NOT NULL,
    account_id VARCHAR(50) NOT NULL REFERENCES ledger_accounts(id),
    debit DECIMAL(14,2) DEFAULT 0,
    credit DECIMAL(14,2) DEFAULT 0,
    balance_after DECIMAL(14,2),
    description TEXT,
    created_by UUID,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_ledger_entries_account ON ledger_entries(account_id, created_at);
CREATE INDEX idx_ledger_entries_reference ON ledger_entries(reference_type, reference_id);
```

---

### `payments` টেবিল

```sql
CREATE TABLE payments (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    ride_id UUID REFERENCES rides(id),
    payer_user_id UUID NOT NULL REFERENCES users(id),
    amount DECIMAL(12,2) NOT NULL,
    currency VARCHAR(3) DEFAULT 'INR',
    method VARCHAR(30) NOT NULL CHECK (method IN ('CASH','UPI','CARD','NETBANKING','WALLET','CORPORATE')),
    status VARCHAR(20) NOT NULL DEFAULT 'PENDING' CHECK (status IN ('PENDING','AUTHORIZED','SUCCESS','FAILED','REFUNDED','EXPIRED')),
    gateway_provider VARCHAR(50),
    gateway_payment_id VARCHAR(255),
    gateway_order_id VARCHAR(255),
    gateway_signature TEXT,
    upi_txn_id VARCHAR(255),
    upi_vpa VARCHAR(255),
    card_last4 VARCHAR(4),
    card_network VARCHAR(20),
    failure_reason TEXT,
    refunded_amount DECIMAL(12,2) DEFAULT 0,
    captured_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_payments_ride ON payments(ride_id);
CREATE INDEX idx_payments_status ON payments(status);
CREATE INDEX idx_payments_gateway ON payments(gateway_provider, gateway_payment_id);
```

---

### `driver_payouts` টেবিল

```sql
CREATE TABLE driver_payouts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    payout_number BIGSERIAL UNIQUE,
    driver_id UUID NOT NULL REFERENCES users(id),
    period_start DATE NOT NULL,
    period_end DATE NOT NULL,
    gross_amount DECIMAL(12,2) NOT NULL,
    platform_fee_total DECIMAL(12,2) NOT NULL,
    gst_deducted DECIMAL(12,2) DEFAULT 0,
    tds_deducted DECIMAL(12,2) DEFAULT 0,
    adjustments DECIMAL(12,2) DEFAULT 0,
    net_amount DECIMAL(12,2) NOT NULL,
    status VARCHAR(20) DEFAULT 'PENDING' CHECK (status IN ('PENDING','PROCESSING','SUCCESS','FAILED','CANCELLED')),
    payment_method VARCHAR(20),
    bank_account_id UUID,
    upi_id VARCHAR(100),
    gateway_payout_id VARCHAR(255),
    failure_reason TEXT,
    processed_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_driver_payouts_driver ON driver_payouts(driver_id, period_start);
CREATE INDEX idx_driver_payouts_status ON driver_payouts(status);
```

---

### Cooperative Tables

```sql
CREATE TABLE coop_members (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id),
    member_number VARCHAR(20) UNIQUE NOT NULL,
    member_type VARCHAR(30) NOT NULL CHECK (member_type IN ('DRIVER','RIDER','WORKER','INVESTOR','FOUNDER')),
    status VARCHAR(20) DEFAULT 'PENDING' CHECK (status IN ('PENDING','ACTIVE','SUSPENDED','RESIGNED','DECEASED')),
    joined_at DATE,
    approved_at TIMESTAMPTZ,
    approved_by UUID,
    share_capital DECIMAL(12,2) DEFAULT 0,
    reserve_fund_contribution DECIMAL(12,2) DEFAULT 0,
    nominee_name VARCHAR(255),
    nominee_relation VARCHAR(100),
    nominee_phone VARCHAR(20),
    kyc_status VARCHAR(20) DEFAULT 'PENDING',
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE coop_shares (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    member_id UUID NOT NULL REFERENCES coop_members(id),
    share_amount DECIMAL(12,2) NOT NULL,
    number_of_units INTEGER NOT NULL,
    unit_price DECIMAL(10,2) NOT NULL,
    issue_date DATE NOT NULL,
    certificate_number VARCHAR(50),
    status VARCHAR(20) DEFAULT 'ACTIVE' CHECK (status IN ('ACTIVE','TRANSFERRED','REFUNDED','CANCELLED')),
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE coop_share_transactions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    member_id UUID NOT NULL REFERENCES coop_members(id),
    transaction_type VARCHAR(20) NOT NULL CHECK (transaction_type IN ('PURCHASE','REFUND','TRANSFER_IN','TRANSFER_OUT','BONUS')),
    amount DECIMAL(12,2) NOT NULL,
    units INTEGER,
    payment_reference VARCHAR(255),
    counterparty_member_id UUID REFERENCES coop_members(id),
    notes TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE coop_patronage (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    member_id UUID NOT NULL REFERENCES coop_members(id),
    fiscal_year INTEGER NOT NULL,
    period VARCHAR(20) NOT NULL,
    rides_completed INTEGER DEFAULT 0,
    fare_generated DECIMAL(12,2) DEFAULT 0,
    fare_paid DECIMAL(12,2) DEFAULT 0,
    patronage_points DECIMAL(12,2) DEFAULT 0,
    payout_amount DECIMAL(12,2) DEFAULT 0,
    status VARCHAR(20) DEFAULT 'CALCULATED' CHECK (status IN ('CALCULATED','APPROVED','PAID','HELD')),
    approved_at TIMESTAMPTZ,
    paid_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(member_id, fiscal_year, period)
);

CREATE TABLE coop_meetings (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    meeting_number VARCHAR(20),
    meeting_type VARCHAR(30) CHECK (meeting_type IN ('AGM','SGM','BOARD','EMERGENCY')),
    title VARCHAR(255) NOT NULL,
    agenda JSONB NOT NULL,
    location VARCHAR(255),
    is_virtual BOOLEAN DEFAULT FALSE,
    virtual_link VARCHAR(512),
    starts_at TIMESTAMPTZ NOT NULL,
    ends_at TIMESTAMPTZ NOT NULL,
    quorum_required INTEGER,
    quorum_achieved INTEGER DEFAULT 0,
    status VARCHAR(20) DEFAULT 'SCHEDULED',
    minutes_url VARCHAR(512),
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE coop_motions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    meeting_id UUID NOT NULL REFERENCES coop_meetings(id),
    title VARCHAR(255) NOT NULL,
    description TEXT,
    motion_type VARCHAR(30) CHECK (motion_type IN ('RESOLUTION','ELECTION','BYLAW_AMENDMENT','BUDGET')),
    voting_start_at TIMESTAMPTZ,
    voting_end_at TIMESTAMPTZ,
    status VARCHAR(20) DEFAULT 'DRAFT',
    results JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE coop_votes (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    motion_id UUID NOT NULL REFERENCES coop_motions(id),
    member_id UUID NOT NULL REFERENCES coop_members(id),
    choice VARCHAR(20) NOT NULL CHECK (choice IN ('FOR','AGAINST','ABSTAIN')),
    weight INTEGER DEFAULT 1,
    cast_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(motion_id, member_id)
);
```

---

### Safety ও Support Tables

```sql
CREATE TABLE safety_incidents (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    ride_id UUID REFERENCES rides(id),
    reported_by_user_id UUID NOT NULL REFERENCES users(id),
    incident_type VARCHAR(30) NOT NULL CHECK (incident_type IN (
        'ACCIDENT','HARASSMENT','THEFT','MEDICAL','ROUTE_DEVIATION',
        'OVERSPEEDING','LONG_STOP','SOS_PRESSED','VERBAL_ABUSE','OTHER'
    )),
    severity VARCHAR(20) DEFAULT 'MEDIUM' CHECK (severity IN ('LOW','MEDIUM','HIGH','CRITICAL')),
    description TEXT,
    evidence_urls JSONB,
    location_lat DECIMAL(10,7),
    location_lng DECIMAL(10,7),
    status VARCHAR(20) DEFAULT 'OPEN' CHECK (status IN ('OPEN','INVESTIGATING','RESOLVED','CLOSED','ESCALATED')),
    assigned_to UUID REFERENCES users(id),
    resolution_notes TEXT,
    resolved_at TIMESTAMPTZ,
    police_case_number VARCHAR(100),
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE support_tickets (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    ticket_number BIGSERIAL UNIQUE,
    user_id UUID NOT NULL REFERENCES users(id),
    ride_id UUID REFERENCES rides(id),
    category VARCHAR(50) NOT NULL,
    subcategory VARCHAR(50),
    subject VARCHAR(255) NOT NULL,
    description TEXT NOT NULL,
    priority VARCHAR(20) DEFAULT 'MEDIUM' CHECK (priority IN ('LOW','MEDIUM','HIGH','URGENT')),
    status VARCHAR(20) DEFAULT 'OPEN' CHECK (status IN ('OPEN','IN_PROGRESS','WAITING_CUSTOMER','WAITING_AGENT','RESOLVED','CLOSED')),
    assigned_to UUID REFERENCES users(id),
    sla_deadline TIMESTAMPTZ,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    resolved_at TIMESTAMPTZ
);

CREATE TABLE support_ticket_messages (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    ticket_id UUID NOT NULL REFERENCES support_tickets(id),
    sender_user_id UUID NOT NULL REFERENCES users(id),
    sender_role VARCHAR(20) NOT NULL,
    message_text TEXT NOT NULL,
    attachments JSONB,
    is_internal BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE saved_addresses (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id),
    label VARCHAR(30) CHECK (label IN ('HOME','WORK','OTHER')),
    custom_label VARCHAR(100),
    address TEXT NOT NULL,
    lat DECIMAL(10,7) NOT NULL,
    lng DECIMAL(10,7) NOT NULL,
    landmark VARCHAR(255),
    is_default BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE emergency_contacts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id),
    name VARCHAR(255) NOT NULL,
    phone VARCHAR(20) NOT NULL,
    relationship VARCHAR(100),
    is_trusted BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE audit_logs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    actor_user_id UUID REFERENCES users(id),
    actor_role VARCHAR(30),
    action VARCHAR(100) NOT NULL,
    resource_type VARCHAR(50) NOT NULL,
    resource_id UUID,
    old_values JSONB,
    new_values JSONB,
    ip_address INET,
    user_agent TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_audit_logs_actor ON audit_logs(actor_user_id);
CREATE INDEX idx_audit_logs_resource ON audit_logs(resource_type, resource_id);
CREATE INDEX idx_audit_logs_action ON audit_logs(action);
```

---

## ৩.২ সম্পূর্ণ REST API স্পেসিফিকেশন

### Base URL

```
Production: https://api.coopride.in/v1
Staging:    https://api.staging.coopride.in/v1
Dev:        https://api.dev.coopride.in/v1
```

### Standard Response Format

```json
{
  "success": true,
  "data": { ... },
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "...",
    "details": []
  },
  "meta": {
    "request_id": "req_xxx",
    "timestamp": "2026-08-28T10:30:00+05:30",
    "pagination": {
      "page": 1,
      "per_page": 20,
      "total": 150
    }
  }
}
```

---

### 🔐 Authentication APIs

```http
POST /v1/auth/otp/request
Content-Type: application/json

{
  "phone_country_code": "+91",
  "phone_number": "9876543210",
  "channel": "SMS"
}

Response:
{
  "success": true,
  "data": {
    "otp_id": "otp_xxx",
    "expires_in": 300,
    "channel": "SMS"
  }
}
```

```http
POST /v1/auth/otp/verify
{
  "otp_id": "otp_xxx",
  "otp_code": "123456",
  "device_info": {
    "device_id": "xxx",
    "platform": "android",
    "os_version": "14",
    "app_version": "1.0.0"
  }
}

Response:
{
  "success": true,
  "data": {
    "access_token": "eyJhbGciOi...",
    "refresh_token": "...",
    "expires_in": 900,
    "user": {
      "id": "user_xxx",
      "role": "RIDER",
      "full_name": "...",
      "is_onboarded": true
    }
  }
}
```

```http
POST /v1/auth/refresh
POST /v1/auth/logout
GET  /v1/auth/me
PUT  /v1/auth/me
DELETE /v1/auth/me  (GDPR/DPDP)
```

---

### 🏍️ Rider APIs

```http
POST /v1/rider/rides/estimate
{
  "pickup_lat": 22.5726,
  "pickup_lng": 88.3639,
  "drop_lat": 22.5958,
  "drop_lng": 88.2639,
  "ride_type_id": "bike",
  "promo_code": "WELCOME50"
}

Response:
{
  "data": {
    "ride_type_id": "bike",
    "distance_km": 5.2,
    "duration_min": 18,
    "base_fare": 25.0,
    "distance_charge": 52.0,
    "time_charge": 0.0,
    "night_surcharge": 0.0,
    "surge_multiplier": 1.0,
    "toll_estimate": 0.0,
    "platform_fee": 10.0,
    "gst_amount": 3.85,
    "promo_discount": 0.0,
    "fare_min": 87.0,
    "fare_max": 95.0,
    "fare_final": 90.85,
    "eta_driver_min": 4,
    "available_ride_types": [...]
  }
}
```

```http
POST /v1/rider/rides
{
  "pickup_lat": 22.5726,
  "pickup_lng": 88.3639,
  "pickup_address": "Park Street, Kolkata",
  "pickup_landmark": "Near Metro",
  "drop_lat": 22.5958,
  "drop_lng": 88.2639,
  "drop_address": "Salt Lake, Kolkata",
  "ride_type_id": "bike",
  "payment_method": "CASH",
  "promo_code": null,
  "special_instructions": "Please call before arrival"
}

Response:
{
  "data": {
    "ride_id": "ride_xxx",
    "ride_number": 10234,
    "status": "SEARCHING",
    "fare_amount": 90.85,
    "ride_otp": null
  }
}
```

```http
GET  /v1/rider/rides/{id}
POST /v1/rider/rides/{id}/cancel
POST /v1/rider/rides/{id}/rate
POST /v1/rider/rides/{id}/sos
POST /v1/rider/rides/{id}/share-link
GET  /v1/rider/rides/history
GET  /v1/rider/rides/{id}/receipt
GET  /v1/rider/rides/{id}/live-location  (WebSocket)
```

```http
GET  /v1/rider/addresses
POST /v1/rider/addresses
PUT  /v1/rider/addresses/{id}
DELETE /v1/rider/addresses/{id}

GET  /v1/rider/emergency-contacts
POST /v1/rider/emergency-contacts
PUT  /v1/rider/emergency-contacts/{id}
DELETE /v1/rider/emergency-contacts/{id}
```

---

### 🚗 Driver APIs

```http
POST /v1/driver/onboard
Content-Type: multipart/form-data

{
  "full_name": "Ramesh Kumar",
  "date_of_birth": "1990-01-15",
  "email": "ramesh@example.com",
  "preferred_language": "hi"
}
```

```http
POST /v1/driver/documents
Content-Type: multipart/form-data

{
  "document_type": "DRIVING_LICENCE",
  "document_number": "WB0720200012345",
  "issue_date": "2020-01-15",
  "expiry_date": "2040-01-14",
  "file": (binary)
}
```

```http
POST /v1/driver/online
POST /v1/driver/offline
POST /v1/driver/break

POST /v1/driver/location/batch
Content-Type: application/json

{
  "locations": [
    {
      "lat": 22.5726,
      "lng": 88.3639,
      "heading": 90.0,
      "speed_mps": 5.2,
      "accuracy_m": 12.0,
      "battery": 78,
      "network": "4G",
      "ts": "2026-08-28T10:30:00+05:30"
    }
  ]
}
```

```http
GET  /v1/driver/ride-requests/active

WebSocket subscription:
wss://ws.coopride.in/v1/driver/ride-requests

Response event:
{
  "event": "RIDE_REQUEST",
  "data": {
    "ride_id": "ride_xxx",
    "pickup_address": "Park Street",
    "drop_address": "Salt Lake",
    "distance_km": 5.2,
    "fare_amount": 90.85,
    "driver_net_amount": 77.85,
    "eta_to_pickup_min": 4,
    "expires_in_sec": 30,
    "ride_type": "bike"
  }
}
```

```http
POST /v1/driver/rides/{id}/accept
POST /v1/driver/rides/{id}/reject
POST /v1/driver/rides/{id}/arrived
POST /v1/driver/rides/{id}/verify-otp
POST /v1/driver/rides/{id}/start
POST /v1/driver/rides/{id}/complete
POST /v1/driver/rides/{id}/collect-payment
POST /v1/driver/rides/{id}/rate
POST /v1/driver/rides/{id}/sos

GET  /v1/driver/earnings/summary?period=weekly&from=2026-08-01&to=2026-08-28
GET  /v1/driver/earnings/daily?date=2026-08-28
GET  /v1/driver/earnings/rides?date=2026-08-28

GET  /v1/driver/payouts
POST /v1/driver/payouts/request
GET  /v1/driver/payouts/{id}

GET  /v1/driver/documents
PUT  /v1/driver/documents/{id}
GET  /v1/driver/documents/expiry-alerts

GET  /v1/driver/training/modules
POST /v1/driver/training/modules/{id}/complete
```

---

### 👮 Admin APIs

```http
GET  /v1/admin/dashboard
GET  /v1/admin/live-rides
GET  /v1/admin/active-drivers
GET  /v1/admin/heatmap

GET  /v1/admin/rides
GET  /v1/admin/rides/{id}
POST /v1/admin/rides/{id}/cancel
POST /v1/admin/rides/{id}/reassign
POST /v1/admin/rides/{id}/refund

GET  /v1/admin/drivers
GET  /v1/admin/drivers/{id}
POST /v1/admin/drivers/{id}/suspend
POST /v1/admin/drivers/{id}/activate
POST /v1/admin/drivers/{id}/block

GET  /v1/admin/riders
GET  /v1/admin/riders/{id}

GET  /v1/admin/kyc/pending
POST /v1/admin/kyc/{document_id}/approve
POST /v1/admin/kyc/{document_id}/reject
{
  "reason": "Document blurry, please reupload"
}

POST /v1/admin/pricing
PUT  /v1/admin/pricing/{id}

POST /v1/admin/zones
PUT  /v1/admin/zones/{id}

GET  /v1/admin/finance/revenue
GET  /v1/admin/finance/settlements
POST /v1/admin/payouts/process
POST /v1/admin/payouts/{id}/approve
GET  /v1/admin/gst/report

GET  /v1/admin/incidents
GET  /v1/admin/incidents/{id}
POST /v1/admin/incidents/{id}/assign
POST /v1/admin/incidents/{id}/resolve

GET  /v1/admin/support/tickets
GET  /v1/admin/support/tickets/{id}
POST /v1/admin/support/tickets/{id}/reply
POST /v1/admin/support/tickets/{id}/resolve

POST /v1/admin/notifications/broadcast
{
  "audience": "ALL_RIDERS",
  "title": "...",
  "body": "...",
  "channels": ["PUSH","IN_APP"]
}

GET  /v1/admin/audit-logs
```

---

### 🤝 Cooperative APIs

```http
POST /v1/coop/membership/apply
GET  /v1/coop/membership/status
GET  /v1/coop/membership/profile

POST /v1/coop/shares/purchase
{
  "number_of_units": 10,
  "payment_method": "UPI"
}

GET  /v1/coop/shares/transactions
GET  /v1/coop/shares/certificate

GET  /v1/coop/patronage/current
GET  /v1/coop/patronage/history
GET  /v1/coop/dividends/history

GET  /v1/coop/meetings
GET  /v1/coop/meetings/{id}
GET  /v1/coop/meetings/{id}/motions
POST /v1/coop/meetings/{id}/motions/{motion_id}/vote
{
  "choice": "FOR"
}
```

---

### 💳 Payment APIs

```http
POST /v1/payments/create-order
{
  "ride_id": "ride_xxx",
  "amount": 90.85,
  "method": "UPI",
  "upi_app": "phonepe"
}

Response:
{
  "order_id": "order_xxx",
  "upi_intent": "upi://pay?pa=...",
  "qr_code_url": "https://..."
}
```

```http
POST /v1/payments/webhook/razorpay
POST /v1/payments/webhook/cashfree
POST /v1/payments/webhook/upi
```

---

### 📥 File Upload APIs

```http
POST /v1/uploads/initiate
{
  "file_name": "dl.jpg",
  "content_type": "image/jpeg",
  "purpose": "DRIVING_LICENCE"
}

Response:
{
  "upload_url": "https://s3.../xxx?signature=...",
  "file_url": "https://cdn.coopride.in/xxx.jpg",
  "expires_in": 300
}
```

---

### 🔌 WebSocket Events

#### Client to Server

```json
// Location update (driver)
{
  "event": "DRIVER_LOCATION",
  "data": {
    "lat": 22.5726,
    "lng": 88.3639,
    "heading": 90,
    "speed_mps": 5.2
  }
}

// Ride status (driver)
{
  "event": "RIDE_STATUS_CHANGE",
  "data": {
    "ride_id": "ride_xxx",
    "status": "ARRIVED"
  }
}
```

#### Server to Client

```json
// Rider receives
{
  "event": "DRIVER_LOCATION_UPDATE",
  "data": {
    "ride_id": "ride_xxx",
    "driver_lat": 22.5726,
    "driver_lng": 88.3639,
    "eta_min": 3
  }
}

{
  "event": "RIDE_STATUS_CHANGE",
  "data": {
    "ride_id": "ride_xxx",
    "status": "DRIVER_ASSIGNED",
    "driver": {...}
  }
}

// Driver receives
{
  "event": "NEW_RIDE_REQUEST",
  "data": {...}
}

{
  "event": "SOS_ALERT",
  "data": {
    "ride_id": "ride_xxx",
    "message": "Passenger pressed SOS"
  }
}
```

---

# 📅 পার্ট ৪: MVP ডেভেলপমেন্টের জন্য সপ্তাহভিত্তিক টাস্ক প্ল্যান

## মোট সময়কাল: ১৬ সপ্তাহ (প্রায় ৪ মাস)

### টিম গঠন

- ১ Product Manager
- ১ UI/UX Designer
- ২ Flutter Developers
- ২ Backend Developers
- ১ QA Engineer
- ১ DevOps (part-time)
- ১ Project Manager

---

## 📅 সপ্তাহ ১: প্রজেক্ট সেটআপ ও পরিকল্পনা

### Backend

- [ ] PostgreSQL + PostGIS ইনস্টলেশন (AWS RDS Mumbai)
- [ ] Redis সেটআপ
- [ ] রিপোজিটরি তৈরি ও CI/CD (GitHub Actions)
- [ ] ডেভেলপমেন্ট এনভায়রনমেন্ট (Docker Compose)
- [ ] Basic NestJS/FastAPI project scaffold
- [ ] লগিং ও মনিটরিং সেটআপ (Prometheus, Grafana, Sentry)

### Flutter

- [ ] Monorepo সেটআপ (Melos)
- [ ] চারটি অ্যাপের skeleton (rider, driver, admin_mobile, admin_web)
- [ ] Design system package শুরু
- [ ] Core package skeleton
- [ ] API client package skeleton
- [ ] Linting, formatting, pre-commit hooks

### ডিজাইন

- [ ] Wireframes: Auth, Home, Ride Request
- [ ] Design system: colors, typography, spacing
- [ ] Component library শুরু

### DevOps

- [ ] AWS/GCP account সেটআপ
- [ ] Domain registration (coopride.in)
- [ ] SSL certificates
- [ ] CI/CD pipeline basic

---

## 📅 সপ্তাহ ২: অথেনটিকেশন ও প্রোফাইল

### Backend

- [ ] Users, rider_profiles, driver_profiles টেবিল
- [ ] JWT auth flow
- [ ] OTP request/verify (SMS gateway integration)
- [ ] DLT registration শুরু
- [ ] Login/logout/refresh APIs
- [ ] Session management

### Flutter

- [ ] Splash screen (সব অ্যাপে)
- [ ] Language selection
- [ ] Login screen (phone + OTP)
- [ ] OTP verification
- [ ] Onboarding slider
- [ ] Profile setup
- [ ] Secure storage wrapper
- [ ] Auth state management (Riverpod)

### ডিজাইন

- [ ] Auth screens complete design
- [ ] Profile screens design

### QA

- [ ] Test plan তৈরি
- [ ] Auth flow test cases

---

## 📅 সপ্তাহ ৩: ম্যাপ ও লোকেশন বেসিক

### Backend

- [ ] Cities, zones টেবিল
- [ ] PostGIS সেটআপ
- [ ] Geocoding service integration
- [ ] Places API proxy

### Flutter

- [ ] Google Maps integration (rider, driver, admin)
- [ ] Location permission handling
- [ ] Current location fetch
- [ ] Pickup selection screen
- [ ] Destination search
- [ ] Places autocomplete

### ডিজাইন

- [ ] Map screens design
- [ ] Address selection design

### QA

- [ ] Location permission test matrix (Android versions, iOS)

---

## 📅 সপ্তাহ ৪: রাইড প্রাইসিং ও এস্টিমেট

### Backend

- [ ] pricing_configs টেবিল
- [ ] ride_requests টেবিল
- [ ] Ride types seed data
- [ ] Fare calculation logic
- [ ] Distance/duration matrix API
- [ ] `/v1/rider/rides/estimate` endpoint

### Flutter

- [ ] Ride type selection screen
- [ ] Fare estimate screen
- [ ] Fare breakdown widget
- [ ] Ride type cards
- [ ] ETA display

### ডিজাইন

- [ ] Fare screens design
- [ ] Ride type cards design

---

## 📅 সপ্তাহ ৫: রাইড রিকোয়েস্ট ও ম্যাচিং বেসিক

### Backend

- [ ] rides টেবিল
- [ ] Ride lifecycle state machine
- [ ] Basic dispatch logic (nearest driver)
- [ ] Ride request creation API
- [ ] Ride status APIs
- [ ] Redis pub/sub setup

### Flutter (Rider)

- [ ] Ride confirmation screen
- [ ] Searching driver animation
- [ ] Ride status polling
- [ ] Cancel button
- [ ] Ride detail card

### Flutter (Driver)

- [ ] Online/offline toggle
- [ ] Basic home screen
- [ ] Ride request notification (local)

### QA

- [ ] Ride lifecycle test cases

---

## 📅 সপ্তাহ ৬: ড্রাইভার অনলাইন ও লোকেশন

### Backend

- [ ] Driver location API (batch)
- [ ] Location storage in Redis (TTL)
- [ ] Nearby drivers query (PostGIS)
- [ ] WebSocket connection management
- [ ] `/v1/driver/location/batch` endpoint

### Flutter (Driver)

- [ ] Foreground service setup (Android)
- [ ] Background location handling
- [ ] Location update service (every 3-5 sec)
- [ ] Online status UI
- [ ] Battery optimization handling
- [ ] Network retry logic

### Flutter (Rider)

- [ ] Nearby drivers display on map
- [ ] Driver markers
- [ ] Live driver count

### QA

- [ ] Background location test (various devices)
- [ ] Battery drain measurement

---

## 📅 সপ্তাহ ৭: ডিসপ্যাচ ও রাইড অফার

### Backend

- [ ] Full dispatch engine
- [ ] Driver scoring algorithm
- [ ] Ride offer to driver
- [ ] Accept/reject handling
- [ ] Offer timeout (30 sec)
- [ ] Retry to next driver
- [ ] WebSocket events to driver

### Flutter (Driver)

- [ ] Ride request popup/overlay
- [ ] Accept/reject buttons
- [ ] Timer countdown
- [ ] Ride details view
- [ ] Push notification for ride request

### Flutter (Rider)

- [ ] Driver assigned screen
- [ ] Driver info display (photo, name, rating, vehicle)
- [ ] Call/chat masked buttons

### QA

- [ ] Dispatch edge cases (no driver, timeout, reject)

---

## 📅 সপ্তাহ ৮: লাইভ ট্র্যাকিং

### Backend

- [ ] Real-time location broadcast
- [ ] WebSocket subscription (ride:{id}:location)
- [ ] Location smoothing
- [ ] ETA calculation

### Flutter (Rider)

- [ ] Live driver tracking on map
- [ ] Driver marker movement
- [ ] ETA display
- [ ] Route polyline
- [ ] Auto-reconnect WebSocket

### Flutter (Driver)

- [ ] Pickup navigation (Google Maps intent)
- [ ] Drop navigation
- [ ] Background location continuation

### QA

- [ ] Live tracking accuracy test
- [ ] Network switch test (4G -> WiFi -> 4G)

---

## 📅 সপ্তাহ ৯: রাইড ট্রিপ স্ট্যাটাস

### Backend

- [ ] Full ride state machine
- [ ] Status history logging
- [ ] Arrived/started/completed APIs
- [ ] Ride OTP generation
- [ ] Ride OTP verification

### Flutter (Driver)

- [ ] Arrived at pickup screen
- [ ] Wait timer
- [ ] OTP collection screen
- [ ] Trip active screen
- [ ] Complete trip button

### Flutter (Rider)

- [ ] Driver arrived notification
- [ ] OTP display
- [ ] Trip active screen
- [ ] Route display

### QA

- [ ] Full ride lifecycle test
- [ ] State machine test

---

## 📅 সপ্তাহ ১০: KYC ও ডকুমেন্ট

### Backend

- [ ] kyc_documents টেবিল
- [ ] File upload service (S3)
- [ ] Pre-signed URL generation
- [ ] Document status management
- [ ] Admin KYC queue APIs

### Flutter (Driver)

- [ ] Document upload screens
- [ ] Image picker + compression
- [ ] Upload progress
- [ ] Application status screen
- [ ] Rejection reason display

### Flutter (Admin)

- [ ] KYC queue screen
- [ ] Document viewer
- [ ] Approve/reject actions
- [ ] Reason input

### QA

- [ ] Upload test (large files, various formats)

---

## 📅 সপ্তাহ ১১: অ্যাডমিন বেসিক

### Backend

- [ ] Admin auth (2FA)
- [ ] RBAC (role-based access control)
- [ ] Admin user management
- [ ] Audit logs table
- [ ] Admin dashboard API

### Flutter (Admin Mobile)

- [ ] Admin login + 2FA
- [ ] Dashboard screen
- [ ] Live rides list
- [ ] Ride detail screen
- [ ] Driver detail screen

### Flutter (Admin Web)

- [ ] Admin login
- [ ] Dashboard layout (sidebar + content)
- [ ] Dashboard widgets
- [ ] Basic tables

### QA

- [ ] Admin auth test
- [ ] RBAC test

---

## 📅 সপ্তাহ ১২: পেমেন্ট ইন্টিগ্রেশন (UPI + Cash)

### Backend

- [ ] payments টেবিল
- [ ] ledger_accounts, ledger_entries টেবিল
- [ ] Payment gateway integration (Razorpay/Cashfree)
- [ ] Webhook handlers
- [ ] Ledger entry logic per ride
- [ ] Cash ride ledger

### Flutter (Rider)

- [ ] Payment method selection
- [ ] UPI intent flow
- [ ] QR code display
- [ ] Cash confirmation
- [ ] Payment status screen

### Flutter (Driver)

- [ ] Cash collection confirmation
- [ ] Payment status display
- [ ] Trip completion with payment

### QA

- [ ] Payment success/failure test
- [ ] Ledger balancing test
- [ ] UPI app intent test

---

## 📅 সপ্তাহ ১৩: রেটিং, রিভিউ ও হিস্ট্রি

### Backend

- [ ] Rating APIs
- [ ] Rating calculation (avg, count)
- [ ] Trip history API
- [ ] Receipt generation
- [ ] GST invoice PDF generation

### Flutter (Rider)

- [ ] Rating screen
- [ ] Tips (optional)
- [ ] Feedback text
- [ ] Trip history screen
- [ ] Receipt screen
- [ ] Download PDF

### Flutter (Driver)

- [ ] Rate passenger screen
- [ ] Earnings summary
- [ ] Trip history

### QA

- [ ] Rating flow test
- [ ] Receipt accuracy test

---

## 📅 সপ্তাহ ১৪: সেফটি ফিচার

### Backend

- [ ] safety_incidents টেবিল
- [ ] emergency_contacts টেবিল
- [ ] saved_addresses টেবিল
- [ ] SOS endpoint
- [ ] Trip share link API
- [ ] Route deviation detection
- [ ] Long stop detection

### Flutter (Rider)

- [ ] SOS button (always visible)
- [ ] SOS screen
- [ ] Emergency contacts screen
- [ ] Trip share screen
- [ ] Incident report form

### Flutter (Driver)

- [ ] SOS button
- [ ] Incident report

### Flutter (Admin)

- [ ] SOS alerts dashboard
- [ ] Incident detail screen
- [ ] Assign/resolution

### QA

- [ ] SOS test
- [ ] Route deviation test

---

## 📅 সপ্তাহ ১৫: সাপোর্ট, নোটিফিকেশন, পলিশ

### Backend

- [ ] support_tickets, messages টেবিল
- [ ] Ticket management APIs
- [ ] Push notification service
- [ ] FCM/APNs integration
- [ ] Notification templates
- [ ] Email notifications

### Flutter (সব অ্যাপ)

- [ ] Support ticket creation
- [ ] Ticket history
- [ ] Push notification handling
- [ ] In-app notification center
- [ ] UI polish
- [ ] Animations
- [ ] Error states
- [ ] Loading states

### QA

- [ ] Notification delivery test
- [ ] Support flow test

---

## 📅 সপ্তাহ ১৬: টেস্টিং, পলিশ ও পাইলট প্রস্তুতি

### Backend

- [ ] Performance optimization
- [ ] Load testing
- [ ] Security audit
- [ ] Final documentation
- [ ] Production environment setup

### Flutter

- [ ] End-to-end testing
- [ ] Performance profiling
- [ ] Battery optimization final check
- [ ] Crash reporting verification
- [ ] Analytics verification
- [ ] Play Store / App Store assets
- [ ] Screenshots, descriptions
- [ ] Release builds

### QA

- [ ] Full regression test
- [ ] Pilot scenario test
- [ ] Field testing (real devices, real network)

### Operations

- [ ] Driver recruitment process
- [ ] Training materials
- [ ] Support team training
- [ ] Pilot area finalization
- [ ] Marketing materials

---

## 📅 সপ্তাহ ১৭-২০: পাইলট (৪ সপ্তাহ)

### সপ্তাহ ১৭

- ২০ ড্রাইভার onboard
- ১০০ যাত্রী invite
- ছোট নির্দিষ্ট এলাকা
- ২৪/৭ support

### সপ্তাহ ১৮

- বাগ ফিক্স
- ফিডব্যাক কালেকশন
- পারফরম্যান্স tuning
- ৫০ ড্রাইভার

### সপ্তাহ ১৯

- ৫০০ যাত্রী
- নতুন ফিচার request analysis
- অপারেশনাল learning

### সপ্তাহ ২০

- Pilot retrospective
- Decision: continue / pivot / expand
- Phase 2 planning

---

## প্রতি সপ্তাহের রুটিন

### সোমবার

- Sprint planning (১ ঘণ্টা)
- গত সপ্তাহের review
- চলতি সপ্তাহের tasks

### প্রতিদিন

- Daily standup (১৫ মিনিট)
- Async updates Slack-এ
- Code review

### শুক্রবার

- Sprint demo
- Retrospective
- Documentation update

---

## প্রতি সপ্তাহের ডেলিভারেবল

| সপ্তাহ | ডেলিভারেবল |
|---|---|
| ১ | Working environment, skeleton apps |
| ২ | Auth flow complete |
| ৩ | Map integration |
| ৪ | Fare estimate working |
| ৫ | Ride request flow |
| ৬ | Driver online/location |
| ৭ | Ride assignment |
| ৮ | Live tracking |
| ৯ | Trip lifecycle |
| ১০ | KYC upload |
| ১১ | Admin basic |
| ১২ | Payments |
| ১৩ | Rating/History |
| ১৪ | Safety features |
| ১৫ | Support/Notifications |
| ১৬ | Production-ready MVP |
| ১৭-২০ | Pilot execution |

---

## 📊 মাইলস্টোন ও KPI

### মাইলস্টোন ১ (সপ্তাহ ৪ শেষে)

- Auth complete
- Map working
- Fare estimate working
- **Demo: একটি সম্পূর্ণ fare estimate দেখানো**

### মাইলস্টোন ২ (সপ্তাহ ৯ শেষে)

- Ride lifecycle complete
- Live tracking working
- **Demo: একটি সম্পূর্ণ রাইড**

### মাইলস্টোন ৩ (সপ্তাহ ১৬ শেষে)

- Production-ready MVP
- All features tested
- **Demo: Full app ready for pilot**

### মাইলস্টোন ৪ (সপ্তাহ ২০ শেষে)

- Successful pilot
- ১,০০০+ রাইড সম্পন্ন
- ৫০+ active drivers
- Positive feedback
- **Decision point: Scale or pivot**

---

## ⚠️ সাধারণ ঝুঁকি ও প্রশমন

| ঝুঁকি | সম্ভাবনা | প্রভাব | প্রশমন |
|---|---:|---:|---|
| Background location Android-এ কঠিন | High | High | সপ্তাহ ৩ থেকেই test শুরু |
| Google Maps খরচ বেশি | Medium | High | Caching, optimize from day 1 |
| DLT SMS approval delay | Medium | Medium | আগেই apply, backup voice OTP |
| Payment gateway approval | Low | High | Multiple gateway options |
| iOS background location rejection | Medium | Medium | Clear justification in App Store |
| Driver adoption slow | High | High | Incentives, local community |
| Safety incident during pilot | Low | Very High | Strong SOS, 24/7 support |

---

## 🎯 চূড়ান্ত পরামর্শ

১. **প্রতি সপ্তাহের শেষে একটি working demo থাকতে হবে।**  
২. **ড্রাইভার অ্যাপে ব্যাকগ্রাউন্ড লোকেশন সবচেয়ে কঠিন—প্রথম দিকেই focus দিন।**  
৩. **ডেটাবেস ডিজাইন পরিবর্তন করা কঠিন, তাই প্রথমেই ঠিক করে নিন।**  
৪. **ভারতীয় কনটেক্সটে DLT, UPI, GST compliance আগে থেকে plan করুন।**  
৫. **Pilot-এর আগে production-grade security ও monitoring সেটআপ করুন।**  
৬. **Documentation শুরু থেকেই লিখুন—পরে লেখা কঠিন।**  
৭. **Test automation শুরু থেকেই build করুন।**  
৮. **ছোট টিম, ছোট scope, দ্রুত iterate করুন।**

চাইলে আমি আরও গভীরভাবে কোনো একটি অংশ নিয়ে কাজ করতে পারি, যেমন:
- সম্পূর্ণ OpenAPI/Swagger specification
- Flutter-এর জন্য detailed code examples (specific screens)
- Backend-এর NestJS/FastAPI implementation skeleton
- CI/CD pipeline configuration
- Database migrations (full SQL)
- Testing strategy documentation


নিচে একবারে **সবগুলো ডেলিভারেবল** দিচ্ছি:

1. সম্পূর্ণ এপিআই স্পেসিফিকেশন / OpenAPI কাঠামো  
2. ফ্লাটার বিস্তারিত কোড স্টার্টার  
3. ব্যাকএন্ড স্কেলেটন: NestJS + Prisma + Redis + WebSocket  
4. CI/CD পাইপলাইন  
5. সম্পূর্ণ ডাটাবেস মাইগ্রেশন SQL  
6. টেস্টিং স্ট্র্যাটেজি ডকুমেন্টেশন  

---

# ১. সম্পূর্ণ এপিআই স্পেসিফিকেশন — OpenAPI কাঠামো

এটি একটি প্রোডাকশন-রেডি বেস স্পেক। প্রয়োজন অনুযায়ী আরও এন্ডপয়েন্ট যোগ করবেন।

```yaml
openapi: 3.1.0
info:
  title: CoopRide India API
  version: 1.0.0
  description: |
    Cooperative ride-sharing platform API for India.
    Supports Rider, Driver, Admin, Finance, Safety and Cooperative modules.
servers:
  - url: https://api.coopride.in/v1
    description: Production
  - url: https://api.staging.coopride.in/v1
    description: Staging
  - url: https://api.dev.coopride.in/v1
    description: Development

security:
  - bearerAuth: []

tags:
  - name: Auth
  - name: Rider
  - name: Driver
  - name: Admin
  - name: Payments
  - name: Cooperative
  - name: Safety
  - name: Support
  - name: Uploads

paths:
  /auth/otp/request:
    post:
      tags: [Auth]
      summary: Send OTP
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [phone_country_code, phone_number]
              properties:
                phone_country_code:
                  type: string
                  example: "+91"
                phone_number:
                  type: string
                  example: "9876543210"
                channel:
                  type: string
                  enum: [SMS, VOICE]
                  default: SMS
      responses:
        '200':
          description: OTP sent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
        '429':
          description: Too many requests

  /auth/otp/verify:
    post:
      tags: [Auth]
      summary: Verify OTP
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [otp_id, otp_code]
              properties:
                otp_id:
                  type: string
                otp_code:
                  type: string
                  minLength: 6
                  maxLength: 6
                device_info:
                  type: object
                  properties:
                    device_id:
                      type: string
                    platform:
                      type: string
                      enum: [android, ios, web]
                    os_version:
                      type: string
                    app_version:
                      type: string
      responses:
        '200':
          description: Tokens returned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'

  /auth/refresh:
    post:
      tags: [Auth]
      summary: Refresh access token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [refresh_token]
              properties:
                refresh_token:
                  type: string
      responses:
        '200':
          description: New tokens
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'

  /auth/me:
    get:
      tags: [Auth]
      summary: Get current user
      responses:
        '200':
          description: User object
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'

  /rider/rides/estimate:
    post:
      tags: [Rider]
      summary: Fare estimate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - pickup_lat
                - pickup_lng
                - drop_lat
                - drop_lng
                - ride_type_id
              properties:
                pickup_lat:
                  type: number
                  format: double
                pickup_lng:
                  type: number
                  format: double
                drop_lat:
                  type: number
                  format: double
                drop_lng:
                  type: number
                  format: double
                ride_type_id:
                  type: string
                  example: bike
                promo_code:
                  type: string
                  nullable: true
      responses:
        '200':
          description: Fare estimate
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/FareEstimate'

  /rider/rides:
    post:
      tags: [Rider]
      summary: Request a ride
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRideRequest'
      responses:
        '201':
          description: Ride created
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/Ride'

  /rider/rides/{id}:
    get:
      tags: [Rider]
      summary: Get ride details
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Ride details
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/Ride'

  /rider/rides/{id}/cancel:
    post:
      tags: [Rider]
      summary: Cancel ride
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
      responses:
        '200':
          description: Ride cancelled

  /rider/rides/{id}/rate:
    post:
      tags: [Rider]
      summary: Rate driver
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [rating]
              properties:
                rating:
                  type: integer
                  minimum: 1
                  maximum: 5
                comment:
                  type: string
      responses:
        '200':
          description: Rating submitted

  /rider/rides/{id}/sos:
    post:
      tags: [Safety]
      summary: Trigger SOS
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SOS created

  /driver/onboard:
    post:
      tags: [Driver]
      summary: Driver onboarding
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DriverOnboardRequest'
      responses:
        '201':
          description: Application created

  /driver/documents:
    post:
      tags: [Driver]
      summary: Upload KYC document metadata
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [document_type, file_url]
              properties:
                document_type:
                  type: string
                  enum:
                    - DRIVING_LICENCE
                    - VEHICLE_RC
                    - INSURANCE
                    - FITNESS_CERTIFICATE
                    - PERMIT
                    - PUC
                    - ADDRESS_PROOF
                    - PAN
                    - BANK_PROOF
                    - PHOTO
                    - POLICE_VERIFICATION
                document_number_masked:
                  type: string
                issue_date:
                  type: string
                  format: date
                expiry_date:
                  type: string
                  format: date
                file_url:
                  type: string
      responses:
        '201':
          description: Document submitted

  /driver/online:
    post:
      tags: [Driver]
      summary: Go online
      responses:
        '200':
          description: Driver online

  /driver/offline:
    post:
      tags: [Driver]
      summary: Go offline
      responses:
        '200':
          description: Driver offline

  /driver/location/batch:
    post:
      tags: [Driver]
      summary: Batch location update
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [locations]
              properties:
                locations:
                  type: array
                  items:
                    type: object
                    required: [lat, lng, ts]
                    properties:
                      lat:
                        type: number
                      lng:
                        type: number
                      heading:
                        type: number
                      speed_mps:
                        type: number
                      accuracy_m:
                        type: number
                      battery:
                        type: integer
                      network:
                        type: string
                      ts:
                        type: string
                        format: date-time
      responses:
        '202':
          description: Accepted

  /driver/rides/{id}/accept:
    post:
      tags: [Driver]
      summary: Accept ride
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Accepted

  /driver/rides/{id}/reject:
    post:
      tags: [Driver]
      summary: Reject ride
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Rejected

  /driver/rides/{id}/arrived:
    post:
      tags: [Driver]
      summary: Arrived at pickup
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Arrived

  /driver/rides/{id}/verify-otp:
    post:
      tags: [Driver]
      summary: Verify ride OTP
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [otp]
              properties:
                otp:
                  type: string
      responses:
        '200':
          description: OTP verified

  /driver/rides/{id}/start:
    post:
      tags: [Driver]
      summary: Start trip
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Trip started

  /driver/rides/{id}/complete:
    post:
      tags: [Driver]
      summary: Complete trip
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                actual_distance_km:
                  type: number
                actual_duration_min:
                  type: integer
                toll_charges:
                  type: number
      responses:
        '200':
          description: Trip completed

  /payments/create-order:
    post:
      tags: [Payments]
      summary: Create payment order
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ride_id, amount, method]
              properties:
                ride_id:
                  type: string
                amount:
                  type: number
                method:
                  type: string
                  enum: [CASH, UPI, CARD, NETBANKING, WALLET]
                provider:
                  type: string
                  enum: [razorpay, cashfree, paytm]
      responses:
        '201':
          description: Payment order created

  /payments/webhook/razorpay:
    post:
      tags: [Payments]
      summary: Razorpay webhook
      security: []
      responses:
        '200':
          description: OK

  /payments/webhook/cashfree:
    post:
      tags: [Payments]
      summary: Cashfree webhook
      security: []
      responses:
        '200':
          description: OK

  /coop/membership/apply:
    post:
      tags: [Cooperative]
      summary: Apply for cooperative membership
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                member_type:
                  type: string
                  enum: [DRIVER, RIDER, WORKER, INVESTOR]
                nominee_name:
                  type: string
                nominee_relation:
                  type: string
                nominee_phone:
                  type: string
      responses:
        '201':
          description: Application submitted

  /coop/shares/purchase:
    post:
      tags: [Cooperative]
      summary: Purchase shares
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [number_of_units]
              properties:
                number_of_units:
                  type: integer
                payment_method:
                  type: string
                  enum: [UPI, CASH, BANK_TRANSFER]
      responses:
        '201':
          description: Share purchase initiated

  /admin/dashboard:
    get:
      tags: [Admin]
      summary: Admin dashboard
      responses:
        '200':
          description: Dashboard data

  /admin/rides:
    get:
      tags: [Admin]
      summary: List rides
      parameters:
        - name: status
          in: query
          schema:
            type: string
        - name: city_id
          in: query
          schema:
            type: string
        - name: from
          in: query
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          schema:
            type: string
            format: date-time
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: per_page
          in: query
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Paginated rides

  /admin/rides/{id}/cancel:
    post:
      tags: [Admin]
      summary: Cancel ride by admin
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason:
                  type: string
      responses:
        '200':
          description: Ride cancelled

  /admin/kyc/{documentId}/approve:
    post:
      tags: [Admin]
      summary: Approve KYC document
      parameters:
        - name: documentId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Approved

  /admin/kyc/{documentId}/reject:
    post:
      tags: [Admin]
      summary: Reject KYC document
      parameters:
        - name: documentId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason:
                  type: string
      responses:
        '200':
          description: Rejected

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    ApiResponse:
      type: object
      properties:
        success:
          type: boolean
        data:
          type: object
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
            details:
              type: array
              items:
                type: object

    AuthResponse:
      type: object
      properties:
        success:
          type: boolean
        data:
          type: object
          properties:
            access_token:
              type: string
            refresh_token:
              type: string
            expires_in:
              type: integer
            user:
              $ref: '#/components/schemas/User'

    User:
      type: object
      properties:
        id:
          type: string
        phone_country_code:
          type: string
        phone_number:
          type: string
        full_name:
          type: string
        role:
          type: string
          enum: [RIDER, DRIVER, ADMIN, SUPPORT, FINANCE, SAFETY, BOARD, SUPER_ADMIN]
        status:
          type: string
        preferred_language:
          type: string
        created_at:
          type: string
          format: date-time

    CreateRideRequest:
      type: object
      required:
        - pickup_lat
        - pickup_lng
        - drop_lat
        - drop_lng
        - ride_type_id
      properties:
        pickup_lat:
          type: number
        pickup_lng:
          type: number
        pickup_address:
          type: string
        pickup_landmark:
          type: string
        drop_lat:
          type: number
        drop_lng:
          type: number
        drop_address:
          type: string
        drop_landmark:
          type: string
        ride_type_id:
          type: string
        payment_method:
          type: string
          enum: [CASH, UPI, CARD, NETBANKING, WALLET]
        promo_code:
          type: string
          nullable: true
        special_instructions:
          type: string

    FareEstimate:
      type: object
      properties:
        ride_type_id:
          type: string
        distance_km:
          type: number
        duration_min:
          type: integer
        base_fare:
          type: number
        distance_charge:
          type: number
        time_charge:
          type: number
        night_surcharge:
          type: number
        surge_multiplier:
          type: number
        toll_estimate:
          type: number
        platform_fee:
          type: number
        gst_amount:
          type: number
        promo_discount:
          type: number
        fare_min:
          type: number
        fare_max:
          type: number
        fare_final:
          type: number
        eta_driver_min:
          type: integer

    Ride:
      type: object
      properties:
        id:
          type: string
        ride_number:
          type: integer
        rider_id:
          type: string
        driver_id:
          type: string
          nullable: true
        status:
          type: string
        pickup_lat:
          type: number
        pickup_lng:
          type: number
        drop_lat:
          type: number
        drop_lng:
          type: number
        fare_amount:
          type: number
        platform_fee:
          type: number
        gst_amount:
          type: number
        driver_net_amount:
          type: number
        payment_method:
          type: string
        payment_status:
          type: string
        rating_by_rider:
          type: integer
          nullable: true
        rating_by_driver:
          type: integer
          nullable: true
        created_at:
          type: string
          format: date-time

    DriverOnboardRequest:
      type: object
      required:
        - full_name
        - date_of_birth
      properties:
        full_name:
          type: string
        date_of_birth:
          type: string
          format: date
        email:
          type: string
        preferred_language:
          type: string
        vehicle_type:
          type: string
          enum: [BIKE, AUTO, CAR_MINI, CAR_SEDAN, CAR_SUV, CAR_PREMIUM]
```

---

# ২. ফ্লাটার বিস্তারিত কোড স্টার্টার

## ২.১ `packages/core/lib/src/config/env.dart`

```dart
import 'dart:convert';
import 'package:flutter/services.dart' show rootBundle;

class Env {
  static late String environment;
  static late String apiBaseUrl;
  static late String websocketUrl;
  static late String googleMapsApiKey;
  static late String sentryDsn;
  static late String appVersion;
  static late bool enableCoopFeatures;
  static late bool enableUpi;
  static late bool enableCash;

  static Future<void> init() async {
    const envName = String.fromEnvironment('ENV', defaultValue: 'dev');
    environment = envName;

    final config = await rootBundle.loadString('assets/config/$envName.json');
    final json = jsonDecode(config) as Map<String, dynamic>;

    apiBaseUrl = json['api_base_url'] as String;
    websocketUrl = json['websocket_url'] as String;
    googleMapsApiKey = json['google_maps_api_key'] as String;
    sentryDsn = json['sentry_dsn'] as String? ?? '';
    appVersion = json['app_version'] as String? ?? '0.1.0';
    enableCoopFeatures = json['enable_coop_features'] as bool? ?? false;
    enableUpi = json['enable_upi'] as bool? ?? false;
    enableCash = json['enable_cash'] as bool? ?? true;
  }

  static bool get isProd => environment == 'prod';
  static bool get isStaging => environment == 'staging';
  static bool get isDev => environment == 'dev';
}
```

---

## ২.২ `packages/core/lib/src/config/app_role.dart`

```dart
enum AppRole {
  rider,
  driver,
  adminMobile,
  adminWeb;

  bool get isRider => this == AppRole.rider;
  bool get isDriver => this == AppRole.driver;
  bool get isAdmin => this == AppRole.adminMobile || this == AppRole.adminWeb;
  bool get isWebAdmin => this == AppRole.adminWeb;
}
```

---

## ২.৩ `packages/core/lib/src/network/http_client.dart`

```dart
import 'package:dio/dio.dart';
import '../config/env.dart';
import '../storage/secure_storage.dart';

class HttpClient {
  late final Dio dio;

  HttpClient() {
    dio = Dio(
      BaseOptions(
        baseUrl: Env.apiBaseUrl,
        connectTimeout: const Duration(seconds: 20),
        receiveTimeout: const Duration(seconds: 30),
        headers: {
          'Accept': 'application/json',
          'X-Client': 'flutter',
          'X-App-Version': Env.appVersion,
        },
      ),
    );

    dio.interceptors.add(
      InterceptorsWrapper(
        onRequest: (options, handler) async {
          final token = await SecureStorage.getAccessToken();
          if (token != null) {
            options.headers['Authorization'] = 'Bearer $token';
          }
          handler.next(options);
        },
        onError: (error, handler) async {
          if (error.response?.statusCode == 401) {
            final refreshed = await _tryRefreshToken();
            if (refreshed) {
              final retryRequest = await dio.fetch(error.requestOptions);
              return handler.resolve(retryRequest);
            }
          }
          handler.next(error);
        },
      ),
    );
  }

  Future<bool> _tryRefreshToken() async {
    try {
      final refreshToken = await SecureStorage.getRefreshToken();
      if (refreshToken == null) return false;

      final response = await Dio().post(
        '${Env.apiBaseUrl}/auth/refresh',
        data: {'refresh_token': refreshToken},
      );

      final accessToken = response.data['data']['access_token'] as String;
      final newRefreshToken = response.data['data']['refresh_token'] as String;

      await SecureStorage.saveAccessToken(accessToken);
      await SecureStorage.saveRefreshToken(newRefreshToken);
      return true;
    } catch (_) {
      return false;
    }
  }
}
```

---

## ২.৪ `packages/core/lib/src/storage/secure_storage.dart`

```dart
import 'package:flutter_secure_storage/flutter_secure_storage.dart';

class SecureStorage {
  static const _storage = FlutterSecureStorage(
    aOptions: AndroidOptions(encryptedSharedPreferences: true),
  );

  static const _accessTokenKey = 'access_token';
  static const _refreshTokenKey = 'refresh_token';

  static Future<void> saveAccessToken(String token) async {
    await _storage.write(key: _accessTokenKey, value: token);
  }

  static Future<void> saveRefreshToken(String token) async {
    await _storage.write(key: _refreshTokenKey, value: token);
  }

  static Future<String?> getAccessToken() async {
    return _storage.read(key: _accessTokenKey);
  }

  static Future<String?> getRefreshToken() async {
    return _storage.read(key: _refreshTokenKey);
  }

  static Future<void> clearAuth() async {
    await _storage.delete(key: _accessTokenKey);
    await _storage.delete(key: _refreshTokenKey);
  }
}
```

---

## ২.৫ `packages/core/lib/src/bootstrap/bootstrap.dart`

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:sentry_flutter/sentry_flutter.dart';

import '../config/app_role.dart';
import '../config/env.dart';
import '../network/http_client.dart';
import 'providers.dart';

Future<void> bootstrap({
  required AppRole appRole,
  required Widget Function() builder,
}) async {
  WidgetsFlutterBinding.ensureInitialized();

  await Env.init();

  await SentryFlutter.init(
    (options) {
      options.dsn = Env.sentryDsn;
      options.environment = Env.environment;
      options.tracesSampleRate = Env.isProd ? 0.2 : 1.0;
    },
    appRunner: () async {
      final httpClient = HttpClient();

      runApp(
        ProviderScope(
          overrides: [
            appRoleProvider.overrideWithValue(appRole),
            httpClientProvider.overrideWithValue(httpClient),
          ],
          child: builder(),
        ),
      );
    },
  );
}
```

---

## ২.৬ `packages/core/lib/src/bootstrap/providers.dart`

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../config/app_role.dart';
import '../network/http_client.dart';

final appRoleProvider = Provider<AppRole>((ref) {
  throw UnimplementedError('appRoleProvider must be overridden');
});

final httpClientProvider = Provider<HttpClient>((ref) {
  throw UnimplementedError('httpClientProvider must be overridden');
});
```

---

## ২.৭ `apps/rider/lib/main_prod.dart`

```dart
import 'package:core/core.dart';
import 'app/rider_app.dart';

void main() {
  bootstrap(
    appRole: AppRole.rider,
    builder: () => const RiderApp(),
  );
}
```

---

## ২.৮ `apps/rider/lib/app/rider_app.dart`

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_localizations/flutter_localizations.dart';
import 'package:design_system/design_system.dart';

import 'rider_router.dart';

class RiderApp extends ConsumerWidget {
  const RiderApp({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final router = ref.watch(riderRouterProvider);

    return MaterialApp.router(
      title: 'CoopRide Rider',
      debugShowCheckedModeBanner: false,
      theme: AppTheme.light(),
      darkTheme: AppTheme.dark(),
      routerConfig: router,
      localizationsDelegates: const [
        GlobalMaterialLocalizations.delegate,
        GlobalWidgetsLocalizations.delegate,
        GlobalCupertinoLocalizations.delegate,
      ],
      supportedLocales: const [
        Locale('en'),
        Locale('hi'),
        Locale('bn'),
      ],
    );
  }
}
```

---

## ২.৯ `apps/rider/lib/app/rider_router.dart`

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';

import 'package:features/auth_feature.dart';
import 'package:features/rider_feature.dart';

final riderRouterProvider = Provider<GoRouter>((ref) {
  return GoRouter(
    initialLocation: '/',
    routes: [
      GoRoute(
        path: '/',
        builder: (context, state) => const RiderHomeScreen(),
        routes: [
          GoRoute(
            path: 'ride/:rideId',
            builder: (context, state) => RideTrackingScreen(
              rideId: state.pathParameters['rideId']!,
            ),
          ),
          GoRoute(
            path: 'history',
            builder: (context, state) => const TripHistoryScreen(),
          ),
        ],
      ),
      GoRoute(
        path: '/auth/login',
        builder: (context, state) => const LoginScreen(),
      ),
      GoRoute(
        path: '/auth/otp',
        builder: (context, state) => const OtpScreen(),
      ),
    ],
  );
});
```

---

## ২.১০ `packages/features/rider/lib/src/home/rider_home_controller.dart`

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:freezed_annotation/freezed_annotation.dart';

part 'rider_home_controller.freezed.dart';
part 'rider_home_controller.g.dart';

@freezed
class RiderHomeState with _$RiderHomeState {
  const factory RiderHomeState({
    @Default(null) double? pickupLat,
    @Default(null) double? pickupLng,
    @Default(null) double? dropLat,
    @Default(null) double? dropLng,
    @Default(null) String? pickupAddress,
    @Default(null) String? dropAddress,
    @Default('bike') String rideTypeId,
    @Default(null) FareEstimate? estimate,
    @Default(false) bool isLoading,
    @Default(null) String? error,
  }) = _RiderHomeState;
}

@riverpod
class RiderHomeController extends _$RiderHomeController {
  @override
  RiderHomeState build() => const RiderHomeState();

  void setPickup({
    required double lat,
    required double lng,
    required String address,
  }) {
    state = state.copyWith(
      pickupLat: lat,
      pickupLng: lng,
      pickupAddress: address,
    );
    _fetchEstimate();
  }

  void setDrop({
    required double lat,
    required double lng,
    required String address,
  }) {
    state = state.copyWith(
      dropLat: lat,
      dropLng: lng,
      dropAddress: address,
    );
    _fetchEstimate();
  }

  void selectRideType(String rideTypeId) {
    state = state.copyWith(rideTypeId: rideTypeId);
    _fetchEstimate();
  }

  Future<void> _fetchEstimate() async {
    if (state.pickupLat == null || state.dropLat == null) return;

    state = state.copyWith(isLoading: true, error: null);

    try {
      final rideService = ref.read(rideServiceProvider);
      final estimate = await rideService.getEstimate(
        pickupLat: state.pickupLat!,
        pickupLng: state.pickupLng!,
        dropLat: state.dropLat!,
        dropLng: state.dropLng!,
        rideTypeId: state.rideTypeId,
      );

      state = state.copyWith(
        estimate: estimate,
        isLoading: false,
      );
    } catch (e) {
      state = state.copyWith(
        error: e.toString(),
        isLoading: false,
      );
    }
  }

  Future<String> requestRide() async {
    final rideService = ref.read(rideServiceProvider);
    final ride = await rideService.createRide(
      pickupLat: state.pickupLat!,
      pickupLng: state.pickupLng!,
      dropLat: state.dropLat!,
      dropLng: state.dropLng!,
      pickupAddress: state.pickupAddress ?? '',
      dropAddress: state.dropAddress ?? '',
      rideTypeId: state.rideTypeId,
      paymentMethod: 'CASH',
    );

    return ride.id;
  }
}

class FareEstimate {
  final String rideTypeId;
  final double fareFinal;
  final double distanceKm;
  final int durationMin;

  FareEstimate({
    required this.rideTypeId,
    required this.fareFinal,
    required this.distanceKm,
    required this.durationMin,
  });

  factory FareEstimate.fromJson(Map<String, dynamic> json) {
    return FareEstimate(
      rideTypeId: json['ride_type_id'] as String,
      fareFinal: (json['fare_final'] as num).toDouble(),
      distanceKm: (json['distance_km'] as num).toDouble(),
      durationMin: json['duration_min'] as int,
    );
  }
}
```

---

## ২.১১ `packages/features/driver/lib/src/location/driver_location_service.dart`

```dart
import 'dart:async';
import 'package:geolocator/geolocator.dart';
import 'package:core/core.dart';

class DriverLocationService {
  final HttpClient _httpClient;
  StreamSubscription<Position>? _positionSubscription;
  final List<Map<String, dynamic>> _locationQueue = [];
  Timer? _flushTimer;

  DriverLocationService(this._httpClient);

  Future<void> start() async {
    bool serviceEnabled = await Geolocator.isLocationServiceEnabled();
    if (!serviceEnabled) {
      await Geolocator.openLocationSettings();
      return;
    }

    LocationPermission permission = await Geolocator.checkPermission();
    if (permission == LocationPermission.denied) {
      permission = await Geolocator.requestPermission();
    }

    if (permission == LocationPermission.deniedForever) {
      await Geolocator.openAppSettings();
      return;
    }

    _positionSubscription = Geolocator.getPositionStream(
      locationSettings: const LocationSettings(
        accuracy: LocationAccuracy.high,
        distanceFilter: 5,
      ),
    ).listen(
      (position) {
        _locationQueue.add({
          'lat': position.latitude,
          'lng': position.longitude,
          'heading': position.heading,
          'speed_mps': position.speed,
          'accuracy_m': position.accuracy,
          'ts': DateTime.now().toIso8601String(),
        });

        if (_locationQueue.length >= 5) {
          flush();
        }
      },
      onError: (error) {
        // log error
      },
    );

    _flushTimer = Timer.periodic(const Duration(seconds: 10), (_) {
      flush();
    });
  }

  Future<void> flush() async {
    if (_locationQueue.isEmpty) return;

    final batch = List<Map<String, dynamic>>.from(_locationQueue);
    _locationQueue.clear();

    try {
      await _httpClient.dio.post(
        '/driver/location/batch',
        data: {'locations': batch},
      );
    } catch (e) {
      _locationQueue.insertAll(0, batch);
    }
  }

  Future<void> stop() async {
    await _positionSubscription?.cancel();
    _flushTimer?.cancel();
    await flush();
  }
}
```

---

## ২.১২ `packages/features/admin/lib/src/dashboard/admin_dashboard_screen.dart`

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

class AdminDashboardScreen extends ConsumerWidget {
  const AdminDashboardScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Admin Dashboard'),
      ),
      body: ListView(
        padding: const EdgeInsets.all(16),
        children: const [
          Row(
            children: [
              Expanded(child: _StatCard(title: 'Active Rides', value: '0')),
              SizedBox(width: 12),
              Expanded(child: _StatCard(title: 'Online Drivers', value: '0')),
            ],
          ),
          SizedBox(height: 12),
          Row(
            children: [
              Expanded(child: _StatCard(title: 'Today Revenue', value: '₹0')),
              SizedBox(width: 12),
              Expanded(child: _StatCard(title: 'SOS Alerts', value: '0')),
            ],
          ),
          SizedBox(height: 24),
          _PendingKycCard(),
          SizedBox(height: 12),
          _IncidentCard(),
        ],
      ),
    );
  }
}

class _StatCard extends StatelessWidget {
  final String title;
  final String value;

  const _StatCard({required this.title, required this.value});

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Padding(
        padding: const EdgeInsets.all(16),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Text(title),
            const SizedBox(height: 8),
            Text(
              value,
              style: Theme.of(context).textTheme.headlineSmall,
            ),
          ],
        ),
      ),
    );
  }
}

class _PendingKycCard extends StatelessWidget {
  const _PendingKycCard();

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: ListTile(
        title: Text('Pending KYC'),
        subtitle: Text('Review driver documents'),
        trailing: Icon(Icons.chevron_right),
      ),
    );
  }
}

class _IncidentCard extends StatelessWidget {
  const _IncidentCard();

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: ListTile(
        title: Text('Safety Incidents'),
        subtitle: Text('Open incidents and SOS alerts'),
        trailing: Icon(Icons.chevron_right),
      ),
    );
  }
}
```

---

# ৩. ব্যাকএন্ড স্কেলেটন — NestJS + Prisma + Redis + WebSocket

## ৩.১ প্রজেক্ট স্ট্রাকচার

```text
backend/
├── src/
│   ├── main.ts
│   ├── app.module.ts
│   ├── common/
│   │   ├── guards/
│   │   │   ├── jwt-auth.guard.ts
│   │   │   └── roles.guard.ts
│   │   ├── decorators/
│   │   │   └── roles.decorator.ts
│   │   ├── interceptors/
│   │   ├── filters/
│   │   └── dto/
│   ├── auth/
│   │   ├── auth.module.ts
│   │   ├── auth.controller.ts
│   │   ├── auth.service.ts
│   │   ├── jwt.strategy.ts
│   │   └── dto/
│   ├── users/
│   ├── rider/
│   ├── driver/
│   ├── rides/
│   │   ├── rides.module.ts
│   │   ├── rides.controller.ts
│   │   ├── rides.service.ts
│   │   └── dto/
│   ├── dispatch/
│   │   ├── dispatch.module.ts
│   │   ├── dispatch.service.ts
│   │   └── realtime.gateway.ts
│   ├── pricing/
│   ├── payments/
│   ├── finance/
│   ├── coop/
│   ├── safety/
│   ├── support/
│   ├── admin/
│   └── prisma/
│       ├── prisma.module.ts
│       └── prisma.service.ts
├── prisma/
│   ├── schema.prisma
│   └── migrations/
├── test/
├── Dockerfile
├── docker-compose.yml
├── package.json
├── tsconfig.json
└── .env.example
```

---

## ৩.২ `src/main.ts`

```typescript
import { NestFactory } from '@nestjs/core';
import { ValidationPipe } from '@nestjs/common';
import { SwaggerModule, DocumentBuilder } from '@nestjs/swagger';
import helmet from 'helmet';
import { AppModule } from './app.module';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);

  app.setGlobalPrefix('v1');
  app.use(helmet());
  app.enableCors({
    origin: process.env.WEB_ADMIN_ORIGIN || '*',
    credentials: true,
  });

  app.useGlobalPipes(
    new ValidationPipe({
      whitelist: true,
      transform: true,
      forbidNonWhitelisted: true,
    }),
  );

  const config = new DocumentBuilder()
    .setTitle('CoopRide India API')
    .setDescription('Cooperative ride-sharing platform API')
    .setVersion('1.0.0')
    .addBearerAuth()
    .build();

  const document = SwaggerModule.createDocument(app, config);
  SwaggerModule.setup('docs', app, document);

  await app.listen(process.env.PORT || 4000);
}

bootstrap();
```

---

## ৩.৩ `src/app.module.ts`

```typescript
import { Module } from '@nestjs/common';
import { ConfigModule } from '@nestjs/config';
import { ScheduleModule } from '@nestjs/schedule';
import { PrismaModule } from './prisma/prisma.module';
import { AuthModule } from './auth/auth.module';
import { RidesModule } from './rides/rides.module';
import { DispatchModule } from './dispatch/dispatch.module';
import { PricingModule } from './pricing/pricing.module';
import { PaymentsModule } from './payments/payments.module';
import { CoopModule } from './coop/coop.module';
import { AdminModule } from './admin/admin.module';
import { SafetyModule } from './safety/safety.module';

@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true }),
    ScheduleModule.forRoot(),
    PrismaModule,
    AuthModule,
    RidesModule,
    DispatchModule,
    PricingModule,
    PaymentsModule,
    CoopModule,
    AdminModule,
    SafetyModule,
  ],
})
export class AppModule {}
```

---

## ৩.৪ `src/prisma/prisma.service.ts`

```typescript
import { Injectable, OnModuleDestroy, OnModuleInit } from '@nestjs/common';
import { PrismaClient } from '@prisma/client';

@Injectable()
export class PrismaService
  extends PrismaClient
  implements OnModuleInit, OnModuleDestroy
{
  async onModuleInit() {
    await this.$connect();
  }

  async onModuleDestroy() {
    await this.$disconnect();
  }
}
```

---

## ৩.৫ `src/auth/auth.controller.ts`

```typescript
import { Body, Controller, Get, Post, UseGuards } from '@nestjs/common';
import { AuthService } from './auth.service';
import { JwtAuthGuard } from '../common/guards/jwt-auth.guard';
import { RequestOtpDto, VerifyOtpDto } from './dto';

@Controller('auth')
export class AuthController {
  constructor(private readonly authService: AuthService) {}

  @Post('otp/request')
  requestOtp(@Body() dto: RequestOtpDto) {
    return this.authService.requestOtp(dto);
  }

  @Post('otp/verify')
  verifyOtp(@Body() dto: VerifyOtpDto) {
    return this.authService.verifyOtp(dto);
  }

  @Post('refresh')
  refresh(@Body('refresh_token') refreshToken: string) {
    return this.authService.refresh(refreshToken);
  }

  @UseGuards(JwtAuthGuard)
  @Get('me')
  me() {
    return this.authService.getProfile();
  }
}
```

---

## ৩.৬ `src/auth/auth.service.ts`

```typescript
import { Injectable, UnauthorizedException } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import { PrismaService } from '../prisma/prisma.service';
import { RequestOtpDto, VerifyOtpDto } from './dto';

@Injectable()
export class AuthService {
  constructor(
    private prisma: PrismaService,
    private jwtService: JwtService,
  ) {}

  async requestOtp(dto: RequestOtpDto) {
    // Save OTP in Redis or DB with TTL
    // Call DLT-approved SMS provider
    return {
      success: true,
      data: {
        otp_id: 'otp_temp_id',
        expires_in: 300,
      },
    };
  }

  async verifyOtp(dto: VerifyOtpDto) {
    // Verify OTP
    // Find or create user
    const user = await this.prisma.user.findUnique({
      where: {
        phone_country_code_phone_number: {
          phone_country_code: '+91',
          phone_number: '9876543210',
        },
      },
    });

    if (!user) {
      throw new UnauthorizedException('Invalid OTP');
    }

    const payload = {
      sub: user.id,
      role: user.role,
    };

    return {
      success: true,
      data: {
        access_token: this.jwtService.sign(payload),
        refresh_token: this.jwtService.sign(payload, { expiresIn: '30d' }),
        expires_in: 900,
        user,
      },
    };
  }

  async refresh(refreshToken: string) {
    try {
      const payload = this.jwtService.verify(refreshToken);
      return {
        success: true,
        data: {
          access_token: this.jwtService.sign({
            sub: payload.sub,
            role: payload.role,
          }),
          refresh_token: refreshToken,
          expires_in: 900,
        },
      };
    } catch {
      throw new UnauthorizedException('Invalid refresh token');
    }
  }

  async getProfile() {
    return { success: true, data: null };
  }
}
```

---

## ৩.৭ `src/common/guards/jwt-auth.guard.ts`

```typescript
import { Injectable } from '@nestjs/common';
import { AuthGuard } from '@nestjs/passport';

@Injectable()
export class JwtAuthGuard extends AuthGuard('jwt') {}
```

---

## ৩.৮ `src/common/decorators/roles.decorator.ts`

```typescript
import { SetMetadata } from '@nestjs/common';

export const ROLES_KEY = 'roles';
export const Roles = (...roles: string[]) => SetMetadata(ROLES_KEY, roles);
```

---

## ৩.৯ `src/rides/rides.controller.ts`

```typescript
import {
  Body,
  Controller,
  Get,
  Param,
  Post,
  UseGuards,
} from '@nestjs/common';
import { RidesService } from './rides.service';
import { JwtAuthGuard } from '../common/guards/jwt-auth.guard';
import { CreateRideDto, EstimateRideDto } from './dto';

@Controller()
export class RidesController {
  constructor(private readonly ridesService: RidesService) {}

  @UseGuards(JwtAuthGuard)
  @Post('rider/rides/estimate')
  estimate(@Body() dto: EstimateRideDto) {
    return this.ridesService.estimate(dto);
  }

  @UseGuards(JwtAuthGuard)
  @Post('rider/rides')
  create(@Body() dto: CreateRideDto) {
    return this.ridesService.create(dto);
  }

  @UseGuards(JwtAuthGuard)
  @Get('rider/rides/:id')
  findOne(@Param('id') id: string) {
    return this.ridesService.findOne(id);
  }

  @UseGuards(JwtAuthGuard)
  @Post('rider/rides/:id/cancel')
  cancel(@Param('id') id: string, @Body('reason') reason: string) {
    return this.ridesService.cancel(id, reason);
  }
}
```

---

## ৩.১০ `src/rides/rides.service.ts`

```typescript
import { Injectable, NotFoundException } from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';
import { DispatchService } from '../dispatch/dispatch.service';
import { CreateRideDto, EstimateRideDto } from './dto';

@Injectable()
export class RidesService {
  constructor(
    private prisma: PrismaService,
    private dispatchService: DispatchService,
  ) {}

  async estimate(dto: EstimateRideDto) {
    const distanceKm = 5.2;
    const durationMin = 18;

    const baseFare = 25;
    const perKm = 10;
    const platformFeePercentage = 0.10;
    const gstPercentage = 0.05;

    const distanceCharge = distanceKm * perKm;
    const subtotal = baseFare + distanceCharge;
    const platformFee = subtotal * platformFeePercentage;
    const gstAmount = (subtotal + platformFee) * gstPercentage;
    const fareFinal = subtotal + platformFee + gstAmount;

    return {
      success: true,
      data: {
        ride_type_id: dto.rideTypeId,
        distance_km: distanceKm,
        duration_min: durationMin,
        base_fare: baseFare,
        distance_charge: distanceCharge,
        time_charge: 0,
        night_surcharge: 0,
        surge_multiplier: 1,
        toll_estimate: 0,
        platform_fee: platformFee,
        gst_amount: gstAmount,
        promo_discount: 0,
        fare_min: fareFinal * 0.95,
        fare_max: fareFinal * 1.05,
        fare_final: fareFinal,
        eta_driver_min: 4,
      },
    };
  }

  async create(dto: CreateRideDto) {
    const ride = await this.prisma.ride.create({
      data: {
        riderId: 'current-user-id',
        cityId: 'kol',
        rideTypeId: dto.rideTypeId,
        status: 'REQUESTED',
        pickupLat: dto.pickupLat,
        pickupLng: dto.pickupLng,
        pickupAddress: dto.pickupAddress,
        dropLat: dto.dropLat,
        dropLng: dto.dropLng,
        dropAddress: dto.dropAddress,
        paymentMethod: dto.paymentMethod,
      },
    });

    await this.dispatchService.dispatch(ride.id);

    return {
      success: true,
      data: ride,
    };
  }

  async findOne(id: string) {
    const ride = await this.prisma.ride.findUnique({ where: { id } });
    if (!ride) throw new NotFoundException('Ride not found');

    return { success: true, data: ride };
  }

  async cancel(id: string, reason: string) {
    const ride = await this.prisma.ride.update({
      where: { id },
      data: {
        status: 'CANCELLED',
        cancelledAt: new Date(),
        cancellationReason: reason,
      },
    });

    return { success: true, data: ride };
  }
}
```

---

## ৩.১১ `src/dispatch/dispatch.service.ts`

```typescript
import { Injectable } from '@nestjs/common';
import Redis from 'ioredis';
import { PrismaService } from '../prisma/prisma.service';
import { RealtimeGateway } from './realtime.gateway';

@Injectable()
export class DispatchService {
  private redis = new Redis({
    host: process.env.REDIS_HOST || 'localhost',
    port: Number(process.env.REDIS_PORT || 6379),
  });

  constructor(
    private prisma: PrismaService,
    private realtime: RealtimeGateway,
  ) {}

  async dispatch(rideId: string) {
    const ride = await this.prisma.ride.findUnique({ where: { id: rideId } });
    if (!ride) return;

    const nearbyDrivers = await this.redis.georadius(
      'drivers:geo',
      ride.pickupLng,
      ride.pickupLat,
      5,
      'km',
      'WITHCOORD',
      'ASC',
      'COUNT',
      10,
    );

    for (const driver of nearbyDrivers) {
      const driverId = driver[0] as string;

      await this.prisma.ride.update({
        where: { id: rideId },
        data: { status: 'OFFERED_TO_DRIVER' },
      });

      this.realtime.sendToDriver(driverId, {
        event: 'NEW_RIDE_REQUEST',
        data: {
          ride_id: ride.id,
          pickup_address: ride.pickupAddress,
          drop_address: ride.dropAddress,
          fare_amount: ride.fareAmount,
        },
      });

      break;
    }
  }
}
```

---

## ৩.১২ `src/dispatch/realtime.gateway.ts`

```typescript
import {
  WebSocketGateway,
  WebSocketServer,
} from '@nestjs/websockets';
import { Server } from 'socket.io';

@WebSocketGateway({
  cors: {
    origin: '*',
  },
  namespace: '/realtime',
})
export class RealtimeGateway {
  @WebSocketServer()
  server: Server;

  sendToDriver(driverId: string, payload: any) {
    this.server.to(`driver:${driverId}`).emit('message', payload);
  }

  sendToRider(riderId: string, payload: any) {
    this.server.to(`rider:${riderId}`).emit('message', payload);
  }

  sendToAdmin(room: string, payload: any) {
    this.server.to(room).emit('message', payload);
  }
}
```

---

## ৩.১৩ `prisma/schema.prisma` সংক্ষিপ্ত অংশ

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Role {
  RIDER
  DRIVER
  ADMIN
  SUPPORT
  FINANCE
  SAFETY
  BOARD
  SUPER_ADMIN
}

enum UserStatus {
  ACTIVE
  PENDING
  SUSPENDED
  BLOCKED
  DEACTIVATED
}

enum RideStatus {
  DRAFT
  REQUESTED
  SEARCHING
  OFFERED_TO_DRIVER
  DRIVER_ASSIGNED
  DRIVER_EN_ROUTE
  ARRIVED_AT_PICKUP
  PASSENGER_BOARDED
  ONGOING
  NEAR_DROP
  COMPLETED
  PAYMENT_PENDING
  PAID
  RATED
  CANCELLED
  DISPUTED
  SAFETY_INCIDENT
}

model User {
  id                String     @id @default(uuid())
  phoneCountryCode  String     @default("+91") @map("phone_country_code")
  phoneNumber       String     @map("phone_number")
  fullName          String     @map("full_name")
  role              Role       @default(RIDER)
  status            UserStatus @default(ACTIVE)
  preferredLanguage String     @default("en") @map("preferred_language")
  createdAt         DateTime   @default(now()) @map("created_at")
  updatedAt         DateTime   @updatedAt @map("updated_at")

  ridesAsRider Ride[] @relation("RiderRides")
  ridesAsDriver Ride[] @relation("DriverRides")

  @@unique([phoneCountryCode, phoneNumber])
  @@map("users")
}

model Ride {
  id               String     @id @default(uuid())
  rideNumber       BigInt     @unique @default(autoincrement()) @map("ride_number")
  riderId          String     @map("rider_id")
  driverId         String?    @map("driver_id")
  status           RideStatus @default(REQUESTED)
  pickupLat        Decimal    @map("pickup_lat") @db.Decimal(10, 7)
  pickupLng        Decimal    @map("pickup_lng") @db.Decimal(10, 7)
  pickupAddress    String?    @map("pickup_address")
  dropLat          Decimal    @map("drop_lat") @db.Decimal(10, 7)
  dropLng          Decimal    @map("drop_lng") @db.Decimal(10, 7)
  dropAddress      String?    @map("drop_address")
  fareAmount       Decimal    @default(0) @map("fare_amount") @db.Decimal(10, 2)
  platformFee      Decimal    @default(0) @map("platform_fee") @db.Decimal(10, 2)
  gstAmount        Decimal    @default(0) @map("gst_amount") @db.Decimal(10, 2)
  driverNetAmount  Decimal    @default(0) @map("driver_net_amount") @db.Decimal(10, 2)
  paymentMethod    String?    @map("payment_method")
  paymentStatus    String     @default("PENDING") @map("payment_status")
  createdAt        DateTime   @default(now()) @map("created_at")
  updatedAt        DateTime   @updatedAt @map("updated_at")

  rider  User  @relation("RiderRides", fields: [riderId], references: [id])
  driver User? @relation("DriverRides", fields: [driverId], references: [id])

  @@map("rides")
}
```

---

## ৩.১৪ `Dockerfile`

```dockerfile
FROM node:20-alpine AS builder

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build && npm prune --omit=dev

FROM node:20-alpine

WORKDIR /app

COPY --from=builder /app/node_modules ./node_modules
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/package.json ./package.json

EXPOSE 4000

CMD ["node", "dist/main"]
```

---

## ৩.১৫ `docker-compose.yml`

```yaml
version: '3.9'

services:
  api:
    build: .
    env_file:
      - .env
    ports:
      - "4000:4000"
    depends_on:
      - postgres
      - redis

  postgres:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: coopride
      POSTGRES_PASSWORD: coopride_password
      POSTGRES_DB: coopride
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres_data:
```

---

# ৪. CI/CD পাইপলাইন

## ৪.১ `.github/workflows/backend.yml`

```yaml
name: Backend CI/CD

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'backend/**'
  pull_request:
    paths:
      - 'backend/**'

defaults:
  run:
    working-directory: backend

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgis/postgis:16-3.4
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: coopride_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379

    env:
      DATABASE_URL: postgresql://test:test@localhost:5432/coopride_test
      REDIS_HOST: localhost
      REDIS_PORT: 6379

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Prisma generate
        run: npx prisma generate

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build

  deploy-staging:
    needs: test
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, push image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: coopride-api
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG ./backend
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

      - name: Deploy to ECS
        run: |
          aws ecs update-service \
            --cluster coopride-staging \
            --service api \
            --force-new-deployment
```

---

## ৪.২ `.github/workflows/flutter-rider-android.yml`

```yaml
name: Rider Android Build

on:
  push:
    branches:
      - main
    paths:
      - 'packages/**'
      - 'apps/rider/**'
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.24.x
          channel: stable
          cache: true

      - name: Install Melos
        run: dart pub global activate melos

      - name: Bootstrap packages
        run: melos bootstrap

      - name: Run tests
        run: melos run test

      - name: Build Android App Bundle
        run: |
          cd apps/rider
          flutter build appbundle \
            --release \
            --target lib/main_prod.dart \
            --dart-define=ENV=prod

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: rider-android-aab
          path: apps/rider/build/app/outputs/bundle/release/app-release.aab

      - name: Upload to Play Console Internal Track
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: in.coopride.rider
          releaseFiles: apps/rider/build/app/outputs/bundle/release/app-release.aab
          track: internal
          status: completed
```

---

## ৪.৩ `.github/workflows/flutter-driver-android.yml`

```yaml
name: Driver Android Build

on:
  push:
    branches:
      - main
    paths:
      - 'packages/**'
      - 'apps/driver/**'
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.24.x
          channel: stable
          cache: true

      - name: Install Melos
        run: dart pub global activate melos

      - name: Bootstrap packages
        run: melos bootstrap

      - name: Build Android App Bundle
        run: |
          cd apps/driver
          flutter build appbundle \
            --release \
            --target lib/main_prod.dart \
            --dart-define=ENV=prod

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: driver-android-aab
          path: apps/driver/build/app/outputs/bundle/release/app-release.aab

      - name: Upload to Play Console Internal Track
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: in.coopride.driver
          releaseFiles: apps/driver/build/app/outputs/bundle/release/app-release.aab
          track: internal
          status: completed
```

---

## ৪.৪ `.github/workflows/admin-web.yml`

```yaml
name: Admin Web Build and Deploy

on:
  push:
    branches:
      - main
    paths:
      - 'packages/**'
      - 'apps/admin_web/**'
  workflow_dispatch:

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.24.x
          channel: stable
          cache: true

      - name: Install Melos
        run: dart pub global activate melos

      - name: Bootstrap packages
        run: melos bootstrap

      - name: Build Web
        run: |
          cd apps/admin_web
          flutter build web \
            --release \
            --target lib/main_prod.dart \
            --dart-define=ENV=prod \
            --base-href /

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1

      - name: Deploy to S3
        run: |
          aws s3 sync apps/admin_web/build/web s3://coopride-admin-web \
            --delete \
            --cache-control "public,max-age=31536000,immutable"

      - name: Invalidate CloudFront
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/*"
```

---

## ৪.৫ `Fastfile` উদাহরণ — iOS

```ruby
default_platform(:ios)

platform :ios do
  desc "Build and upload Rider iOS app to TestFlight"
  lane :rider_beta do
    Dir.chdir("apps/rider") do
      sh(
        "flutter build ipa " \
        "--release " \
        "--target lib/main_prod.dart " \
        "--dart-define=ENV=prod"
      )
    end

    upload_to_testflight(
      app_identifier: "in.coopride.rider",
      ipa: "apps/rider/build/ios/ipa/coopride_rider.ipa",
      skip_waiting_for_build_processing: true
    )
  end

  desc "Build and upload Driver iOS app to TestFlight"
  lane :driver_beta do
    Dir.chdir("apps/driver") do
      sh(
        "flutter build ipa " \
        "--release " \
        "--target lib/main_prod.dart " \
        "--dart-define=ENV=prod"
      )
    end

    upload_to_testflight(
      app_identifier: "in.coopride.driver",
      ipa: "apps/driver/build/ios/ipa/coopride_driver.ipa",
      skip_waiting_for_build_processing: true
    )
  end
end
```

---

## ৪.৬ প্রয়োজনীয় Secrets

```text
AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY
PLAY_SERVICE_ACCOUNT_JSON
APPLE_CERTIFICATE
APPLE_CERTIFICATE_PASSWORD
APPLE_PROVISIONING_PROFILE
APPLE_APP_SPECIFIC_PASSWORD
DATABASE_URL
REDIS_URL
JWT_SECRET
RAZORPAY_KEY_ID
RAZORPAY_KEY_SECRET
CASHFREE_APP_ID
CASHFREE_SECRET_KEY
MSG91_AUTH_KEY
GOOGLE_MAPS_API_KEY
SENTRY_DSN
FIREBASE_SERVICE_ACCOUNT
CLOUDFRONT_DISTRIBUTION_ID
```

---

# ৫. সম্পূর্ণ ডাটাবেস মাইগ্রেশন SQL

নিচের ফাইলটি `migrations/001_initial_schema.sql` হিসেবে ব্যবহার করা যাবে।

```sql
-- =====================================================
-- CoopRide India - Initial Database Schema
-- PostgreSQL 15+ with PostGIS
-- =====================================================

CREATE EXTENSION IF NOT EXISTS postgis;
CREATE EXTENSION IF NOT EXISTS pgcrypto;
CREATE EXTENSION IF NOT EXISTS citext;

-- =====================================================
-- ENUM TYPES
-- =====================================================

CREATE TYPE user_role AS ENUM (
  'RIDER',
  'DRIVER',
  'ADMIN',
  'SUPPORT',
  'FINANCE',
  'SAFETY',
  'BOARD',
  'SUPER_ADMIN'
);

CREATE TYPE user_status AS ENUM (
  'ACTIVE',
  'PENDING',
  'SUSPENDED',
  'BLOCKED',
  'DEACTIVATED'
);

CREATE TYPE ride_status AS ENUM (
  'DRAFT',
  'REQUESTED',
  'SEARCHING',
  'OFFERED_TO_DRIVER',
  'DRIVER_ASSIGNED',
  'DRIVER_EN_ROUTE',
  'ARRIVED_AT_PICKUP',
  'PASSENGER_BOARDED',
  'ONGOING',
  'NEAR_DROP',
  'COMPLETED',
  'PAYMENT_PENDING',
  'PAID',
  'RATED',
  'CANCELLED',
  'DISPUTED',
  'SAFETY_INCIDENT'
);

CREATE TYPE payment_status AS ENUM (
  'PENDING',
  'AUTHORIZED',
  'SUCCESS',
  'FAILED',
  'REFUNDED',
  'EXPIRED'
);

CREATE TYPE payment_method AS ENUM (
  'CASH',
  'UPI',
  'CARD',
  'NETBANKING',
  'WALLET',
  'CORPORATE'
);

CREATE TYPE kyc_status AS ENUM (
  'PENDING',
  'SUBMITTED',
  'AUTO_APPROVED',
  'MANUAL_REVIEW',
  'APPROVED',
  'REJECTED',
  'SUSPENDED'
);

CREATE TYPE document_type AS ENUM (
  'DRIVING_LICENCE',
  'VEHICLE_RC',
  'INSURANCE',
  'FITNESS_CERTIFICATE',
  'PERMIT',
  'PUC',
  'ADDRESS_PROOF',
  'PAN',
  'BANK_PROOF',
  'PHOTO',
  'POLICE_VERIFICATION',
  'AADHAAR',
  'SIGNATURE'
);

CREATE TYPE coop_member_type AS ENUM (
  'DRIVER',
  'RIDER',
  'WORKER',
  'INVESTOR',
  'FOUNDER'
);

CREATE TYPE coop_member_status AS ENUM (
  'PENDING',
  'ACTIVE',
  'SUSPENDED',
  'RESIGNED',
  'DECEASED'
);

CREATE TYPE ledger_account_type AS ENUM (
  'ASSET',
  'LIABILITY',
  'EQUITY',
  'REVENUE',
  'EXPENSE'
);

-- =====================================================
-- USERS
-- =====================================================

CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  phone_country_code VARCHAR(4) NOT NULL DEFAULT '+91',
  phone_number VARCHAR(15) NOT NULL,
  email CITEXT,
  full_name VARCHAR(255) NOT NULL,
  profile_photo_url VARCHAR(512),
  role user_role NOT NULL DEFAULT 'RIDER',
  status user_status NOT NULL DEFAULT 'ACTIVE',
  preferred_language VARCHAR(10) DEFAULT 'en',
  preferred_currency CHAR(3) DEFAULT 'INR',
  timezone VARCHAR(50) DEFAULT 'Asia/Kolkata',
  device_fingerprint JSONB,
  last_login_at TIMESTAMPTZ,
  last_active_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

  CONSTRAINT users_phone_unique UNIQUE (phone_country_code, phone_number)
);

CREATE INDEX idx_users_phone ON users(phone_country_code, phone_number);
CREATE INDEX idx_users_role ON users(role);
CREATE INDEX idx_users_status ON users(status);
CREATE INDEX idx_users_created_at ON users(created_at DESC);

-- =====================================================
-- RIDER PROFILES
-- =====================================================

CREATE TABLE rider_profiles (
  user_id UUID PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
  emergency_contact_name VARCHAR(255),
  emergency_contact_phone VARCHAR(20),
  secondary_emergency_name VARCHAR(255),
  secondary_emergency_phone VARCHAR(20),
  default_payment_method payment_method DEFAULT 'CASH',
  total_rides INTEGER NOT NULL DEFAULT 0,
  total_spent NUMERIC(12,2) NOT NULL DEFAULT 0,
  rating NUMERIC(3,2) NOT NULL DEFAULT 5.00,
  rating_count INTEGER NOT NULL DEFAULT 0,
  referral_code VARCHAR(20) UNIQUE,
  referred_by UUID REFERENCES users(id),
  is_coop_member BOOLEAN NOT NULL DEFAULT FALSE,
  member_id UUID,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- =====================================================
-- DRIVER PROFILES
-- =====================================================

CREATE TABLE driver_profiles (
  user_id UUID PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
  licence_number VARCHAR(50),
  licence_expiry DATE,
  kyc_status kyc_status NOT NULL DEFAULT 'PENDING',
  on_duty_status VARCHAR(20) NOT NULL DEFAULT 'OFFLINE'
    CHECK (on_duty_status IN ('ONLINE','OFFLINE','BUSY','BREAK')),
  current_lat NUMERIC(10,7),
  current_lng NUMERIC(10,7),
  current_heading NUMERIC(5,2),
  current_speed_mps NUMERIC(5,2),
  last_location_at TIMESTAMPTZ,
  rating NUMERIC(3,2) NOT NULL DEFAULT 5.00,
  rating_count INTEGER NOT NULL DEFAULT 0,
  acceptance_rate NUMERIC(5,2) NOT NULL DEFAULT 100.00,
  cancellation_rate NUMERIC(5,2) NOT NULL DEFAULT 0.00,
  total_rides INTEGER NOT NULL DEFAULT 0,
  total_earnings NUMERIC(12,2) NOT NULL DEFAULT 0,
  current_vehicle_id UUID,
  member_id UUID,
  bank_account_id UUID,
  upi_id VARCHAR(100),
  pan_number VARCHAR(20),
  aadhaar_last4 CHAR(4),
  police_verification_status VARCHAR(20),
  police_verification_expiry DATE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_driver_profiles_status
  ON driver_profiles(on_duty_status, kyc_status);

CREATE INDEX idx_driver_profiles_location
  ON driver_profiles USING GIST (
    ST_SetSRID(ST_MakePoint(current_lng, current_lat), 4326)
  );

-- =====================================================
-- CITIES AND ZONES
-- =====================================================

CREATE TABLE cities (
  id VARCHAR(50) PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  state VARCHAR(100) NOT NULL,
  country VARCHAR(50) NOT NULL DEFAULT 'India',
  timezone VARCHAR(50) NOT NULL DEFAULT 'Asia/Kolkata',
  currency CHAR(3) NOT NULL DEFAULT 'INR',
  is_active BOOLEAN NOT NULL DEFAULT FALSE,
  default_language VARCHAR(10) NOT NULL DEFAULT 'en',
  supported_languages VARCHAR(10)[] NOT NULL DEFAULT ARRAY['en','hi'],
  launch_date DATE,
  operations_contact VARCHAR(50),
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE zones (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  city_id VARCHAR(50) NOT NULL REFERENCES cities(id) ON DELETE CASCADE,
  name VARCHAR(100) NOT NULL,
  polygon GEOGRAPHY(POLYGON, 4326) NOT NULL,
  is_active BOOLEAN NOT NULL DEFAULT TRUE,
  surge_enabled BOOLEAN NOT NULL DEFAULT FALSE,
  night_surcharge_enabled BOOLEAN NOT NULL DEFAULT TRUE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_zones_polygon ON zones USING GIST (polygon);
CREATE INDEX idx_zones_city ON zones(city_id);

-- =====================================================
-- RIDE TYPES
-- =====================================================

CREATE TABLE ride_types (
  id VARCHAR(50) PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  description TEXT,
  min_seating INTEGER NOT NULL DEFAULT 1,
  max_seating INTEGER NOT NULL DEFAULT 4,
  base_icon_url VARCHAR(512),
  is_active BOOLEAN NOT NULL DEFAULT TRUE,
  sort_order INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- =====================================================
-- VEHICLES
-- =====================================================

CREATE TABLE vehicles (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  driver_id UUID NOT NULL REFERENCES driver_profiles(user_id) ON DELETE CASCADE,
  vehicle_type VARCHAR(20) NOT NULL
    CHECK (vehicle_type IN (
      'BIKE','AUTO','CAR_MINI','CAR_SEDAN','CAR_SUV','CAR_PREMIUM',
      'EV_BIKE','EV_AUTO','EV_CAR'
    )),
  make VARCHAR(100),
  model VARCHAR(100),
  year INTEGER,
  color VARCHAR(50),
  registration_number VARCHAR(20) NOT NULL,
  registration_expiry DATE,
  insurance_provider VARCHAR(100),
  insurance_policy_number VARCHAR(100),
  insurance_expiry DATE,
  fitness_certificate_expiry DATE,
  permit_type VARCHAR(50),
  permit_expiry DATE,
  puc_expiry DATE,
  fuel_type VARCHAR(20)
    CHECK (fuel_type IN ('PETROL','DIESEL','CNG','ELECTRIC','HYBRID')),
  seating_capacity INTEGER,
  status VARCHAR(20) NOT NULL DEFAULT 'PENDING',
  photos JSONB,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

  CONSTRAINT vehicles_registration_unique UNIQUE (registration_number)
);

CREATE INDEX idx_vehicles_driver ON vehicles(driver_id);
CREATE INDEX idx_vehicles_expiry
  ON vehicles(registration_expiry, insurance_expiry, puc_expiry);

-- =====================================================
-- KYC DOCUMENTS
-- =====================================================

CREATE TABLE kyc_documents (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  document_type document_type NOT NULL,
  document_number_masked VARCHAR(100),
  issuer VARCHAR(255),
  issue_date DATE,
  expiry_date DATE,
  file_url VARCHAR(512) NOT NULL,
  thumbnail_url VARCHAR(512),
  file_size_bytes INTEGER,
  mime_type VARCHAR(100),
  ocr_extracted_data JSONB,
  verification_status kyc_status NOT NULL DEFAULT 'PENDING',
  rejection_reason TEXT,
  verified_by UUID REFERENCES users(id),
  verified_at TIMESTAMPTZ,
  external_verification_ref VARCHAR(255),
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_kyc_documents_user ON kyc_documents(user_id);
CREATE INDEX idx_kyc_documents_status ON kyc_documents(verification_status);
CREATE INDEX idx_kyc_documents_type ON kyc_documents(document_type);
CREATE INDEX idx_kyc_documents_expiry ON kyc_documents(expiry_date);

-- =====================================================
-- PRICING CONFIGS
-- =====================================================

CREATE TABLE pricing_configs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  city_id VARCHAR(50) NOT NULL REFERENCES cities(id) ON DELETE CASCADE,
  ride_type_id VARCHAR(50) NOT NULL REFERENCES ride_types(id),
  base_fare NUMERIC(10,2) NOT NULL,
  per_km_rate NUMERIC(10,2) NOT NULL,
  per_minute_rate NUMERIC(10,2) NOT NULL DEFAULT 0,
  minimum_fare NUMERIC(10,2) NOT NULL,
  cancellation_fee NUMERIC(10,2) NOT NULL DEFAULT 0,
  waiting_charge_per_min NUMERIC(10,2) NOT NULL DEFAULT 0,
  free_waiting_minutes INTEGER NOT NULL DEFAULT 3,
  night_start_time TIME NOT NULL DEFAULT '22:00:00',
  night_end_time TIME NOT NULL DEFAULT '05:00:00',
  night_multiplier NUMERIC(4,2) NOT NULL DEFAULT 1.25,
  peak_hours JSONB,
  peak_multiplier NUMERIC(4,2) NOT NULL DEFAULT 1.0,
  max_surge_multiplier NUMERIC(4,2) NOT NULL DEFAULT 1.5,
  platform_fee_percentage NUMERIC(5,2) NOT NULL DEFAULT 10.0,
  platform_fee_fixed NUMERIC(10,2) NOT NULL DEFAULT 0,
  gst_percentage NUMERIC(5,2) NOT NULL DEFAULT 5.0,
  gst_on_platform_fee_only BOOLEAN NOT NULL DEFAULT FALSE,
  toll_policy VARCHAR(20) NOT NULL DEFAULT 'ACTUALS'
    CHECK (toll_policy IN ('ACTUALS','INCLUDED','EXCLUDED')),
  is_active BOOLEAN NOT NULL DEFAULT TRUE,
  valid_from DATE NOT NULL DEFAULT CURRENT_DATE,
  valid_until DATE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

  CONSTRAINT pricing_configs_unique
    UNIQUE (city_id, ride_type_id, valid_from)
);

-- =====================================================
-- RIDE REQUESTS
-- =====================================================

CREATE TABLE ride_requests (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  rider_id UUID NOT NULL REFERENCES users(id),
  pickup_lat NUMERIC(10,7) NOT NULL,
  pickup_lng NUMERIC(10,7) NOT NULL,
  pickup_address TEXT,
  pickup_landmark VARCHAR(255),
  pickup_zone_id UUID REFERENCES zones(id),
  drop_lat NUMERIC(10,7) NOT NULL,
  drop_lng NUMERIC(10,7) NOT NULL,
  drop_address TEXT,
  drop_landmark VARCHAR(255),
  drop_zone_id UUID REFERENCES zones(id),
  ride_type_id VARCHAR(50) NOT NULL REFERENCES ride_types(id),
  estimated_distance_km NUMERIC(8,3),
  estimated_duration_min INTEGER,
  estimated_fare_min NUMERIC(10,2),
  estimated_fare_max NUMERIC(10,2),
  surge_multiplier NUMERIC(4,2) NOT NULL DEFAULT 1.0,
  promo_code VARCHAR(50),
  promo_discount NUMERIC(10,2) NOT NULL DEFAULT 0,
  preferred_payment_method payment_method,
  special_instructions TEXT,
  is_scheduled BOOLEAN NOT NULL DEFAULT FALSE,
  scheduled_at TIMESTAMPTZ,
  status VARCHAR(30) NOT NULL DEFAULT 'DRAFT',
  notes TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  expired_at TIMESTAMPTZ
);

CREATE INDEX idx_ride_requests_rider ON ride_requests(rider_id);
CREATE INDEX idx_ride_requests_status ON ride_requests(status);
CREATE INDEX idx_ride_requests_pickup
  ON ride_requests USING GIST (
    ST_SetSRID(ST_MakePoint(pickup_lng, pickup_lat), 4326)
  );

-- =====================================================
-- RIDES
-- =====================================================

CREATE TABLE rides (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ride_number BIGSERIAL UNIQUE,
  request_id UUID REFERENCES ride_requests(id),
  rider_id UUID NOT NULL REFERENCES users(id),
  driver_id UUID REFERENCES users(id),
  vehicle_id UUID REFERENCES vehicles(id),
  city_id VARCHAR(50) REFERENCES cities(id),
  zone_id UUID REFERENCES zones(id),
  ride_type_id VARCHAR(50) REFERENCES ride_types(id),
  status ride_status NOT NULL DEFAULT 'REQUESTED',

  pickup_lat NUMERIC(10,7) NOT NULL,
  pickup_lng NUMERIC(10,7) NOT NULL,
  pickup_address TEXT,
  pickup_landmark VARCHAR(255),
  drop_lat NUMERIC(10,7) NOT NULL,
  drop_lng NUMERIC(10,7) NOT NULL,
  drop_address TEXT,
  drop_landmark VARCHAR(255),

  actual_distance_km NUMERIC(8,3),
  actual_duration_min INTEGER,

  base_fare NUMERIC(10,2) NOT NULL DEFAULT 0,
  distance_charge NUMERIC(10,2) NOT NULL DEFAULT 0,
  time_charge NUMERIC(10,2) NOT NULL DEFAULT 0,
  night_surcharge NUMERIC(10,2) NOT NULL DEFAULT 0,
  surge_charge NUMERIC(10,2) NOT NULL DEFAULT 0,
  toll_charges NUMERIC(10,2) NOT NULL DEFAULT 0,
  waiting_charges NUMERIC(10,2) NOT NULL DEFAULT 0,
  promo_discount NUMERIC(10,2) NOT NULL DEFAULT 0,
  fare_amount NUMERIC(10,2) NOT NULL DEFAULT 0,

  platform_fee NUMERIC(10,2) NOT NULL DEFAULT 0,
  gst_amount NUMERIC(10,2) NOT NULL DEFAULT 0,
  total_payable NUMERIC(10,2) NOT NULL DEFAULT 0,
  driver_net_amount NUMERIC(10,2) NOT NULL DEFAULT 0,

  payment_method payment_method,
  payment_status payment_status NOT NULL DEFAULT 'PENDING',
  payment_reference VARCHAR(255),

  ride_otp VARCHAR(6),
  pickup_at TIMESTAMPTZ,
  started_at TIMESTAMPTZ,
  completed_at TIMESTAMPTZ,
  cancelled_at TIMESTAMPTZ,
  cancelled_by VARCHAR(20),
  cancellation_reason TEXT,
  cancellation_fee NUMERIC(10,2) NOT NULL DEFAULT 0,

  rating_by_rider SMALLINT CHECK (rating_by_rider BETWEEN 1 AND 5),
  rating_comment_by_rider TEXT,
  rating_by_driver SMALLINT CHECK (rating_by_driver BETWEEN 1 AND 5),
  rating_comment_by_driver TEXT,

  safety_flags JSONB NOT NULL DEFAULT '{}',
  metadata JSONB NOT NULL DEFAULT '{}',

  invoice_number VARCHAR(50),
  gstin VARCHAR(50),
  sac_code VARCHAR(20),

  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_rides_rider ON rides(rider_id);
CREATE INDEX idx_rides_driver ON rides(driver_id);
CREATE INDEX idx_rides_status ON rides(status);
CREATE INDEX idx_rides_city ON rides(city_id);
CREATE INDEX idx_rides_created_at ON rides(created_at DESC);
CREATE INDEX idx_rides_pickup
  ON rides USING GIST (
    ST_SetSRID(ST_MakePoint(pickup_lng, pickup_lat), 4326)
  );
CREATE INDEX idx_rides_drop
  ON rides USING GIST (
    ST_SetSRID(ST_MakePoint(drop_lng, drop_lat), 4326)
  );

-- =====================================================
-- RIDE STATUS HISTORY
-- =====================================================

CREATE TABLE ride_status_history (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ride_id UUID NOT NULL REFERENCES rides(id) ON DELETE CASCADE,
  from_status ride_status,
  to_status ride_status NOT NULL,
  triggered_by VARCHAR(20) NOT NULL,
  reason TEXT,
  metadata JSONB NOT NULL DEFAULT '{}',
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_ride_status_history_ride
  ON ride_status_history(ride_id, created_at);

-- =====================================================
-- RIDE LOCATION EVENTS (PARTITIONED)
-- =====================================================

CREATE TABLE ride_location_events (
  id UUID NOT NULL DEFAULT gen_random_uuid(),
  ride_id UUID NOT NULL,
  driver_id UUID NOT NULL,
  lat NUMERIC(10,7) NOT NULL,
  lng NUMERIC(10,7) NOT NULL,
  heading NUMERIC(5,2),
  speed_mps NUMERIC(5,2),
  accuracy_m NUMERIC(6,2),
  battery_percentage SMALLINT,
  network_type VARCHAR(20),
  app_version VARCHAR(20),
  device_id VARCHAR(100),
  event_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

  PRIMARY KEY (id, event_at)
) PARTITION BY RANGE (event_at);

CREATE TABLE ride_location_events_y2026m01
  PARTITION OF ride_location_events
  FOR VALUES FROM ('2026-01-01') TO ('2026-02-01');

CREATE TABLE ride_location_events_y2026m02
  PARTITION OF ride_location_events
  FOR VALUES FROM ('2026-02-01') TO ('2026-03-01');

CREATE TABLE ride_location_events_y2026m03
  PARTITION OF ride_location_events
  FOR VALUES FROM ('2026-03-01') TO ('2026-04-01');

CREATE INDEX idx_ride_location_events_ride
  ON ride_location_events(ride_id, event_at);

-- =====================================================
-- LEDGER ACCOUNTS
-- =====================================================

CREATE TABLE ledger_accounts (
  id VARCHAR(50) PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  type ledger_account_type NOT NULL,
  subtype VARCHAR(50),
  description TEXT,
  owner_user_id UUID REFERENCES users(id),
  is_active BOOLEAN NOT NULL DEFAULT TRUE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- =====================================================
-- LEDGER ENTRIES
-- =====================================================

CREATE TABLE ledger_entries (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  reference_type VARCHAR(50) NOT NULL,
  reference_id UUID NOT NULL,
  account_id VARCHAR(50) NOT NULL REFERENCES ledger_accounts(id),
  debit NUMERIC(14,2) NOT NULL DEFAULT 0,
  credit NUMERIC(14,2) NOT NULL DEFAULT 0,
  balance_after NUMERIC(14,2),
  description TEXT,
  created_by UUID REFERENCES users(id),
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

  CONSTRAINT ledger_entries_debit_or_credit
    CHECK (debit >= 0 AND credit >= 0 AND NOT (debit > 0 AND credit > 0))
);

CREATE INDEX idx_ledger_entries_account
  ON ledger_entries(account_id, created_at);

CREATE INDEX idx_ledger_entries_reference
  ON ledger_entries(reference_type, reference_id);

-- =====================================================
-- PAYMENTS
-- =====================================================

CREATE TABLE payments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ride_id UUID REFERENCES rides(id),
  payer_user_id UUID NOT NULL REFERENCES users(id),
  amount NUMERIC(12,2) NOT NULL,
  currency CHAR(3) NOT NULL DEFAULT 'INR',
  method payment_method NOT NULL,
  status payment_status NOT NULL DEFAULT 'PENDING',
  gateway_provider VARCHAR(50),
  gateway_payment_id VARCHAR(255),
  gateway_order_id VARCHAR(255),
  gateway_signature TEXT,
  upi_txn_id VARCHAR(255),
  upi_vpa VARCHAR(255),
  card_last4 CHAR(4),
  card_network VARCHAR(20),
  failure_reason TEXT,
  refunded_amount NUMERIC(12,2) NOT NULL DEFAULT 0,
  captured_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_payments_ride ON payments(ride_id);
CREATE INDEX idx_payments_status ON payments(status);
CREATE INDEX idx_payments_gateway
  ON payments(gateway_provider, gateway_payment_id);

-- =====================================================
-- DRIVER PAYOUTS
-- =====================================================

CREATE TABLE driver_payouts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  payout_number BIGSERIAL UNIQUE,
  driver_id UUID NOT NULL REFERENCES users(id),
  period_start DATE NOT NULL,
  period_end DATE NOT NULL,
  gross_amount NUMERIC(12,2) NOT NULL,
  platform_fee_total NUMERIC(12,2) NOT NULL,
  gst_deducted NUMERIC(12,2) NOT NULL DEFAULT 0,
  tds_deducted NUMERIC(12,2) NOT NULL DEFAULT 0,
  adjustments NUMERIC(12,2) NOT NULL DEFAULT 0,
  net_amount NUMERIC(12,2) NOT NULL,
  status VARCHAR(20) NOT NULL DEFAULT 'PENDING'
    CHECK (status IN ('PENDING','PROCESSING','SUCCESS','FAILED','CANCELLED')),
  payment_method VARCHAR(20),
  bank_account_id UUID,
  upi_id VARCHAR(100),
  gateway_payout_id VARCHAR(255),
  failure_reason TEXT,
  processed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_driver_payouts_driver
  ON driver_payouts(driver_id, period_start);

CREATE INDEX idx_driver_payouts_status
  ON driver_payouts(status);

-- =====================================================
-- COOPERATIVE MEMBERS
-- =====================================================

CREATE TABLE coop_members (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id),
  member_number VARCHAR(20) UNIQUE NOT NULL,
  member_type coop_member_type NOT NULL,
  status coop_member_status NOT NULL DEFAULT 'PENDING',
  joined_at DATE,
  approved_at TIMESTAMPTZ,
  approved_by UUID REFERENCES users(id),
  share_capital NUMERIC(12,2) NOT NULL DEFAULT 0,
  reserve_fund_contribution NUMERIC(12,2) NOT NULL DEFAULT 0,
  nominee_name VARCHAR(255),
  nominee_relation VARCHAR(100),
  nominee_phone VARCHAR(20),
  kyc_status kyc_status NOT NULL DEFAULT 'PENDING',
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_coop_members_user ON coop_members(user_id);
CREATE INDEX idx_coop_members_status ON coop_members(status);

-- =====================================================
-- COOP SHARES
-- =====================================================

CREATE TABLE coop_shares (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  member_id UUID NOT NULL REFERENCES coop_members(id) ON DELETE CASCADE,
  share_amount NUMERIC(12,2) NOT NULL,
  number_of_units INTEGER NOT NULL,
  unit_price NUMERIC(10,2) NOT NULL,
  issue_date DATE NOT NULL,
  certificate_number VARCHAR(50),
  status VARCHAR(20) NOT NULL DEFAULT 'ACTIVE'
    CHECK (status IN ('ACTIVE','TRANSFERRED','REFUNDED','CANCELLED')),
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_coop_shares_member ON coop_shares(member_id);

-- =====================================================
-- COOP SHARE TRANSACTIONS
-- =====================================================

CREATE TABLE coop_share_transactions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  member_id UUID NOT NULL REFERENCES coop_members(id) ON DELETE CASCADE,
  transaction_type VARCHAR(20) NOT NULL
    CHECK (transaction_type IN (
      'PURCHASE','REFUND','TRANSFER_IN','TRANSFER_OUT','BONUS'
    )),
  amount NUMERIC(12,2) NOT NULL,
  units INTEGER,
  payment_reference VARCHAR(255),
  counterparty_member_id UUID REFERENCES coop_members(id),
  notes TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_coop_share_transactions_member
  ON coop_share_transactions(member_id, created_at);

-- =====================================================
-- COOP PATRONAGE
-- =====================================================

CREATE TABLE coop_patronage (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  member_id UUID NOT NULL REFERENCES coop_members(id) ON DELETE CASCADE,
  fiscal_year INTEGER NOT NULL,
  period VARCHAR(20) NOT NULL,
  rides_completed INTEGER NOT NULL DEFAULT 0,
  fare_generated NUMERIC(12,2) NOT NULL DEFAULT 0,
  fare_paid NUMERIC(12,2) NOT NULL DEFAULT 0,
  patronage_points NUMERIC(12,2) NOT NULL DEFAULT 0,
  payout_amount NUMERIC(12,2) NOT NULL DEFAULT 0,
  status VARCHAR(20) NOT NULL DEFAULT 'CALCULATED'
    CHECK (status IN ('CALCULATED','APPROVED','PAID','HELD')),
  approved_at TIMESTAMPTZ,
  paid_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

  CONSTRAINT coop_patronage_unique UNIQUE (member_id, fiscal_year, period)
);

CREATE INDEX idx_coop_patronage_member ON coop_patronage(member_id);

-- =====================================================
-- COOP MEETINGS
-- =====================================================

CREATE TABLE coop_meetings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  meeting_number VARCHAR(20),
  meeting_type VARCHAR(30)
    CHECK (meeting_type IN ('AGM','SGM','BOARD','EMERGENCY')),
  title VARCHAR(255) NOT NULL,
  agenda JSONB NOT NULL,
  location VARCHAR(255),
  is_virtual BOOLEAN NOT NULL DEFAULT FALSE,
  virtual_link VARCHAR(512),
  starts_at TIMESTAMPTZ NOT NULL,
  ends_at TIMESTAMPTZ NOT NULL,
  quorum_required INTEGER,
  quorum_achieved INTEGER NOT NULL DEFAULT 0,
  status VARCHAR(20) NOT NULL DEFAULT 'SCHEDULED',
  minutes_url VARCHAR(512),
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- =====================================================
-- COOP MOTIONS
-- =====================================================

CREATE TABLE coop_motions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  meeting_id UUID NOT NULL REFERENCES coop_meetings(id) ON DELETE CASCADE,
  title VARCHAR(255) NOT NULL,
  description TEXT,
  motion_type VARCHAR(30)
    CHECK (motion_type IN ('RESOLUTION','ELECTION','BYLAW_AMENDMENT','BUDGET')),
  voting_start_at TIMESTAMPTZ,
  voting_end_at TIMESTAMPTZ,
  status VARCHAR(20) NOT NULL DEFAULT 'DRAFT',
  results JSONB,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_coop_motions_meeting ON coop_motions(meeting_id);

-- =====================================================
-- COOP VOTES
-- =====================================================

CREATE TABLE coop_votes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  motion_id UUID NOT NULL REFERENCES coop_motions(id) ON DELETE CASCADE,
  member_id UUID NOT NULL REFERENCES coop_members(id) ON DELETE CASCADE,
  choice VARCHAR(20) NOT NULL CHECK (choice IN ('FOR','AGAINST','ABSTAIN')),
  weight INTEGER NOT NULL DEFAULT 1,
  cast_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

  CONSTRAINT coop_votes_unique UNIQUE (motion_id, member_id)
);

CREATE INDEX idx_coop_votes_motion ON coop_votes(motion_id);
CREATE INDEX idx_coop_votes_member ON coop_votes(member_id);

-- =====================================================
-- SAFETY INCIDENTS
-- =====================================================

CREATE TABLE safety_incidents (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ride_id UUID REFERENCES rides(id),
  reported_by_user_id UUID NOT NULL REFERENCES users(id),
  incident_type VARCHAR(30) NOT NULL
    CHECK (incident_type IN (
      'ACCIDENT','HARASSMENT','THEFT','MEDICAL','ROUTE_DEVIATION',
      'OVERSPEEDING','LONG_STOP','SOS_PRESSED','VERBAL_ABUSE','OTHER'
    )),
  severity VARCHAR(20) NOT NULL DEFAULT 'MEDIUM'
    CHECK (severity IN ('LOW','MEDIUM','HIGH','CRITICAL')),
  description TEXT,
  evidence_urls JSONB,
  location_lat NUMERIC(10,7),
  location_lng NUMERIC(10,7),
  status VARCHAR(20) NOT NULL DEFAULT 'OPEN'
    CHECK (status IN ('OPEN','INVESTIGATING','RESOLVED','CLOSED','ESCALATED')),
  assigned_to UUID REFERENCES users(id),
  resolution_notes TEXT,
  resolved_at TIMESTAMPTZ,
  police_case_number VARCHAR(100),
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_safety_incidents_status ON safety_incidents(status);
CREATE INDEX idx_safety_incidents_ride ON safety_incidents(ride_id);
CREATE INDEX idx_safety_incidents_created
  ON safety_incidents(created_at DESC);

-- =====================================================
-- SUPPORT TICKETS
-- =====================================================

CREATE TABLE support_tickets (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ticket_number BIGSERIAL UNIQUE,
  user_id UUID NOT NULL REFERENCES users(id),
  ride_id UUID REFERENCES rides(id),
  category VARCHAR(50) NOT NULL,
  subcategory VARCHAR(50),
  subject VARCHAR(255) NOT NULL,
  description TEXT NOT NULL,
  priority VARCHAR(20) NOT NULL DEFAULT 'MEDIUM'
    CHECK (priority IN ('LOW','MEDIUM','HIGH','URGENT')),
  status VARCHAR(20) NOT NULL DEFAULT 'OPEN'
    CHECK (status IN (
      'OPEN','IN_PROGRESS','WAITING_CUSTOMER','WAITING_AGENT','RESOLVED','CLOSED'
    )),
  assigned_to UUID REFERENCES users(id),
  sla_deadline TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  resolved_at TIMESTAMPTZ,
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_support_tickets_user ON support_tickets(user_id);
CREATE INDEX idx_support_tickets_status ON support_tickets(status);
CREATE INDEX idx_support_tickets_priority ON support_tickets(priority);

CREATE TABLE support_ticket_messages (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ticket_id UUID NOT NULL REFERENCES support_tickets(id) ON DELETE CASCADE,
  sender_user_id UUID NOT NULL REFERENCES users(id),
  sender_role user_role NOT NULL,
  message_text TEXT NOT NULL,
  attachments JSONB,
  is_internal BOOLEAN NOT NULL DEFAULT FALSE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_support_ticket_messages_ticket
  ON support_ticket_messages(ticket_id, created_at);

-- =====================================================
-- SAVED ADDRESSES
-- =====================================================

CREATE TABLE saved_addresses (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  label VARCHAR(30) CHECK (label IN ('HOME','WORK','OTHER')),
  custom_label VARCHAR(100),
  address TEXT NOT NULL,
  lat NUMERIC(10,7) NOT NULL,
  lng NUMERIC(10,7) NOT NULL,
  landmark VARCHAR(255),
  is_default BOOLEAN NOT NULL DEFAULT FALSE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_saved_addresses_user ON saved_addresses(user_id);

-- =====================================================
-- EMERGENCY CONTACTS
-- =====================================================

CREATE TABLE emergency_contacts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  name VARCHAR(255) NOT NULL,
  phone VARCHAR(20) NOT NULL,
  relationship VARCHAR(100),
  is_trusted BOOLEAN NOT NULL DEFAULT FALSE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_emergency_contacts_user ON emergency_contacts(user_id);

-- =====================================================
-- AUDIT LOGS
-- =====================================================

CREATE TABLE audit_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  actor_user_id UUID REFERENCES users(id),
  actor_role user_role,
  action VARCHAR(100) NOT NULL,
  resource_type VARCHAR(50) NOT NULL,
  resource_id UUID,
  old_values JSONB,
  new_values JSONB,
  ip_address INET,
  user_agent TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_audit_logs_actor ON audit_logs(actor_user_id);
CREATE INDEX idx_audit_logs_resource
  ON audit_logs(resource_type, resource_id);
CREATE INDEX idx_audit_logs_action ON audit_logs(action);
CREATE INDEX idx_audit_logs_created ON audit_logs(created_at DESC);

-- =====================================================
-- UPDATED_AT TRIGGER FUNCTION
-- =====================================================

CREATE OR REPLACE FUNCTION set_updated_at()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = NOW();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER set_users_updated_at
BEFORE UPDATE ON users
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE TRIGGER set_rider_profiles_updated_at
BEFORE UPDATE ON rider_profiles
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE TRIGGER set_driver_profiles_updated_at
BEFORE UPDATE ON driver_profiles
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE TRIGGER set_rides_updated_at
BEFORE UPDATE ON rides
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE TRIGGER set_vehicles_updated_at
BEFORE UPDATE ON vehicles
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE TRIGGER set_kyc_documents_updated_at
BEFORE UPDATE ON kyc_documents
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE TRIGGER set_payments_updated_at
BEFORE UPDATE ON payments
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE TRIGGER set_support_tickets_updated_at
BEFORE UPDATE ON support_tickets
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- =====================================================
-- SEED DATA
-- =====================================================

INSERT INTO cities (id, name, state, is_active)
VALUES
  ('kol', 'Kolkata', 'West Bengal', TRUE),
  ('del', 'Delhi', 'Delhi', FALSE),
  ('blr', 'Bengaluru', 'Karnataka', FALSE),
  ('hyd', 'Hyderabad', 'Telangana', FALSE),
  ('mum', 'Mumbai', 'Maharashtra', FALSE);

INSERT INTO ride_types (id, name, description, min_seating, max_seating, sort_order)
VALUES
  ('bike', 'Bike', 'Fast and affordable', 1, 1, 1),
  ('auto', 'Auto', 'Three-wheeler', 1, 3, 2),
  ('car_mini', 'Mini Car', 'Compact car', 1, 4, 3),
  ('car_sedan', 'Sedan', 'Comfortable sedan', 1, 4, 4),
  ('car_suv', 'SUV', 'Spacious SUV', 1, 6, 5),
  ('ev_bike', 'EV Bike', 'Eco-friendly bike', 1, 1, 6),
  ('women_only', 'Women Safe Ride', 'Women driver preferred', 1, 4, 7);

INSERT INTO ledger_accounts (id, name, type, subtype, description)
VALUES
  ('PLATFORM_REVENUE', 'Platform Revenue', 'REVENUE', 'RIDE_FEES', 'Commission income from rides'),
  ('GST_COLLECTED', 'GST Collected', 'LIABILITY', 'TAX', 'GST collected from customers'),
  ('DRIVER_PAYABLE', 'Driver Payable', 'LIABILITY', 'PAYABLE', 'Amount payable to drivers'),
  ('DRIVER_CASH_COLLECTED', 'Driver Cash Collected', 'ASSET', 'CASH', 'Cash collected by drivers on behalf of platform'),
  ('PAYMENT_GATEWAY_RECEIVABLE', 'Payment Gateway Receivable', 'ASSET', 'RECEIVABLE', 'Amount receivable from payment gateway'),
  ('PROMO_EXPENSE', 'Promotional Expense', 'EXPENSE', 'PROMO', 'Discounts and promos'),
  ('REFUND_PAYABLE', 'Refund Payable', 'LIABILITY', 'REFUND', 'Refunds to customers'),
  ('COOP_SHARE_CAPITAL', 'Cooperative Share Capital', 'EQUITY', 'SHARES', 'Member share capital'),
  ('COOP_RESERVE_FUND', 'Cooperative Reserve Fund', 'EQUITY', 'RESERVE', 'Statutory reserve fund'),
  ('COOP_PATRONAGE_PAYABLE', 'Cooperative Patronage Payable', 'LIABILITY', 'PATRONAGE', 'Patronage distribution payable to members');
```

---

# ৬. টেস্টিং স্ট্র্যাটেজি ডকুমেন্টেশন

## ৬.১ টেস্ট পিরামিড

```text
          E2E / Field Tests
        --------------------
        Integration Tests
      ------------------------
      Unit Tests (সবচেয়ে বেশি)
```

### লক্ষ্য

| স্তর | কভারেজ লক্ষ্য |
|---|---:|
| ইউনিট টেস্ট | ৭০–৮০% |
| ইন্টিগ্রেশন টেস্ট | গুরুত্বপূর্ণ ফ্লো ১০০% |
| উইজেট টেস্ট | প্রধান স্ক্রিন ৬০%+ |
| E2E টেস্ট | প্রধান জার্নি ১০০% |
| লোড টেস্ট | পাইলট KPI অনুযায়ী |

---

## ৬.২ ফ্লাটার টেস্টিং

### টুলস

- `flutter_test`
- `mocktail`
- `bloc_test` বা `riverpod_test`
- `integration_test`
- `patrol` বা `appium` advanced E2E-এর জন্য
- `golden_toolkit` UI regression-এর জন্য

### ইউনিট টেস্ট উদাহরণ

```dart
import 'package:flutter_test/flutter_test.dart';

double calculateFare({
  required double baseFare,
  required double perKm,
  required double distanceKm,
  required double platformFeePercentage,
  required double gstPercentage,
}) {
  final distanceCharge = perKm * distanceKm;
  final subtotal = baseFare + distanceCharge;
  final platformFee = subtotal * platformFeePercentage;
  final gst = (subtotal + platformFee) * gstPercentage;
  return subtotal + platformFee + gst;
}

void main() {
  group('Fare calculation', () {
    test('should calculate fare correctly', () {
      final fare = calculateFare(
        baseFare: 25,
        perKm: 10,
        distanceKm: 5,
        platformFeePercentage: 0.10,
        gstPercentage: 0.05,
      );

      // base = 25
      // distance = 50
      // subtotal = 75
      // platform fee = 7.5
      // gst = 4.125
      // total = 86.625
      expect(fare, closeTo(86.625, 0.001));
    });

    test('should not return negative fare', () {
      final fare = calculateFare(
        baseFare: 0,
        perKm: 0,
        distanceKm: 0,
        platformFeePercentage: 0,
        gstPercentage: 0,
      );

      expect(fare, greaterThanOrEqualTo(0));
    });
  });
}
```

---

## ৬.৩ উইজেট টেস্ট উদাহরণ

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('OTP screen shows 6 input boxes', (tester) async {
    await tester.pumpWidget(
      const MaterialApp(
        home: Scaffold(
          body: Center(
            child: Text('OTP Screen Placeholder'),
          ),
        ),
      ),
    );

    expect(find.text('OTP Screen Placeholder'), findsOneWidget);
  });
}
```

---

## ৬.৪ ইন্টিগ্রেশন টেস্ট — মূল ফ্লো

প্রতিটি মূল ফ্লো ইন্টিগ্রেশন টেস্টে কভার করতে হবে:

### রাইডার ফ্লো

1. লগইন
2. পিকআপ ও ড্রপ নির্বাচন
3. ফেয়ার এস্টিমেট
4. রাইড রিকোয়েস্ট
5. ড্রাইভার অ্যাসাইন
6. লাইভ ট্র্যাকিং
7. রাইড সম্পন্ন
8. পেমেন্ট
9. রেটিং
10. রসিদ

### ড্রাইভার ফ্লো

1. লগইন
2. ডকুমেন্ট আপলোড
3. অ্যাডমিন অনুমোদন
4. অনলাইন হওয়া
5. রাইড অফার পাওয়া
6. রাইড গ্রহণ
7. পিকআপে যাওয়া
8. OTP যাচাই
9. ট্রিপ শুরু
10. ট্রিপ শেষ
11. আয় দেখা

### অ্যাডমিন ফ্লো

1. লগইন
2. ড্যাশবোর্ড দেখা
3. রাইড মনিটর করা
4. KYC অনুমোদন
5. রাইড বাতিল/রিঅ্যাসাইন
6. সেফটি ইনসিডেন্ট হ্যান্ডলিং

---

## ৬.৫ ব্যাকএন্ড টেস্ট — NestJS উদাহরণ

```typescript
import { Test } from '@nestjs/testing';
import { RidesService } from './rides.service';
import { PrismaService } from '../prisma/prisma.service';
import { DispatchService } from '../dispatch/dispatch.service';

describe('RidesService', () => {
  let ridesService: RidesService;

  const mockPrisma = {
    ride: {
      create: jest.fn(),
      findUnique: jest.fn(),
      update: jest.fn(),
    },
  };

  const mockDispatch = {
    dispatch: jest.fn(),
  };

  beforeEach(async () => {
    const moduleRef = await Test.createTestingModule({
      providers: [
        RidesService,
        { provide: PrismaService, useValue: mockPrisma },
        { provide: DispatchService, useValue: mockDispatch },
      ],
    }).compile();

    ridesService = moduleRef.get<RidesService>(RidesService);
  });

  it('should estimate fare', async () => {
    const result = await ridesService.estimate({
      pickupLat: 22.5726,
      pickupLng: 88.3639,
      dropLat: 22.5958,
      dropLng: 88.2639,
      rideTypeId: 'bike',
    } as any);

    expect(result.success).toBe(true);
    expect(result.data.fare_final).toBeGreaterThan(0);
  });
});
```

---

## ৬.৬ API টেস্ট — কমান্ড লাইন উদাহরণ

### `health.http`

```http
GET https://api.dev.coopride.in/v1/health
```

### OTP request

```http
POST https://api.dev.coopride.in/v1/auth/otp/request
Content-Type: application/json

{
  "phone_country_code": "+91",
  "phone_number": "9876543210",
  "channel": "SMS"
}
```

### Ride estimate

```http
POST https://api.dev.coopride.in/v1/rider/rides/estimate
Authorization: Bearer {{access_token}}
Content-Type: application/json

{
  "pickup_lat": 22.5726,
  "pickup_lng": 88.3639,
  "drop_lat": 22.5958,
  "drop_lng": 88.2639,
  "ride_type_id": "bike"
}
```

---

## ৬.৭ লোড টেস্ট — k6

```javascript
import http from 'k6/http';
import { check, sleep } from 'k6';

export const options = {
  vus: 50,
  duration: '2m',
  thresholds: {
    http_req_duration: ['p(95)<800'],
    http_req_failed: ['rate<0.02'],
  },
};

const BASE_URL = __ENV.BASE_URL || 'https://api.dev.coopride.in/v1';

export default function () {
  const payload = JSON.stringify({
    pickup_lat: 22.5726,
    pickup_lng: 88.3639,
    drop_lat: 22.5958,
    drop_lng: 88.2639,
    ride_type_id: 'bike',
  });

  const res = http.post(`${BASE_URL}/rider/rides/estimate`, payload, {
    headers: {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${__ENV.TEST_TOKEN}`,
    },
  });

  check(res, {
    'status is 200': (r) => r.status === 200,
    'fare_final exists': (r) =>
      r.json('data.fare_final') !== undefined,
  });

  sleep(1);
}
```

### লোড টেস্ট লক্ষ্য

| মেট্রিক | লক্ষ্য |
|---|---:|
| Ride estimate p95 latency | < 800ms |
| Ride create p95 latency | < 1200ms |
| Location batch API p95 latency | < 400ms |
| Error rate | < 2% |
| Concurrent active rides | প্রথমে 1,000, পরে 10,000 |
| Driver location updates/sec | 1,000+ |

---

## ৬.৮ ফিল্ড টেস্টিং চেকলিস্ট

### রাইডার অ্যাপ

- [ ] দুর্বল 4G নেটওয়ার্কে রাইড বুকিং
- [ ] WiFi থেকে 4G সুইচ
- [ ] রাইড চলাকালীন অ্যাপ ব্যাকগ্রাউন্ডে
- [ ] GPS signal loss
- [ ] SOS বাটন টেস্ট
- [ ] পেমেন্ট টাইমআউট
- [ ] ক্যাশ রাইড সম্পন্ন
- [ ] রেটিং সাবমিট
- [ ] রসিদ ডাউনলোড

### ড্রাইভার অ্যাপ

- [ ] ব্যাকগ্রাউন্ড লোকেশন ২ ঘণ্টা চালু
- [ ] ব্যাটারি ড্রেইন পরিমাপ
- [ ] লো-এন্ড ডিভাইসে পারফরম্যান্স
- [ ] রাইড অফার পপআপ দ্রুত আসা
- [ ] অ্যাকসেপ্ট টাইমআউট
- [ ] নেভিগেশন অ্যাপে গিয়ে ফিরে আসা
- [ ] অফলাইন কুই কাজ করছে কি না
- [ ] আয় হিসাব সঠিক কি না
- [ ] ডকুমেন্ট আপলোড দুর্বল নেটওয়ার্কে

### অ্যাডমিন

- [ ] লাইভ রাইড আপডেট
- [ ] SOS অ্যালার্ট
- [ ] KYC approval
- [ ] রাইড রিঅ্যাসাইন
- [ ] রিপোর্ট এক্সপোর্ট

---

## ৬.৯ ডিভাইস টেস্ট ম্যাট্রিক্স

### অ্যান্ড্রয়েড

| ডিভাইস ক্যাটাগরি | উদাহরণ |
|---|---|
| Low-end | 2GB/3GB RAM, Android 11/12 |
| Mid-range | 4GB/6GB RAM, Android 12/13 |
| High-end | 8GB+ RAM, Android 13/14 |
| Xiaomi | Redmi series |
| Samsung | M/A series |
| Realme | Narzo/Number series |
| OnePlus | Nord series |

### iOS

| ডিভাইস | ন্যূনতম |
|---|---|
| iPhone SE 2 | iOS 15+ |
| iPhone 11 | iOS 16+ |
| iPhone 12/13/14 | iOS 16/17 |

---

## ৬.১০ সিকিউরিটি টেস্টিং

- [ ] JWT expire হলে refresh flow
- [ ] Invalid token rejection
- [ ] Role-based access enforcement
- [ ] Rider অন্যের রাইড access করতে পারছে না
- [ ] Driver অন্যের আয় দেখতে পারছে না
- [ ] Admin API RBAC সঠিক
- [ ] File upload validation
- [ ] SQL injection protection
- [ ] XSS protection admin web-এ
- [ ] Rate limiting OTP endpoint
- [ ] OTP brute-force protection
- [ ] Webhook signature validation
- [ ] Sensitive logs masked
- [ ] Aadhaar/KYC data access control

---

## ৬.১১ পাইলট টেস্টিং প্ল্যান

### সপ্তাহ ১

- ১০ ড্রাইভার
- ৫০ রাইডার
- ১টি ছোট এলাকা
- শুধুমাত্র ক্যাশ পেমেন্ট
- ২৪/৭ মনিটরিং

### সপ্তাহ ২

- ২৫ ড্রাইভার
- ২০০ রাইডার
- UPI পেমেন্ট চালু
- সেফটি ইনসিডেন্ট ড্রিল

### সপ্তাহ ৩

- ৫০ ড্রাইভার
- ৫০০ রাইডার
- পিক আওয়ার টেস্ট
- বৃষ্টি/রাতের টেস্ট

### সপ্তাহ ৪

- পূর্ণ পাইলট রিপোর্ট
- বাগ শ্রেণিবিন্যাস
- স্কেল-আপ সিদ্ধান্ত

---

## ৬.১২ টেস্ট রিপোর্ট টেমপ্লেট

```text
তারিখ:
এনভায়রনমেন্ট:
টেস্টার:
মডিউল:

পাস করা টেস্ট:
ফেইল করা টেস্ট:
ব্লকড টেস্ট:
ক্রিটিক্যাল বাগ:
হাই বাগ:
মিডিয়াম বাগ:
লো বাগ:
পারফরম্যান্স পর্যবেক্ষণ:
ব্যাটারি পর্যবেক্ষণ:
নেটওয়ার্ক পর্যবেক্ষণ:
পরবর্তী পদক্ষেপ:
```

---

## ৬.১৩ রিলিজ গেট

প্রোডাকশনে যাওয়ার আগে এই শর্তগুলো পূরণ হতে হবে:

- [ ] সব ক্রিটিক্যাল বাগ সমাধান
- [ ] কোনো হাই সিকিউরিটি ইস্যু খোলা নেই
- [ ] লোড টেস্ট পাস
- [ ] পেমেন্ট টেস্ট পাস
- [ ] লেজার ব্যালেন্সিং পাস
- [ ] ব্যাকগ্রাউন্ড লোকেশন ২ ঘণ্টা+ স্থিতিশীল
- [ ] ডেটা ব্যাকআপ ও রিস্টোর টেস্ট পাস
- [ ] অ্যাডমিন RBAC পাস
- [ ] সেফটি ড্রিল সম্পন্ন
- [ ] লিগ্যাল/কমপ্লায়েন্স সাইন-অফ
- [ ] পাইলট সাপোর্ট টিম প্রস্তুত

---

# চূড়ান্ত বাস্তবায়ন সুপারিশ

এখন আপনার হাতে আছে:

1. এপিআই স্পেসিফিকেশন  
2. ফ্লাটার কোড স্টার্টার  
3. ব্যাকএন্ড স্কেলেটন  
4. CI/CD পাইপলাইন  
5. ডাটাবেস মাইগ্রেশন  
6. টেস্টিং স্ট্র্যাটেজি  

এরপর বাস্তবায়নের ধাপ:

1. প্রথমে রিপোজিটরি তৈরি করুন:
   - `packages/core`
   - `packages/design_system`
   - `packages/features`
   - `apps/rider`
   - `apps/driver`
   - `apps/admin_mobile`
   - `apps/admin_web`

2. ব্যাকএন্ডে আগে তৈরি করুন:
   - Auth
   - Users
   - Rides
   - Driver location
   - Admin basic

3. ফ্লাটারে আগে তৈরি করুন:
   - লগইন
   - ম্যাপ
   - রাইড রিকোয়েস্ট
   - ড্রাইভার অনলাইন
   - লাইভ ট্র্যাকিং

4. পেমেন্ট, লেজার, সেফটি যোগ করুন মূল ফ্লো স্থিতিশীল হওয়ার পর।

5. পাইলটের আগে কমপক্ষে ৪ সপ্তাহ ফিল্ড টেস্ট করুন।